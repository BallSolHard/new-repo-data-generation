A financial application is hosted in an EC2 instance launched in a private subnet. It must fetch and process the data stored in an Amazon S3 bucket to generate a PDF report file that will be uploaded back to the bucket. The traffic between the EC2 instance and Amazon S3 should not leave the Amazon network to protect confidential information.

What should the Network Engineer do to satisfy this requirement in the most cost-effective manner?

- Set up a NAT Gateway to privately access the S3 bucket.
- Set up an Egress-Only Internet Gateway to privately access the S3 bucket.
- Set up an Interface VPC endpoint to privately access the S3 bucket.
- **Set up a Gateway VPC endpoint to privately access the S3 bucket.**
    
    A **VPC endpoint** enables you to privately connect your VPC to supported AWS services and VPC endpoint services powered by PrivateLink without requiring an internet gateway, NAT device, VPN connection, or AWS Direct Connect connection. Instances in your VPC do not require public IP addresses to communicate with resources in the service. Traffic between your VPC and the other service does not leave the Amazon network.
    
    **Endpoints** are virtual devices. They are horizontally scaled, redundant, and highly available VPC components that allow communication between instances in your VPC and services without imposing availability risks or bandwidth constraints on your network traffic.
    
    There are two types of VPC endpoints:
    
    1. Interface endpoint
    2. Gateway endpoint
    
    An [interface endpoint](https://docs.aws.amazon.com/vpc/latest/userguide/vpce-interface.html) is an elastic network interface with a private IP address from the IP address range of your subnet that serves as an entry point for traffic destined to a supported service. Interface endpoints are powered by AWS PrivateLink, a technology that enables you to privately access services by using private IP addresses. AWS PrivateLink restricts all network traffic between your VPC and services to the Amazon network. You do not need an internet gateway, a NAT device, or a virtual private gateway.
    
    ![](https://media.tutorialsdojo.com/Interface-vs-Gateway-VPC-Endpoint.png)
    
    A [gateway endpoint](https://docs.aws.amazon.com/vpc/latest/userguide/vpce-gateway.html) is a gateway that you specify as a target for a route in your route table for traffic destined to a supported AWS service. The following AWS services are supported:
    
    – Amazon S3
    
    – DynamoDB
    
    You can use two types of VPC endpoints to access Amazon S3: *gateway endpoints* and *interface endpoints*. A *gateway endpoint* is a gateway that you specify in your route table to access Amazon S3 from your VPC over the AWS network. *Interface endpoints* extend the functionality of gateway endpoints by using private IP addresses to route requests to Amazon S3 from within your VPC, on-premises, or from a VPC in another AWS Region using VPC peering or AWS Transit Gateway
    
    Hence, the correct answer is: **Set up a Gateway VPC endpoint to privately access the S3 bucket.** A Gateway VPC endpoint is more cost-effective than an Interface VPC endpoint.
    
    The option that says: **Set up a NAT Gateway to privately access the S3 bucket** is incorrect because a NAT Gateway simply enables EC2 instances in a private subnet to connect to the Internet or other AWS services, but prevent the Internet from initiating a connection with those instances.
    
    The option that says: **Set up an Egress-Only Internet Gateway to privately access the S3 bucket** is incorrect because an egress-only Internet Gateway is just the same thing as a NAT Gateway that only supports IPv6 network. The most suitable solution in this scenario is to create a VPC Endpoint in order to privately access the S3 bucket.
    
    The option that says: **Set up an Interface VPC endpoint to privately access the S3 bucket** is incorrect. Remember that you won’t get billed if you use a Gateway VPC endpoint for your Amazon S3 bucket, unlike an Interface VPC endpoint which has an associated cost. Take note that the scenario explicitly asks for the most cost-effective solution.
    

An AWS Direct Connect connection has been established between the company’s on-premises data center and Amazon VPC using a private virtual interface with propagated routes. The Network team has been instructed to increase the connection bandwidth by allowing more than 1500 bytes of data and increasing the payload size per packet. This will enable the applications hosted in Amazon EC2 instances to send more data on-premises with the same amount of overhead. What should the team do to satisfy this requirement?

- **Enable the Jumbo MTU option in the private virtual interface to allow MTU size of up to 9001. Verify that the EC2 instances support jumbo frames and configure the network MTU setting of each instance to 9001.**
    
    **AWS Direct Connect** supports an Ethernet frame size of 1522 or 9023 bytes (14 bytes Ethernet header + 4 bytes VLAN tag + bytes for the IP datagram + 4 bytes FCS) at the link layer.
    
    The maximum transmission unit (MTU) of a network connection is the size, in bytes, of the largest permissible packet that can be passed over the connection. The MTU of a virtual private interface can be either 1500 or 9001 (jumbo frames). The MTU of a transit virtual interface can be either 1500 or 8500 (jumbo frames). You can specify the MTU when you create the interface or update it after you create it. Setting the MTU of a virtual interface to 8500 (jumbo frames) or 9001 (jumbo frames) can cause an update to the underlying physical connection if it wasn’t updated to support jumbo frames. Updating the connection disrupts network connectivity for all virtual interfaces associated with the connection for up to 30 seconds. To check whether a connection or virtual interface supports jumbo frames, select it in the AWS Direct Connect console and find *Jumbo Frame Capable* on the *Summary* tab.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-07_02-50-13-95c0d817ec7978ca8a1a770465fffd8f.png)
    
    After you enable jumbo frames for your private virtual interface, you can only associate it with a connection or LAG that is jumbo frame capable. Jumbo frames are supported on virtual private interfaces attached to a virtual private gateway or a Direct Connect gateway.
    
    Jumbo frames apply only to propagated routes from AWS Direct Connect. If you add static routes to your virtual private gateway, traffic that is routed through the static route defaults to 1500 MTU. If you have two private virtual interfaces that advertise the same route but use different MTU values, 1500 MTU is used.
    
    To confirm the jumbo frame compatibility of a Direct Connect connection, verify the following in your configuration:
    
    – AWS Managed VPN doesn’t support jumbo frames. If the same route is advertised over DX and AWS Managed VPN, then 1500 MTU is used.
    
    – Public virtual interfaces don’t support jumbo frames. Jumbo frames are supported only on private virtual interfaces attached to a virtual private gateway or a DX gateway.
    
    – Confirm that all nodes support jumbo frames, including the router, switch, and end devices (such as network cards). The Amazon Elastic Compute Cloud (Amazon EC2) instances in your VPN must also support jumbo frames. Otherwise, network performance could decrease because incompatible devices are likely to drop or fragment frames.
    
    – Jumbo frames apply only to propagated routes from DX. If you add static routes to a route table that points to your virtual private gateway, then traffic routed through the static routes is sent using 1500 MTU.
    
    – If two virtual interfaces advertise the same route but use different MTUs, then 1500 MTU is used for both virtual interfaces.
    
    Hence, the correct answer is: **Enable the Jumbo MTU option in the private virtual interface to allow MTU size of up to 9001. Verify that the EC2 instances support jumbo frames and configure the network MTU setting of each instance to 9001.**
    
    The option that says: **Replace the existing private virtual interface with a public virtual interface. Enable jumbo frames on the Direct Connect connection** is incorrect because public virtual interfaces don’t support jumbo frames. This is supported only on private virtual interfaces attached to a virtual private gateway or a DX gateway.
    
    The option that says: **Move all the EC2 instances in a single Cluster Placement Group to increase the connection bandwidth. The AWS Direct Connect connection will automatically use jumbo frames to send and receive data from the EC2 instances to the on-premises network** is incorrect because Direct Connect doesn’t automatically use jumbo frames. You have to manually configure the AWS Direct Connect connection to enable this feature. In addition, you must verify that the EC2 instances support jumbo frames and configure the network MTU setting of each instance to 9001.
    
    The option that says: **Add static routes to the route table that point to your virtual private gateway** is incorrect because jumbo frames only apply to propagated routes from the Direct Connect connection. If you add static routes to a route table that point to your virtual private gateway, then the traffic routed through the static routes is sent using 1500 MTU.
    
- Add static routes to the route table that point to your virtual private gateway.
- Replace the existing private virtual interface with a public virtual interface. Enable jumbo frames on the Direct Connect connection.
- Move all the EC2 instances in a single Cluster Placement Group to increase the connection bandwidth. The AWS Direct Connect connection will automatically use jumbo frames to send and receive data from the EC2 instances to the on-premises network.

An external Network Time Protocol (NTP) server is being used in the company’s Amazon VPC to synchronize system time across all of its Linux and Windows EC2 instances. There were several incidents when the existing NTP server goes down, which affects the mission-critical batch process in the production environment. A Network Engineer has been instructed to set alternate NTP source in the VPC. The NTP server update must be propagated without having to stop or reboot the already running EC2 instances.

What must the Engineer do to complete this task?

- Modify the NTP Servers of the existing DHCP Options set that is associated with the VPC.
- Update the user data of all of the EC2 instances using the AWS Systems Manager Session Manager.
- Use the Amazon Time Sync Service to update the associated NTP server of the VPC to the new one.
- **Create a new DHCP Options set with the updated NTP server. Replace the existing DHCP options set association of the VPC with the newly created one.**
    
    The Dynamic Host Configuration Protocol (DHCP) provides a standard for passing configuration information to hosts on a TCP/IP network. The options field of a DHCP message contains configuration parameters, including the domain name, domain name server, and the netbios-node-type. You can configure DHCP option sets for your virtual private clouds (VPCs).
    
    After you create a set of DHCP options, you can’t modify them. If you want your VPC to use a different set of DHCP options, you must create a new set and associate them with your VPC. You can also set up your VPC to use no DHCP options at all.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-11_06-30-06-1e289a02f61bb7b82ff7dfe54df9f032.png)
    
    After you associate a new set of DHCP options with the VPC, any existing instances and all new instances that you launch in that VPC use the new options. You don’t need to restart or relaunch the instances. They automatically pick up the changes within a few hours, depending on how frequently the instance renews its DHCP lease. If you want, you can explicitly renew the lease using the operating system on the instance.
    
    Hence, the correct answer is: **Create a new DHCP Options set with the updated NTP server. Replace the existing DHCP options set association of the VPC with the newly created one.**
    
    The option that says: **Modify the NTP Servers of the existing DHCP Options set that is associated with the VPC** is incorrect because after you create a set of DHCP options, you won’t be able to modify them directly. If you want your VPC to use a different set of DHCP options, you must create a new set and associate them with your VPC.
    
    The option that says: **Update the user data of all of the EC2 instances using the AWS Systems Manager Session Manager** is incorrect because a user data is only used to perform common automated configuration tasks and run scripts after the instance starts. Therefore, the commands to change the NTP server in your user data for each EC2 instance will not be reflected unless you restart the instance.
    
    The option that says: **Use the Amazon Time Sync Service to update associated NTP server of the VPC to the new one** is incorrect because the Amazon Time Sync Service is already an NTP service and not just a utility that you use to point to the new external NTP source.
    

A company is launching a web application that will be hosted in an Amazon ECS cluster with an EC2 launch type. The Network Engineer configured the associated security group and network ACL of the instances to allow inbound traffic on ports 80 and 443. After the deployment, the QA team noticed that the application is unreachable over the public Internet.

What should the Engineer do to rectify this issue?

- **Ensure that the network ACL has a rule that allows Outbound traffic on the ephemeral ports 1024 – 65535.**
    
    To enable the connection to a service running on an instance, the associated network ACL must allow both inbound traffic on the port that the service is listening on as well as allow outbound traffic from ephemeral ports. When a client connects to a server, a random port from the ephemeral port range (1024-65535) becomes the client’s source port.
    
    The designated ephemeral port then becomes the destination port for return traffic from the service, so outbound traffic from the ephemeral port must be allowed in the network ACL. By default, network ACLs allow all inbound and outbound traffic. If your network ACL is more restrictive, then you need to explicitly allow traffic from the ephemeral port range.
    
    ![](https://media.tutorialsdojo.com/public/2020-01-28_09-08-44-2d1d017db4b5bf572b45417a00aeac63.png)
    
    You might want to use a different range for your network ACLs depending on the type of client that you’re using or with which you’re communicating. The client that initiates the request chooses the ephemeral port range. The range varies depending on the client’s operating system.
    
    - Many Linux kernels (including the Amazon Linux kernel) use ports 32768-61000.
    - Requests originating from Elastic Load Balancing use ports 1024-65535.
    - Windows operating systems through Windows Server 2003 use ports 1025-5000.
    - Windows Server 2008 and later versions use ports 49152-65535.
    - A NAT gateway uses ports 1024-65535.
    - AWS Lambda functions use ports 1024-65535.
    
    For example, if a request comes into a web server in your VPC from a Windows XP client on the internet, your network ACL must have an outbound rule to enable traffic destined for ports 1025-5000.
    
    If an instance in your VPC is the client initiating a request, your network ACL must have an inbound rule to enable traffic destined for the ephemeral ports specific to the type of instance (Amazon Linux, Windows Server 2008, and so on).
    
    In practice, to cover the different types of clients that might initiate traffic to public-facing instances in your VPC, you can open ephemeral ports 1024-65535. However, you can also add rules to the ACL to deny traffic on any malicious ports within that range. Ensure that you place the *deny* rules earlier in the table than the *allow* rules that open the wide range of ephemeral ports.
    
    Hence, the correct answer is: ****Ensure that the network ACL has a rule that allows Outbound traffic on the ephemeral ports 1024 – 65535.**
    
    The option that says: **Ensure that the security group has a rule that allows outbound traffic on port 80 and port 443** is incorrect because Security groups are stateful, which means that if you send a request from your instance, the response traffic for that request is allowed to flow in regardless of inbound security group rules. An allowed inbound traffic is also permitted to flow out, regardless of outbound rules.
    
    The option that says: ****Verify that the network ACL has a rule that allows Inbound traffic on the ephemeral ports 1024 – 65535** is incorrect because you should allow the Outbound traffic to the ephemeral port range (1024-65535) in the network ACL and not the Inbound traffic.
    
    The option that says: **Set the network mode to `bridge` to ensure that every task that is launched from the task definition gets its own elastic network interface (ENI) and a primary private IP address** is incorrect. If the network mode is set to `bridge`, the task utilizes Docker’s built-in virtual network, which runs inside each container instance. A better solution is to use the `awsvpc` network mode. When you use the `awsvpc` network mode in your task definitions, every task that is launched from that task definition gets its elastic network interface (ENI) and a primary private IP address.
    
- Ensure that the security group has a rule that allows outbound traffic on port 80 and port 443.
- Verify that the network ACL has a rule that allows Inbound traffic on the ephemeral ports 1024 – 65535.
- Set the network mode to `bridge` to ensure that every task that is launched from the task definition gets its own elastic network interface (ENI) and a primary private IP address.

A company has a hybrid network architecture that consists of multiple regional on-premises data centers and hundreds of AWS VPCs in various AWS Regions. The Network Administrator has been tasked to connect all of the company’s VPCs, on-premises networks, SD-WAN (Software-Defined Wide Area Network) virtual appliances, and VPN connections into a single gateway. The solution should also support inter-region peering across multiple AWS regions and provide higher bandwidth performance than a regular VPN connection.

What’s the MOST suitable solution that the Administrator should implement to build the requested network architecture?

- Enable inter-region VPC peering and use AWS Managed VPN that supports static routes and dynamic Border Gateway Protocol (BGP) peering and routing policies. Consolidate all external networks by using the AWS Resource Access Manager (AWS RAM).
- **Use AWS Transit Gateway to connect the geographically dispersed VPCs and remote networks in order to create a global network transit center. Create Transit Gateway Connect attachments to establish a connection to third-party SD-WAN virtual appliances via AWS Direct Connect and Generic Routing Encapsulation (GRE) tunnels.**
    
    **AWS Transit Gateway** is a service that enables customers to connect their Amazon Virtual Private Clouds (VPCs) and their on-premises networks to a single gateway. As you grow the number of workloads running on AWS, you need to be able to scale your networks across multiple accounts and Amazon VPCs to keep up with the growth. Today, you can connect pairs of Amazon VPCs using peering. However, managing point-to-point connectivity across many Amazon VPCs, without the ability to centrally manage the connectivity policies can be operationally costly and cumbersome. For on-premises connectivity, you need to attach your AWS VPN to each individual Amazon VPC. This solution can be time-consuming to build and hard to manage when the number of VPCs grows into the hundreds.
    
    ![](https://media.tutorialsdojo.com/public/cisco-based-transit-vpc-architecture.b22d83fc90ca68b1e4f6e516ecc2091c303aa8eb.png)
    
    With AWS Transit Gateway, you only have to create and manage a single connection from the central gateway into each Amazon VPC, on-premises data center, or remote office across your network. Transit Gateway acts as a hub that controls how traffic is routed among all the connected networks, which act like spokes. This hub and spoke model significantly simplifies management and reduces operational costs because each network only has to connect to the Transit Gateway and not to every other network. Any new VPC is simply connected to the Transit Gateway and is then automatically available to every other network that is connected to the Transit Gateway. This ease of connectivity makes it easy to scale your network as you grow.
    
    You can create a transit gateway Connect attachment to establish a connection between a transit gateway and third-party virtual appliances (such as SD-WAN appliances) running in a VPC. A Connect attachment supports the Generic Routing Encapsulation (GRE) tunnel protocol for high performance and Border Gateway Protocol (BGP) for dynamic routing. After you create a Connect attachment, you can create one or more GRE tunnels (also referred to as Transit Gateway Connect peers) on the Connect attachment to connect the transit gateway and the third-party appliance. You establish two BGP sessions over the GRE tunnel to exchange routing information. The two BGP sessions are for redundancy.
    
    A Connect attachment uses an existing VPC or AWS Direct Connect attachment as the underlying transport mechanism. This is referred to as the transport attachment. The transit gateway identifies matched GRE packets from the third-party appliance as traffic from the Connect attachment. It treats any other packets, including GRE packets with incorrect source or destination information, as traffic from the transport attachment.
    
    Hence, the correct answer is: **Use AWS Transit Gateway to connect the geographically dispersed VPCs and remote networks in order to create a global network transit center. Create Transit Gateway Connect attachments to establish a connection to third-party SD-WAN virtual appliances via AWS Direct Connect and Generic Routing Encapsulation (GRE) tunnels.**
    
    The option that says: **Using an AWS Direct Connect Gateway to achieve inter-region VPC access to all of the AWS resources and on-premises data centers. Connect the SD-WAN virtual appliances to your cloud architecture using the AWS Cloud Map service** is incorrect because a Direct Connect Gateway can only manage a single connection for multiple VPCs or VPNs that are in the same Region. Using multiple Direct Connect Gateways is operationally costly and cumbersome to implement. Remember that the company has several VPCs and VPN connections in multiple AWS Regions. In addition, AWS Cloud Map is a cloud resource discovery service and is not capable of directly connecting to an SD-WAN virtual appliance.
    
    The option that says: **Set up AWS VPN CloudHub to achieve inter-region VPC access to all of the AWS resources and on-premises data centers. Establish connection to the SD-WAN virtual appliances using Amazon Route 53 Resolver DNS Firewall** is incorrect because this solution would require a lot of manual setups and management overhead to successfully build a functional, error-free inter-region VPC network, as compared with just using a Transit Gateway. AWS VPN CloudHub can connect a single VPC to multiple on-premises data centers. This solution is not capable of connecting the company’s on-premises data centers in multiple AWS Regions using a single gateway. It is also not possible to establish a connection to your SD-WAN virtual appliances using the Amazon Route 53 Resolver DNS Firewall service.
    
    The option that says: **Enable inter-region VPC peering and use AWS Managed VPN that supports static routes and dynamic Border Gateway Protocol (BGP) peering and routing policies. Consolidate all external networks by using the AWS Resource Access Manager (AWS RAM)** is incorrect because this solution is not capable of managing hundreds of VPCs with multiple VPN connections to their data centers that span to multiple AWS Regions. Inter-Region VPC Peering and AWS Managed VPN can’t interconnect all of the company’s on-premises networks, VPNs, and VPCs into a single gateway. You have to use a Transit Gateway instead.
    
- Use AWS Direct Connect Gateway to achieve inter-region VPC access to all of the AWS resources and on-premises data centers. Connect the SD-WAN virtual appliances to your cloud architecture using the AWS Cloud Map service
- Set up AWS VPN CloudHub to achieve inter-region VPC access to all of the AWS resources and on-premises data centers. Establish connection to the SD-WAN virtual appliances using Amazon Route 53 Resolver DNS Firewall

An enterprise application is hosted in Amazon EKS on Fargate with an Amazon VPC Container Network Interface (CNI) plugin configured. The application hosts corporate report files that are accessed by millions of people around the world.

The company decided to launch a new security feature to prevent users in specific geographic locations from accessing the website’s static content. The solution should also reduce the number of simultaneous requests that are sent to your origin and improve the cache hit ratio.

What should be done to enable this new feature?

- Integrate AWS Global Accelerator to the Application Load Balancer and enable the Geo-Restriction feature. Launch a Route 53 Resolver Inbound endpoint to improve the cache hit ratio.
- Use Geoproximity routing in Amazon Route 53 to route traffic only to the whitelisted geographic locations. Integrate a CloudFront function to reduce the number of simultaneous requests that are sent to your origin and improve the cache hit ratio.
- **Launch a CloudFront distribution and set the enterprise application as the origin. Enable the Geo-Restriction and Origin Shield features in the Amazon CloudFront distribution.**
    
    You can use *geo-restriction*, also known as *geo-blocking*, to prevent users in specific geographic locations from accessing content that you’re distributing through a CloudFront web distribution. To use geo-restriction, you have two options:
    
    **– Use the CloudFront geo restriction feature.** Use this option to restrict access to all of the files that are associated with a distribution and to restrict access at the country level.
    
    **– Use a third-party geolocation service.** Use this option to restrict access to a subset of the files that are associated with a distribution or to restrict access at a finer granularity than the country level.
    
    ![](https://media.tutorialsdojo.com/amazon-cloudfront-geo-restriction-ans-c01.png)
    
    Here’s how geo-restriction works:
    
    1. Suppose you have the right to distribute your content only in the Philippines. You update your CloudFront web distribution and add a whitelist that contains only the Philippines. (Alternatively, you could add a blacklist that contains every country except the Philippines.)
    2. A user in India requests your content, and DNS routes the request to the CloudFront edge location in Singapore.
    3. The edge location in Singapore looks up your distribution and determines that the user in India is not allowed to download your content.
    4. CloudFront returns an HTTP status code 403 (Forbidden) to the user.
    
    Geo restriction applies to the entire web distribution. If you need to apply one restriction to a part of your content and a different restriction (or no restriction) to another part, you must either create separate CloudFront web distributions or use a third-party geolocation service.
    
    ![](https://media.tutorialsdojo.com/amazon-cloudfront-originshield-ans-c01.png)
    
    The Origin Shield is a centralized caching layer in Amazon CloudFront that helps increase your cache hit ratio to reduce the load on your origin. Origin Shield also decreases your origin operating costs by collapsing requests across regions, so as few as one request goes to your origin per object. You can also use Lambda@Edge with Origin Shield to enable advanced serverless logic like dynamic origin load balancing.
    
    Hence, the correct answer is: **Launch a CloudFront distribution and set the enterprise application as the origin. Enable the Geo-Restriction and Origin Shield features in the Amazon CloudFront distribution.**
    
    The option says: **Integrate AWS Global Accelerator to the Application Load Balancer and enable the Geo-Restriction feature. Launch a Route 53 Resolver Inbound endpoint to improve the cache hit ratio** is incorrect because there is no Geo-Restriction feature in AWS Global Accelerator. You have to use CloudFront instead. Moreover, a Route 53 Resolver Inbound endpoint is simply a type of DNS resolver that can easily resolve domain names of your AWS resources, such as EC2 instances or records in a Route 53 private hosted zone.
    
    The option says: **Configure a Geolocation routing policy in the associated Amazon Route 53 record of the application. Launch a Route 53 Resolver Outbound endpoint to reduce the number of simultaneous requests that are sent to the origin** is incorrect because Geolocation routing just lets you choose the resources that serve your traffic based on the geographic location of your users, or in other words, the location where the DNS queries originate from. You have to launch a CloudFront distribution and enable the Geo-Restriction feature to satisfy the requirements of this scenario. A Route 53 Resolver Outbound endpoint is not necessary for this scenario too. This just conditionally forwards queries to the DNS resolvers hosted on your on-premises network.
    
    The option says: **Use Geoproximity routing in Amazon Route 53 to route traffic only to the whitelisted geographic locations. Integrate a CloudFront function to reduce the number of simultaneous requests that are sent to your origin and improve the cache hit ratio** is incorrect because Geoproximity routing simply lets Amazon Route 53 route traffic to your resources based on the geographic location of your users and your resources. This can’t be used to restrict a specific country or a list of countries from accessing your website. Furthermore, a CloudFront function is not capable of reducing the number of simultaneous requests that are sent to your origin; let alone, improving the cache hit ratio of your CloudFront distribution.
    
- Configure a Geolocation routing policy in the associated Amazon Route 53 record of the application. Launch a Route 53 Resolver Outbound endpoint to reduce the number of simultaneous requests that are sent to the origin.

A company needs to establish network connectivity between its Amazon VPCs and on-premises network using multiple AWS Site-to-Site VPN connections that are associated with a Transit Gateway. The Network Engineer has been assigned to increase the traffic bandwidth over multiple paths and get a higher VPN bandwidth beyond the default 1.25 Gbps limit.

Currently, the Site-to-Site VPN connections only support IPv4 traffic. There’s a new requirement to update these VPN connections to allow IPv6 communication between the company’s AWS resources and on-premises servers.

Which combination of steps below provides the most operationally efficient solution to satisfy the requirement? (Select TWO.)

- **Set up Equal-Cost Multi-Path (ECMP) routing. Ensure that VPN ECMP Support and Dynamic VPN options are enabled in the Transit Gateway.**
- Set up a private Multiprotocol Label Switching (MPLS) network in your on-premises data center. Launch a multicast domain on the Transit Gateway to further improve the bandwidth.
- Increase the number of Site-to-Site VPN tunnels within each VPN connection to scale bandwidth beyond the 1.25 Gbps limit.
- **Launch a new set of Site-to-Site VPN connections with IPv6 traffic enabled and replace the existing ones.**
- Use Jumbo Frames by setting the MTU to 9001 and implement Q-in-Q Tunnels by adding a second 802.1Q tag to an already tagged frame
- **Evaluation**
    
    AWS Site-to-Site VPN offers customizable tunnel options, including inside tunnel IP address, pre-shared key, and Border Gateway Protocol Autonomous System Number (BGP ASN). In this way, you can set up multiple secure VPN tunnels to increase the bandwidth for your applications or for resiliency in case of downtime.
    
    ![Multiple VPN Tunnels diagram](https://media.tutorialsdojo.com/public/Multiple-Tunnels.png)
    
    Equal-cost multi-path routing (ECMP) is available with AWS Site-to-Site VPN on AWS Transit Gateway to help increase the traffic bandwidth over multiple paths. You can use ECMP to get higher VPN bandwidth than the default VPN bandwidth limit of 1.25 Gbps by aggregating multiple VPN connections.
    
    You have to confirm that your customer gateway is configured to perform ECMP for traffic going out to AWS for all VPN tunnels. If necessary, configure your customer gateway BGP to accept the route from AWS so that the customer gateway installs all the routes with the same metric. You will also have to verify that your customer gateway is advertising the on-premises prefix to AWS with the same BGP AS PATH attribute. For AWS to choose all the available ECMP paths, the AS Path and AS Number must match.
    
    Your Site-to-Site VPN connection on a transit gateway can support either IPv4 traffic or IPv6 traffic inside the VPN tunnels. By default, a Site-to-Site VPN connection supports IPv4 traffic inside the VPN tunnels. You can configure a new Site-to-Site VPN connection to support IPv6 traffic inside the VPN tunnels. Then, if your VPC and your on-premises network are configured for IPv6 addressing, you can send IPv6 traffic over the VPN connection.
    
    ![Creating VPN connection](https://media.tutorialsdojo.com/aws-vpn-connection-types-accelerated-VPN.png)
    
    If you enable IPv6 for the VPN tunnels for your Site-to-Site VPN connection, each tunnel has two CIDR blocks. One is a size /30 IPv4 CIDR block, and the other is a size /126 IPv6 CIDR block.
    
    The following rules apply:
    
    – IPv6 addresses are only supported for the inside IP addresses of the VPN tunnels. The outside tunnel IP addresses for the AWS endpoints are IPv4 addresses, and the public IP address of your customer gateway must be an IPv4 address.
    
    – Site-to-Site VPN connections on a virtual private gateway do not support IPv6.
    
    – You cannot enable IPv6 support for an existing Site-to-Site VPN connection.
    
    For ECMP to function properly, **Dynamic VPN** and **VPN ECMP Support** must be enabled on the transit gateway. The **VPN ECMP Support** option can only be enabled or disabled when you create a transit gateway.
    
    Hence, the correct answers are:
    
    **– Launch a new set of Site-to-Site VPN connections with IPv6 traffic enabled and replace the existing ones.**
    
    **– Set up Equal-Cost Multi-Path (ECMP) routing. Ensure that VPN ECMP Support and Dynamic VPN options are enabled in the Transit Gateway.**
    
    The option that says: **Set up a private Multiprotocol Label Switching (MPLS) network in your on-premises data center. Launch a multicast domain on the Transit Gateway to further improve the bandwidth** is incorrect because MPLS doesn’t increase the traffic bandwidth of the VPN connection over multiple paths nor provide a higher VPN bandwidth beyond the default 1.25 Gbps limit. In the MPLS forwarding paradigm, once a packet is assigned to an FEC (Forwarding Equivalence Classes), no further header analysis is done by subsequent routers; all forwarding is driven by the labels. This is the MPLS advantage over conventional network layer forwarding but not to the connection bandwidth. Moreover, launching a multicast domain on the Transit Gateway will not improve the bandwidth of your VPN connections.
    
    The option that says: **Use Jumbo Frames by setting the MTU to 9001 and implement Q-in-Q Tunnels by adding a second 802.1Q tag to an already tagged frame** is incorrect because AWS Managed VPN doesn’t support jumbo frames. If the same route is advertised over DX and AWS Managed VPN, then 1500 MTU is used. A Q-in-Q Tunnel simply enables a service provider to segregate the traffic of different customers in their infrastructure while still giving the customer a full range of VLANs for their internal use.
    
    The option that says: **Increase the number of Site-to-Site VPN tunnels within each VPN connection to scale bandwidth beyond the 1.25 Gbps limit** is incorrect because AWS only supports two tunnels per Site-to-Site VPN connection. To achieve higher bandwidth, multiple separate Site-to-Site VPN connections must be created and ECMP (Equal-Cost Multi-Path) routing must be enabled in AWS Transit Gateway.
    

A company is planning for a mission-critical application that requires high-speed data transmission between its data center and AWS. A network engineer assessed the required bandwidth and recommended an Amazon Direct Connect (DX) connection with a link capacity of 10 Gbps. Due to strict requirements, all traffic must be protected by means of encryption with the least impact on network throughput.

How can the engineer meet the objective?

- **Enable MACsec support for the DX connection.**
    
    Media Access Control security (MACsec) is an IEEE 802.1AE standard for encrypting packets traveling between two MACsec-capable Ethernet switches/routers. In comparison to upper-layer protocols such as IPSec and TLS for instance, MACsec was originally designed as a hop-by-hop encryption technology. Frames are encrypted when they are sent on the wire and are decrypted by the next hop device processing the frames.
    
    Encryption of traffic at multi-gigabit speeds ( ≥ 10 Gbps) usually requires the aggregation of multiple IPsec VPN tunnels to overcome the throughput limits of using a single VPN connection. This solution is complex and less appealing to use for securing high-speed connections.
    
    ![](https://media.tutorialsdojo.com/aws-direct-connect-macsec-support.jpg)
    
    Enabling MACsec support for AWS Direct Connect delivers native, near line-rate, point-to-point encryption, making it suitable to secure connection to AWS at the highest speed available.
    
    Hence, the correct answer is: **Enable MACsec support for the DX connection**
    
    The option that says: **Apply IPsec VPN on top of a single public virtual interface (VIF) DX connection** is incorrect. The maximum throughput of an IPsec VPN usually caps at 1Gbps, which is not enough to handle the required throughput.
    
    The option that says: **Configure the application to use HTTPS** is incorrect as this will only secure HTTP traffic. Note that the scenario requires encryption of all traffic.
    
    The option that says: **Apply IPsec VPN on top of a single private virtual interface (VIF) DX connection ****is incorrect. This setup is not possible because IPsec VPN requires the use of a public VIF, not a private VIF.
    
- Apply IPsec VPN on top of a single private virtual interface (VIF) DX connection.
- Apply IPsec VPN on top of a single public virtual interface (VIF) DX connection.
- Configure the application to use HTTPS.

An online customer portal is hosted in an Amazon ECS cluster behind an Application Load Balancer. The portal is set as the origin of a CloudFront Web distribution to deliver the dynamic and static content to users in low-latency. A Cloud Engineer was assigned to configure CloudFront to communicate with your origin using HTTP or HTTPS, based on the protocol of the viewer request.

What should the Engineer implement to complete this task?

- **Set the Origin Protocol policy of the CloudFront distribution to `Match Viewer`.**
    
    For web distributions, you can configure CloudFront to require that viewers use HTTPS to request your objects so that connections are encrypted when CloudFront communicates with viewers. You also can configure CloudFront to use HTTPS to get objects from your origin so that connections are encrypted when CloudFront communicates with your origin.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-17_09-29-04-2a75f84e5ce3820ee3474cbf60d16eb5.png)
    
    In CloudFront, there are 3 options that you can choose as the value for your Origin Protocol Policy:
    
    1. HTTP Only
    2. HTTPS Only
    3. Match Viewer.
    
    For HTTP Only, CloudFront solely uses HTTP to access the origin. For HTTPS Only, CloudFront uses only HTTPS to access the origin and for Match Viewer, CloudFront communicates with your origin using HTTP or HTTPS, depending on the protocol of the viewer request. CloudFront caches the object only once even if viewers make requests using both HTTP and HTTPS protocols.
    
    Take note that the term “Viewer” in CloudFront basically means the requestor or the client that sends the request. Therefore, the name: “MatchViewer” basically means that CloudFront is simply matching the protocol being used by the viewer.
    
    Hence, the correct answer is: **Set the Origin Protocol policy of the CloudFront distribution to `Match Viewer`.**
    
    The option that says: **Set the Origin Protocol policy of the CloudFront distribution to `HTTP and HTTPS`** is incorrect because this is not a valid value for the Origin Protocol policy. The **`HTTP and HTTPS`** type is only valid with the Viewer Protocol policy.
    
    The option that says: **Set the Viewer Protocol policy of the CloudFront distribution to `HTTP and HTTPS`**is incorrect because you have to use the Origin Protocol policy and not Viewer Protocol.
    
    The option that says: **Set the Viewer Protocol policy of the CloudFront distribution to `Match Viewer`** is incorrect because the **`Match Viewer`** type is only applicable if you are using an Origin Protocol policy.
    
- Set the Origin Protocol policy of the CloudFront distribution to `HTTP and HTTPS`.
- Set the Viewer Protocol policy of the CloudFront distribution to `Match Viewer`.
- Set the Viewer Protocol policy of the CloudFront distribution to `HTTP and HTTPS`.

A company has a VPC with a private hosted zone configured using Amazon Route 53. The Network team noticed that the EC2 instances in the VPC do not have public DNS hostnames. In addition, these instances cannot resolve Amazon-provided private DNS hostnames.

What must be done to resolve this problem?

- Set the `enableDnsHostNames` attribute of your VPC to `true` and the `enableDnsSupport`attribute to `false`.
- Enable equal-cost multi-path routing (ECMP) in Amazon Route 53.
- **Set the `enableDnsHostNames` attribute of the VPC to `true` and the `enableDnsSupport`attribute to `true`.**
    
    **A** **private hosted zone** is a container that holds information about how you want Amazon Route 53 to respond to DNS queries for a domain and its subdomains within one or more VPCs that you create with the Amazon VPC service. Your VPC has attributes that determine whether your EC2 instance receives public DNS hostnames and whether DNS resolution through the Amazon DNS server is supported.
    
    **`enableDnsHostnames** -` Indicates whether the instances launched in the VPC get public DNS hostnames. If this attribute is `true`, instances in the VPC get public DNS hostnames, but only if the `enableDnsSupport` attribute is also set to `true`.
    
    **`enableDnsSupport** -` Indicates whether the DNS resolution is supported for the VPC. If this attribute is `false`, the Amazon-provided DNS server in the VPC that resolves public DNS hostnames to IP addresses is not enabled. If this attribute is `true`, queries to the Amazon-provided DNS server at the 169.254.169.253 IP address, or the reserved IP address at the base of the VPC IPv4 network range plus two ( *.*.*.2 ) will succeed.
    
    Since you want your new EC2 instances to automatically have a public DNS hostname, and you want to configure your VPC to resolve Amazon-provided private DNS hostnames as well.
    
    ![](https://media.tutorialsdojo.com/public/DrewDennis_DNSresolutionDNShostnames.png)
    
    If both attributes are set to `true`, the following occurs:
    
    - Your EC2 instance receives a public DNS hostname.
    - The Amazon-provided DNS server can resolve Amazon-provided private DNS hostnames.
    
    If either or both of the attributes is set to `false`, the following occurs:
    
    - Your instance does not receive a public DNS hostname that can be viewed in the Amazon EC2 console or described by a command line tool or AWS SDK.
    - The Amazon-provided DNS server cannot resolve Amazon-provided private DNS hostnames.
    - Your instance receives a custom private DNS hostname if you’ve specified a custom domain name in your [DHCP options set](https://docs.aws.amazon.com/vpc/latest/userguide/VPC_DHCP_Options.html). If you are not using the Amazon-provided DNS server, your custom domain name servers must resolve the hostname as appropriate.
    
    Hence, the correct answer is: **Set the `enableDnsHostNames` and `enableDnsSupport` attributes of the VPC to `true`.**
    
    The option that says: **Set the `enableDnsHostNames` attribute of your VPC to `true` and the `enableDnsSupport` attribute to `false`** is incorrect because even though the former is set to `true`, the latter should also be set to `true` in order for your new EC2 instances launched in the VPC to get public DNS hostnames.
    
    The option that says: **Enable the Open Shortest Path First (OSPF) feature in Amazon Route 53** is incorrect because there is no OSPF feature in Amazon Route 53. OSPF is just a TCP/IP internet routing protocol that distributes routing information between routers belonging to a single Autonomous System (AS).
    
    The option that says: **Enable equal-cost multi-path routing (ECMP) in Amazon Route 53** is incorrect because there is no ECMP option in Amazon Route 53. ECMP is primarily used in establishing network connectivity between virtual private clouds (VPCs) and your on-premises network using multiple VPN connections that are associated with a Transit Gateway.
    
- Enable the Open Shortest Path First (OSPF) feature in Amazon Route 53.

An application is running on an Amazon EC2 instance launched in a private subnet in us-east-1 (N. Virginia) region. A new feature will be released that requires the application to pull data from several public web services over the Internet to complete its processing. The result will be stored in an S3 bucket in the same region. The Network Engineer must restrict outbound Internet access to a list of whitelisted URLs only.

What should the Engineer do to satisfy these requirements?

- **Launch a NAT instance in a public subnet with a running Squid proxy to whitelist the outbound Internet access. Create a VPC Gateway endpoint to access the Amazon S3 bucket.**
    
    You can use a network address translation (NAT) instance in a public subnet in your VPC to enable instances in the private subnet to initiate outbound IPv4 traffic to the Internet or other AWS services but prevent the instances from receiving inbound traffic initiated by someone on the Internet.
    
    Limiting outbound traffic to certain trusted domains (called “whitelisting”), helps prevent instances from downloading malware, communicating with bot networks, or attacking internet hosts. You can use Squid, a leading open-source proxy, to implement a transparent proxy that can restrict both HTTP and HTTPS outbound traffic to a given set of Internet domains while being fully transparent for instances in the private subnet.
    
    ![](https://media.tutorialsdojo.com/public/fig1-add-DNS_filtering_NAT_instance_with_Squid.png)
    
    A VPC endpoint enables you to privately connect your VPC to supported AWS services and VPC endpoint services powered by AWS PrivateLink without requiring an internet gateway, NAT device, VPN connection, or AWS Direct Connect connection. Instances in your VPC do not require public IP addresses to communicate with resources in the service. Traffic between your VPC and the other service does not leave the Amazon network.
    
    Endpoints are virtual devices. They are horizontally scaled, redundant, and highly available VPC components. They allow communication between instances in your VPC and services without imposing availability risks or bandwidth constraints on your network traffic.
    
    Hence, the correct answer is: **Launch a NAT instance in a public subnet with a running Squid proxy to whitelist the outbound Internet access. Create a VPC Gateway endpoint to access the Amazon S3 bucket.**
    
    The option that says: **Set up a NAT instance in a private subnet with a Network ACL to whitelist the outbound Internet access. Launch a VPC Gateway endpoint to access the Amazon S3 bucket** is incorrect because you have to launch the NAT Instance to a public subnet in order for it to access the Internet. In addition, you can’t whitelist the outbound Internet access of an EC2 instance using Network ACL. You have to use an HTTP proxy for this scenario, like Squid proxy.
    
    The option that says: **Deploy a NAT instance in a private subnet. Install a Squid proxy in the instance to whitelist the outbound Internet access. Use AWS Transit Gateway to access the Amazon S3 bucket** is incorrect. Similar to the other incorrect option, a NAT instance should be in a public subnet, not private, since it needs internet connectivity to function. Moreover, the use of AWS Transit Gateway in this option is incorrect because Transit Gateway primarily connects VPCs and on-premises networks, and does not facilitate connections to Amazon S3.
    
    The option that says: **Use a NAT instance in a public subnet to allow the application to access the public web services. Use Amazon GuardDuty to whitelist the outbound Internet access. Create a VPC Gateway endpoint to access the Amazon S3 bucket** is incorrect because Amazon GuardDuty is just a threat detection service that continuously monitors for malicious activity and unauthorized behavior to protect your AWS accounts and workloads. It is not capable of whitelisting the outbound Internet access of your application.
    
- Use a NAT instance in a public subnet to allow the application to access the public web services. Use Amazon GuardDuty to whitelist the outbound Internet access. Create a VPC Gateway endpoint to access the Amazon S3 bucket.
- Set up a NAT instance in a private subnet with a Network ACL to whitelist the outbound Internet access. Launch a VPC Gateway endpoint to access the Amazon S3 bucket.
- Deploy a NAT instance in a private subnet. Install a Squid proxy in the instance to whitelist the outbound Internet access. Use AWS Transit Gateway to access the Amazon S3 bucket.

An Auto Scaling group of EC2 instances performs a handshake process with clients to establish an HTTPS connection. To improve system scalability and security, the network architecture must be modified to make the web servers offload the SSL/TLS processing to the Hardware Security Module (HSMs) in AWS.

What is the MOST suitable, scalable, and highly available solution that you must implement in this scenario?

- Offload the SSL/TLS processing using Amazon GuardDuty.
- Offload the SSL/TLS processing using the AWS Certificate Manager.
- **Create an AWS CloudHSM cluster with more than one HSM in different Availability Zones under a single VPC. Configure the web servers to offload the SSL/TLS processing to the CloudHSM cluster.**
    
    Web servers and their clients (web browsers) can use Secure Sockets Layer (SSL) or Transport Layer Security (TLS). These protocols confirm the identity of the web server and establish a secure connection to send and receive webpages or other data over the internet. This is commonly known as HTTPS. The web server uses a public–private key pair and an SSL/TLS public key certificate to establish an HTTPS session with each client. This process involves a lot of computation for the web server, but you can offload some of this to the HSMs in your AWS CloudHSM cluster. This is sometimes known as SSL acceleration. Offloading reduces the computational burden on your web server and provides extra security by storing the server’s private key in the HSMs.
    
    AWS CloudHSM provides hardware security modules (HSMs) in a *cluster*. A cluster is a collection of individual HSMs that AWS CloudHSM keeps in sync. You can think of a cluster as one logical HSM. When you perform a task or operation on one HSM in a cluster, the other HSMs in that cluster are automatically kept up to date.
    
    ![](https://media.tutorialsdojo.com/public/ssl-offload-handshake-process.png)
    
    When you create the HSMs in different AWS Availability Zones, you automatically get high availability. High availability means that you get higher reliability because no individual HSM is a single point of failure. It is recommended that you have a minimum of two HSMs in each cluster, with each HSM in different Availability Zones within an AWS Region.
    
    ![](https://media.tutorialsdojo.com/public/cluster-architecture.png)
    
    When you create an AWS CloudHSM cluster with more than one HSM, you automatically get load balancing. Load balancing means that the AWS CloudHSM client distributes cryptographic operations across all HSMs in the cluster based on each HSM’s capacity for additional processing.
    
    Hence, the correct answer is: **Create an AWS CloudHSM cluster with more than one HSM in different Availability Zones under a single VPC. Configure the web servers to offload the SSL/TLS processing to the CloudHSM cluster.**
    
    The option that says: **Set up an AWS KMS cluster in different Availability Zones under a single VPC. Configure the web servers to offload the SSL/TLS processing to the KMS cluster** is incorrect because there is no such thing as an AWS KMS cluster. Moreover, you can’t place AWS KMS in a VPC since this is a fully managed AWS service. You have to use a CloudHSM cluster instead.
    
    The option that says: **Offload the SSL/TLS processing using the AWS Certificate Manager** is incorrect because the AWS Certificate Manager (ACM) service simply makes it easier for you to provision, manage, and deploy SSL/TLS certificates on AWS managed resources. This can’t be used for the actual SSL/TLS processing.
    
    The option that says: **Offload the SSL/TLS processing using Amazon GuardDuty** is incorrect because you can’t use Amazon GuardDuty to process SSL/TLS certificates. Amazon GuardDuty simply protects your AWS accounts and workloads with intelligent threat detection and continuous monitoring.
    
- Set up an AWS KMS cluster in different Availability Zones under a single VPC. Configure the web servers to offload the SSL/TLS processing to the KMS cluster.

A multinational company has established both AWS-Managed VPN and AWS Direct Connect connections from the on-premises data center to its Amazon VPC. The virtual private gateway uses path selection and longest prefix match to determine how to route traffic.

Which is MOST preferred route that the virtual private gateway will prioritize first?

- The path with the lowest MED (multi-exit discriminator) value.
- **BGP propagated routes from an AWS Direct Connect connection.**
    
    The type of routing that you select can depend on the make and model of your customer gateway device. If your customer gateway device supports Border Gateway Protocol (BGP), specify dynamic routing when you configure your Site-to-Site VPN connection. If your customer gateway device does not support BGP, specify static routing
    
    If you use a device that supports BGP advertising, you don’t specify static routes to the Site-to-Site VPN connection because the device uses BGP to advertise its routes to the virtual private gateway. If you use a device that doesn’t support BGP advertising, you must select static routing and enter the routes (IP prefixes) for your network that should be communicated to the virtual private gateway.
    
    ![](https://media.tutorialsdojo.com/public/image10.png)
    
    It is recommended that you use BGP-capable devices, when available, because the BGP protocol offers robust liveness detection checks that can assist failover to the second VPN tunnel if the first tunnel goes down. Devices that don’t support BGP may also perform health checks to assist failover to the second tunnel when needed.
    
    AWS uses the most specific route in your route table that matches the traffic to determine how to route the traffic (longest prefix match).
    
    Only IP prefixes that are known to the virtual private gateway, whether through BGP advertisements or a static route entry, can receive traffic from your VPC. The virtual private gateway does not route any other traffic destined outside of received BGP advertisements, static route entries, or its attached VPC CIDR.
    
    When a virtual private gateway receives routing information, it uses path selection to determine how to route traffic. Longest prefix match is also used for the path selection. If the prefixes are the same, then the virtual private gateway prioritizes routes as follows, from MOST preferred to LEAST preferred:
    
    - BGP propagated routes from an AWS Direct Connect connection
    - Manually added static routes for a Site-to-Site VPN connection
    - BGP propagated routes from a Site-to-Site VPN connection
    - For matching prefixes where each Site-to-Site VPN connection uses BGP, the AS PATH is compared and the prefix with the shortest AS PATH is preferred. Alternatively, you can prepend `AS_PATH`, so that the path is less preferred.
    - When the AS PATHs are the same length and if the first AS in the AS_SEQUENCE is the same across multiple paths, multi-exit discriminators (MEDs) are compared. The path with the lowest MED value is preferred.
    
    Hence, the correct answer is: **BGP propagated routes from an AWS Direct Connect connection.**
    
    The option that says: **Manually added static routes for a Site-to-Site VPN connection** is incorrect because this is just the second most preferred route, next to a BGP propagated route from an AWS Direct Connect connection.
    
    The option that says: **The prefix with the longest AS PATH in a Site-to-Site VPN connection** is incorrect because the preferred route should be the prefix with the shortest AS PATH and not the longest.
    
    The option that says: **The path with the lowest MED (multi-exit discriminator) value** is incorrect because this is actually the least preferred route out of all options.
    
- Manually added static routes for a Site-to-Site VPN connection.
- The prefix with the longest AS PATH in a Site-to-Site VPN connection.

A company has an online customer portal hosted in Amazon Elastic Kubernetes Service (EKS) that uses AWS KMS to secure its proprietary data. The Network Administrator must ensure that communications between the VPC and KMS do not pass through the public Internet.

What is the MOST suitable solution that the Administrator should implement in this scenario?

- **Create a new VPC endpoint for KMS with private DNS enabled. Set up the KMS key policy to include the `aws:sourceVpce` condition and supply the ID of the newly launched VPC endpoint.**
    
    You can connect directly to AWS KMS through a private endpoint in your VPC instead of connecting over the internet. When you use a VPC endpoint, communication between your VPC and AWS KMS is conducted entirely within the AWS network. AWS KMS supports Amazon Virtual Private Cloud (Amazon VPC) interface endpoints that are powered by AWS PrivateLink.
    
    Each VPC endpoint is represented by one or more Elastic Network Interfaces (ENIs) with private IP addresses in your VPC subnets. The VPC interface endpoint connects your VPC directly to AWS KMS without an internet gateway, NAT device, VPN connection, or AWS Direct Connect connection. The instances in your VPC do not need public IP addresses to communicate with AWS KMS.
    
    ![Amazon VPC KMS Key policy](https://media.tutorialsdojo.com/amazon-vpc-kms-key-policy.png)
    
    If you use the default domain name servers (AmazonProvidedDNS) and enable private DNS hostnames for your VPC endpoint, you do not need to specify the endpoint URL. AWS populates your VPC name server with private zone data, so the public KMS endpoint (`https://kms.<region>.amazonaws.com`) resolves to your private VPC endpoint. To enable this feature when using your own name servers, forward requests for the KMS domain to the VPC name server.
    
    You can also use AWS CloudTrail logs to audit your use of KMS keys through the VPC endpoint. And you can use the conditions in IAM and key policies to deny access to any request that does not come from a specified VPC or VPC endpoint.
    
    Hence, the correct answer is: **Create a new VPC endpoint for KMS with private DNS enabled. Set up the KMS key policy to include the `aws:sourceVpce` condition and supply the ID of the newly launched VPC endpoint.**
    
    The option that says: **Set up an AWS Transit Gateway to ensure that communications between the VPC and KMS do not pass through the public Internet. Enable the Traffic Mirroring feature to ensure that the traffic only traverses the AWS network** is incorrect because AWS Transit Gateway is just a service that enables customers to connect their VPCs and their on-premises networks to a single gateway. Traffic Mirroring is just an Amazon VPC feature that you can use to copy network traffic from the elastic network interface of your Amazon EC2 instance. This feature is not capable of ensuring that your application traffic only traverses the AWS network.
    
    The option that says: **Set up an AWS Direct Connect connection with MAC Security Standard (MACsec) encryption and a MACsec secret key to ensure that communications between the VPC and KMS do not pass through the public Internet** is incorrect because this service is not suitable for integrating AWS KMS and your VPC. AWS Direct Connect is primarily used to connect your on-premises data center and your VPC. In addition, the Media Access Control security (MACsec) is simply an IEEE 802.1AE standard for encrypting packets traveling between two MACsec-capable Ethernet switches/routers, which is commonly used in an AWS Direct Connect connection. Furthermore, MACsec secret key is used for securing traffic over Direct Connect but has no relevance to AWS KMS communications within AWS.
    
    The option that says: **Create a new VPC endpoint for KMS with public DNS enabled. Add a new `aws:sourceVpc` condition in the KMS key policy and reference the VPC endpoint ID** is incorrect because you have to use AWS KMS with a private DNS enabled instead. The `aws:sourceVpc` condition is more suitable if you already have multiple VPC endpoints configured in the same VPC. This means that you still have to use VPC Endpoints in order for you to use this condition. Moreover, if you use the `aws:sourceVpc`condition, you have to specify the VPC ID and not the VPC endpoint ID.
    
- Create a new VPC endpoint for KMS with public DNS enabled. Add a new `aws:sourceVpc`condition in the KMS key policy and reference the VPC endpoint ID.
- Set up an AWS Direct Connect connection with MAC Security Standard (MACsec) encryption and a MACsec secret key to ensure that communications between the VPC and KMS do not pass through the public Internet.
- Set up an AWS Transit Gateway to ensure that communications between the VPC and KMS do not pass through the public Internet. Enable the Traffic Mirroring feature to ensure that the traffic only traverses the AWS network.

The Network team is currently implementing a hybrid cloud architecture with AWS. However, the Border Gateway Protocol (BGP) session can’t establish a connection over the Direct Connect link. Upon investigation, the BGP session is stuck in an `Idle` state.

What should the team do to properly troubleshoot this problem? (Select TWO.)

- Verify that the MTU of the virtual interface is set to 9001 (jumbo frames).
- **Verify that the Direct Connect router or any other device is not blocking ingress or egress from TCP port 179 and other appropriate ephemeral ports.**
- Verify that the Direct Connect router or any other device doesn't allow traffic to ephemeral ports.
- **Verify that the BGP peers are not more than one hop away from each other since the Virtual Private Gateway does not allow external BGP (EBGP) multi-hopping.**
- Verify that the External BGP (EBGP) multi-hop is enabled in the Virtual Private Gateway.
- **Evaluation**
    
    To bring up the BGP session, the physical Direct Connect link must be up, and there must be connectivity between the BGP peer IPs on both your end and the AWS end. If the physical connection is down, or there is no connectivity between the BGP peers.
    
    You have to check and verify the configuration on your Direct Connect router by doing the following steps:
    
    - The IP addresses of the local and remote BGP peers, local and remote BGP Autonomous System Numbers (ASN), and the BGP MD5 password must be configured with the downloaded Direct Connect configuration file from the AWS Direct Connect console.
    - Verify that the Direct Connect router or any other device is not blocking ingress or egress from TCP port 179 and other appropriate ephemeral ports.
    - BGP peers can’t be more than one hop away from each other, because external BGP (EBGP) Multi-hop is disabled on the AWS end.
    
    For a public virtual interface (VIF), verify that the BGP peer IPs fall in the CIDR range that is approved by AWS. If the BGP peer IPs are not approved, the BGP session won’t be established.
    
    Hence, the correct answers are:
    
    - **Verify that the Direct Connect router or any other device is not blocking ingress or egress from TCP port 179 and other appropriate ephemeral ports.**
    - **Verify that the BGP peers are not more than one hop away from each other since the Virtual Private Gateway does not allow external BGP (EBGP) multi-hopping.**
    
    The option that says: **Verify that the MTU of the virtual interface is set to 9001 (jumbo frames)** is incorrect because jumbo frames simply allow more than 1500 bytes of data by increasing the payload size per packet, and thus, increasing the percentage of the packet which is not considered as packet overhead. This is not a likely root cause of this issue.
    
    The option that says: **Verify that the External BGP (EBGP) multi-hop is enabled in the Virtual Private Gateway** is incorrect because the AWS Virtual Private Gateway does not allow external BGP (EBGP) multi-hopping.
    
    The option that says: **Verify that the Direct Connect router or any other device doesn’t allow traffic to ephemeral ports** is incorrect because it should actually allow other appropriate ephemeral ports in order for the Direct Connect connection to function properly.
    

A leading social media network needs to process real-time streaming data from its users all around the world. The company needs to track and analyze globally-distributed user activity on its website and on its mobile apps. The Network Engineer has been instructed to ensure that the data are processed in close geographical proximity to the users and to respond to user requests at low latencies.

What is the MOST suitable solution that the Engineer should implement?

- Process real-time streaming data using Amazon Kinesis Data Stream. Launch an Amazon CloudFront web distribution and Route 53 with a Geoproximity routing policy to process the data effectively.
- Process real-time streaming data using Amazon Kinesis Data Stream. Use AWS Global Accelerator in order to process the data in close geographical proximity to users and respond to user requests at low latencies.
- Use Amazon Athena to process real-time streaming data. Integrate Amazon CloudFront with Lambda@Edge in order to process the data in close geographical proximity to users and respond to user requests at low latencies.
- **Process real-time streaming data using Amazon Kinesis Data Stream. Integrate Amazon CloudFront with Lambda@Edge in order to process the data in close geographical proximity to users and respond to user requests at low latencies.**
    
    **Lambda[@Edge](https://portal.tutorialsdojo.com/members/edge/)** is a feature of Amazon CloudFront that lets you run code closer to users of your application, which improves performance and reduces latency. With Lambda[@Edge](https://portal.tutorialsdojo.com/members/edge/), you don’t have to provision or manage infrastructure in multiple locations around the world. You pay only for the compute time you consume – there is no charge when your code is not running.
    
    With Lambda[@Edge](https://portal.tutorialsdojo.com/members/edge/), you can enrich your web applications by making them globally distributed and improving their performance — all with zero server administration. Lambda[@Edge](https://portal.tutorialsdojo.com/members/edge/) runs your code in response to events generated by the Amazon CloudFront content delivery network (CDN). Just upload your code to AWS Lambda, which takes care of everything required to run and scale your code with high availability at an AWS location closest to your end-user.
    
    ![](https://media.tutorialsdojo.com/public/AWS-Lambda-at-Edge_User-Tracking-Analytics-diagram%20Oct%202018.5cc920f99c9450467b7290c20a8a4eb7c444e915.png)
    
    By using Lambda[@Edge](https://portal.tutorialsdojo.com/members/edge/) and Kinesis together, you can process real-time streaming data so that you can track and analyze globally-distributed user activity on your website and mobile applications, including clickstream analysis.
    
    Hence, the correct answer in this scenario is the option that says: **Process real-time streaming data using Amazon Kinesis Data Stream. Integrate Amazon CloudFront with Lambda[@Edge](https://portal.tutorialsdojo.com/members/edge/) in order to process the data in close geographical proximity to users and respond to user requests at low latencies.**
    
    The option that says: **Process real-time streaming data using Amazon Kinesis Data Stream. Launch an Amazon CloudFront web distribution and Route 53 with a Geoproximity routing policy to process the data effectively** is incorrect. You can only route traffic using Route 53 since it does not have any computing capability. This solution would not be able to process and return the data in close geographical proximity to your users since it is not using Lambda[@Edge](https://portal.tutorialsdojo.com/members/edge/).
    
    The option that says: **Process real-time streaming data using Amazon Kinesis Data Stream. Use AWS Global Accelerator in order to process the data in close geographical proximity to users and respond to user requests at low latencies** is incorrect because AWS Global Accelerator is just a service that enables you to create accelerators to improve the availability and performance of your applications for local and global users. This is a networking service, and it doesn’t have any computing capability thus, it cannot process any data. Instead, you have to use Lambda[@Edge](https://portal.tutorialsdojo.com/members/edge/) which allows you to run the code closer to the users of your application.
    
    The option that says: **Use Amazon Athena to process real-time streaming data. Integrate Amazon CloudFront with Lambda[@Edge](https://portal.tutorialsdojo.com/members/edge/) in order to process the data in close geographical proximity to users and respond to user requests at low latencies** is incorrect. Although using Lambda[@Edge](https://portal.tutorialsdojo.com/members/edge/) is right, using Amazon Athena to process real-time streaming data is wrong. This service is just an interactive query service that enables you to easily analyze data in Amazon S3 using standard SQL. Amazon Kinesis Data Stream should be used to process the streaming data in real time.
    

A company is developing an online education portal in AWS which uses WebSockets for its live coding feature. The portal will be hosted in an Amazon ECS cluster and must be behind a load balancer that supports host-based and path-based routing to properly distribute the incoming traffic. There should also be consistent visibility and network traffic controls for every service of the containerized application to ensure high availability.

Which of the following is the MOST suitable solution that needs to be implemented to meet the above requirements?

- **Use an Application Load Balancer and launch the ECS cluster with Amazon CloudWatch Container Insights enabled for traffic monitoring.**
    
    An **Application Load Balancer** functions at the application layer, the seventh layer of the Open Systems Interconnection (OSI) model. After the load balancer receives a request, it evaluates the listener rules in priority order to determine which rule to apply, and then selects a target from the target group for the rule action. You can configure listener rules to route requests to different target groups based on the content of the application traffic. Routing is performed independently for each target group, even when a target is registered with multiple target groups. You can configure the routing algorithm used at the target group level. The default routing algorithm is round robin; alternatively, you can specify the least outstanding requests routing algorithm.
    
    ![Elastic Load Balancing Table](https://media.tutorialsdojo.com/aws-load-balancer-websockets.png)
    
    **CloudWatch Container Insights** provides detailed visibility into the performance and health of containerized applications operating in Amazon ECS including important key metrics such as CPU, memory, disk I/O, and network traffic at the container and service level. This enables monitoring of WebSocket connections and network traffic, ensuring high availability and optimal performance. By aggregating logs and metrics, Container Insights simplifies troubleshooting and real-time monitoring, allowing teams to maintain consistent service quality. It integrates seamlessly with AWS services like CloudTrail and X-Ray to provide a comprehensive observability solution for containerized applications. This makes it ideal for ensuring the health and stability of an ECS-hosted portal with live WebSocket features.
    
    Hence, the correct answer is: **Use an Application Load Balancer and launch the ECS cluster with Amazon CloudWatch Container Insights enabled for traffic monitoring.**
    
    The option that says: **Deploy an Advanced Load Balancer from AWS Marketplace that supports WebSockets as well as host-based and path-based routing. Configure the ECS cluster to use Amazon ECS Service Discovery** is incorrect because there is no need to use a 3rd party load balancer to satisfy the requirements. An Application Load Balancer has all of the required features. The Amazon ECS Service Discovery service is primarily used to manage HTTP and DNS namespaces for your Amazon ECS services. It doesn’t standardizes how your services communicate nor give you end-to-end visibility.
    
    The option that says: **Create a Network Load Balancer and set up the ECS cluster with the FireLens integration option enabled** is incorrect. Although a Network Load Balancer supports WebSockets, it doesn’t have path-based and host-based routing capabilities. In addition, the FireLens integration simply provides an option to direct container logs to storage and analytics tools without modifying deployment scripts, manually installing extra software, or writing additional code.
    
    The option that says: **Launch a Gateway Load Balancer and create the Amazon ECS cluster with the CloudWatch Container Insights feature enabled** is incorrect because while Gateway Load Balancer supports WebSockets, it cannot route traffic based on path or host. The CloudWatch Container Insights service is mainly used to collect, aggregate, and summarize metrics and logs from your containerized applications and microservices. This option doesn’t provide consistent visibility, and network traffic controls to your cluster as required by the scenario.
    
- Create a Network Load Balancer and set up the ECS cluster with the FireLens integration option enabled.
- Deploy an Advanced Load Balancer from AWS Marketplace that supports WebSockets as well as host-based and path-based routing. Configure the ECS cluster to use Amazon ECS Service Discovery.
- Launch a Gateway Load Balancer and create the Amazon ECS cluster with the CloudWatch Container Insights feature enabled.

The company’s two customer data centers have a total of four Direct Connect (DX) connections to its Amazon VPC. The architecture utilizes two DX locations with two connections each. The Network Engineer needs to aggregate multiple connections at a single AWS Direct Connect endpoint to treat them as a single, managed connection.

What should you use to fulfill this requirement?

- Autonomous system (AS) path prepending
- **Link aggregation group (LAG)**
    
    A link aggregation group (LAG) is a logical interface that uses the Link Aggregation Control Protocol (LACP) to aggregate multiple connections at a single AWS Direct Connect endpoint, allowing you to treat them as a single, managed connection.
    
    You can create a LAG from existing connections, or you can provision new connections. After you’ve created the LAG, you can associate existing connections (whether standalone or part of another LAG) with the LAG.
    
    ![](https://media.tutorialsdojo.com/public/LAG_description.png)
    
    The following rules apply:
    
    - All connections in the LAG must use the same bandwidth.
    - You can have a maximum of four connections in a LAG. Each connection in the LAG counts towards your overall connection limit for the Region.
    - All connections in the LAG must terminate at the same AWS Direct Connect endpoint.
    
    When you create a LAG, you can download the Letter of Authorization and Connecting Facility Assignment (LOA-CFA) for each new physical connection individually from the AWS Direct Connect console.
    
    All LAGs have an attribute that determines the minimum number of connections in the LAG that must be operational for the LAG itself to be operational. By default, new LAGs have this attribute set to 0. You can update your LAG to specify a different value—doing so means that your entire LAG becomes non-operational if the number of operational connections falls below this threshold. This attribute can be used to prevent over-utilization of the remaining connections. All connections in a LAG operate in Active/Active mode.
    
    Hence, the correct answer is: **Link aggregation group (LAG).**
    
    **Direct Connect Gateway** is incorrect because it is not capable of aggregating multiple connections at a single AWS Direct Connect endpoint to treat them as a single, managed connection. A Direct Connect gateway simply enables you to use your AWS Direct Connect connections to interface with more than one AWS Region and reduce the number of Border Gateway Protocol sessions between your on-premises network and AWS deployments.
    
    **Autonomous system (AS) path prepending** is incorrect because this is only used in influencing the AWS outbound traffic. The BGP Best Path Algorithm decides how the best path to an autonomous system is selected. AS Path prepending is applicable when two or more routes exist to reach a particular prefix. By default, BGP prefers the route with the shortest AS Path. The secondary router will advertise a longer AS path, which ensures that the traffic from the VPC to your network always passes through the primary router.
    
    **Equal-cost multi-path routing (ECMP)** is incorrect because this is commonly used in establishing network connectivity between virtual private clouds (VPCs) and your on-premises network using multiple VPN connections that are associated with a Transit Gateway.
    
- Equal-cost multi-path routing (ECMP)
- Direct Connect Gateway

A government agency processes personal information submitted through its web portal hosted in Elastic Beanstalk with a Microsoft SQL Server database running on Amazon RDS. All of the personally identifiable information (PII) must be encrypted at all times to avoid any security breaches. You are assigned to configure the database to automatically encrypt data before it is persisted to storage and automatically decrypt data when they are fetched.

What should you do to satisfy this requirement?

- Set up IAM DB Authentication in the Amazon RDS database.
- **Configure the Microsoft SQL Server database to use Transparent Data Encryption (TDE).**
    
    Amazon RDS supports using Transparent Data Encryption (TDE) to encrypt stored data on your DB instances running Microsoft SQL Server. TDE automatically encrypts data before it is written to storage and automatically decrypts data when the data is read from storage.
    
    ![](https://media.tutorialsdojo.com/public/2019-06-26_06-40-30-6f92efec7d7944f7a2698921f8ac94cc.png)
    
    To enable transparent data encryption for an RDS SQL Server DB instance, specify the TDE option in an RDS option group that is associated with that DB instance.
    
    Transparent data encryption for SQL Server provides encryption key management by using a two-tier key architecture. A certificate, which is generated from the database master key, is used to protect the data encryption keys. The database encryption key performs the actual encryption and decryption of data on the user database. Amazon RDS backs up and manages the database master key and the TDE certificate. To comply with several security standards, Amazon RDS is working to implement automatic periodic master key rotation.
    
    Hence, the correct answer is: **Configure the Microsoft SQL Server database to use Transparent Data Encryption (TDE).**
    
    The option that says: **Set up IAM DB Authentication in the Amazon RDS database** is incorrect because this option simply lets you authenticate to your DB instance using AWS Identity and Access Management (IAM) database authentication. The more appropriate security feature to use here is to use Transparent Data Encryption (TDE).
    
    The option that says: **Enable Amazon RDS Encryption ****is incorrect because this just encrypts your Amazon RDS DB instances and snapshots at rest. It doesn’t automatically encrypt data before they are written to storage, nor automatically decrypt data when they are read from storage.
    
    The option that says: **Integrate Amazon Macie with Amazon RDS to automatically encrypt and decrypt data ****is incorrect. Although Amazon Macie can automatically detect personally identifiable information (PII), it is not capable of encrypting data.
    
- Enable Amazon RDS Encryption.
- Integrate Amazon Macie with Amazon RDS to automatically encrypt and decrypt data.

A company has a Kubernetes application hosted in a fleet of Linux-based EC2 instances with private IPv4 addresses and a public data repository running in IPv6. The application uses a self-hosted MySQL database cluster that uses multiple private subnets in the company’s Amazon VPC. To fetch the required software and database patches, the EC2 container instances must be able to connect to the public Internet and download the needed data during the maintenance window.

A Network Administrator was instructed to set up a highly available and scalable solution that will safely allow the EC2 servers and the database instances to fetch the software patches from the Internet but prevent the outside network from initiating a connection. All of the company’s VPCs are configured with flow logs.

Which combination of steps can provide the FASTEST method for analyzing network traffic patterns and identifying risks across the corporate network? (Select TWO.)

- Modify the existing flow log and configure it to publish the records to Amazon CloudWatch Logs. Use CloudWatch RUM to analyze the network traffic log patterns and identify threats in near real-time. Utilize both the Reachability Analyzer and Network Access Analyzer to perform connectivity testing and access troubleshooting.
- **Launch a NAT Gateway to enable the database and the Kubernetes pods running in EC2 instances to fetch the software patches from the Internet but prevent the outside network from initiating a connection. Enable DNS64 to the relevant IPv6-only subnets to allow IPv4-IPv6 communication via an internal Amazon Route 53 Resolver.**
- Create a new flow log that publishes to an Amazon S3 bucket. Establish an integration between Amazon Athena and the S3 bucket. Create a table in Amazon Athena and run queries directly in the Athena console to analyze the flow logs in near real-time.
- **Launch a new Amazon OpenSearch Service domain. Create a new flow log that publishes the records to the Amazon Data Firehose stream which delivers the records to the OpenSearch. Leverage on the built-in visualization and analytics features of OpenSearch for real-time network threat detection and incident management.**
- Launch a Transit Gateway with multicast domain to enable the database and the Kubernetes application to fetch the software patches from the Internet but prevent the outside network from initiating a connection. Set up Amazon Route 53 Inbound and Outbound Resolvers to allow two-way communication between the AWS resources with IPv4 addresses and the ones with IPv6 via IGMPv2.
- **Evaluation**
    
    You can use a NAT device to allow resources in private subnets to connect to the internet, other VPCs, or on-premises networks. These instances can communicate with services outside the VPC, but they cannot receive unsolicited connection requests.
    
    The NAT device replaces the source IPv4 address of the instances with the address of the NAT device. When sending response traffic to the instances, the NAT device translates the addresses back to the original source IPv4 addresses.
    
    You can use a managed NAT device offered by AWS, called a *NAT gateway*, or you can create your own NAT device on an EC2 instance, called a *NAT instance*. It is recommended that you use NAT gateways because they provide better availability and bandwidth and require less effort on your part to administer.
    
    ![](https://media.tutorialsdojo.com/dns64-nat64-tutorialsdojo.png)
    
    A NAT gateway supports network address translation from IPv6 to IPv4, popularly known as NAT64. NAT64 helps your IPv6 AWS resources communicate with IPv4 resources in the same VPC or a different VPC, in your on-premises network or over the internet. You can use NAT64 with DNS64 on Amazon Route 53 Resolver or use your own DNS64 server.
    
    VPC Flow Logs is a feature that enables you to capture information about the IP traffic going to and from network interfaces in your VPC. Flow log data can be published to the following locations: Amazon CloudWatch Logs, Amazon S3, or Amazon Data Firehose. After you create a flow log, you can retrieve and view the flow log records in the log group, bucket, or Firehose stream that you configured.
    
    ![](https://media.tutorialsdojo.com/amazon-vpc-flow-logs-destinations.png)
    
    Flow logs can publish flow log data directly to Data Firehose. When publishing to Data Firehose, flow log data is published to a Data Firehose stream, in plain text format.
    
    When you publish to Data Firehose, you can choose a Firehose stream that’s in the same account as the resource to monitor (the source account), or in a different account (the destination account). To enable cross-account delivery of flow logs to Data Firehose, you must create an IAM role in the source account and an IAM role in the destination account.
    
    Amazon Data Firehose is a fully managed service for delivering near real-time streaming data to various destinations for storage and performing near real-time analytics. With its extensible data transformation capabilities, you can also streamline log processing and log delivery pipelines into a single Firehose stream. It can deliver the VPC Flow Logs to Amazon OpenSearch Service to perform interactive log analytics, real-time application monitoring, website search, and more.
    
    The Kinesis Firehose – Amazon OpenSearch Service integration can provide a faster way to analyze and monitor your flow logs than using Amazon CloudWatch Logs, which has a significant amount of latency. You can also use Amazon Athena to query the flow logs stored in Amazon S3, but this solution doesn’t provide real-time analytics.
    
    Hence, the correct answers are:
    
    **– Launch a NAT Gateway to enable the database and the Kubernetes pods running in EC2 instances to fetch the software patches from the Internet but prevent the outside network from initiating a connection. Enable DNS64 to the relevant IPv6-only subnets to allow IPv4-IPv6 communication via an internal Amazon Route 53 Resolver.**
    
    **– Launch a new Amazon OpenSearch Service domain. Create a new flow log that publishes the records to the Amazon Data Firehose stream which delivers the records to the OpenSearch. Leverage on the built-in visualization and analytics features of OpenSearch for real-time network threat detection and incident management.**
    
    The option that says: **Create a new flow log that publishes to an Amazon S3 bucket. Establish an integration between Amazon Athena and the S3 bucket. Create a table in Amazon Athena and run queries directly in the Athena console to analyze the flow logs in near real-time** is incorrect. Although using Amazon Athena to run queries against the flow log records stored in Amazon S3 is possible, this solution still does not provide the fastest method of collecting and analyzing the flow logs. This setup entails a significant lead time before the user can view the actual flow log from Athena. A better solution is to deliver the flow logs records to Amazon OpenSearch service using Kinesis Firehose to analyze the records in near-real-time.
    
    The option that says: **Modify the existing flow log and configure it to publish the records to Amazon CloudWatch Logs. Use CloudWatch RUM to analyze the network traffic log patterns and identify threats in near real-time. Utilize both the Reachability Analyzer and Network Access Analyzer to perform connectivity testing and access troubleshooting** is incorrect because first of all, you cannot directly modify an existing flow log. You have to create a new flow log to set a new destination for the network records. Moreover, CloudWatch RUM only performs real-time analysis of your web application performance and not on the actual packets that go in and out of your Amazon VPC. CloudWatch RUM is primarily used for real user monitoring to collect and view client-side data about your web application performance from actual user sessions in near real-time.
    
    The option that says: **Launch a Transit Gateway with multicast domain to enable the database and the Kubernetes application to fetch the software patches from the Internet but prevent the outside network from initiating a connection. Set up Amazon Route 53 Inbound and Outbound Resolvers to allow two-way communication between the AWS resources with IPv4 addresses and the ones with IPv6 via IGMPv2** is incorrect. Transit Gateway simply connects your Amazon Virtual Private Clouds (VPCs) and on-premises networks through a central hub and does not act as a Network Address Translation (NAT) service. You have to use a NAT Gateway to satisfy the requirement. A multicast domain indeed uses IGMPv2 but it does not allow two-way communication between your AWS resources with IPv4 addresses and the ones with IPv6.
    

A multinational organization has several AWS accounts that are consolidated using AWS Organizations and an entirely separate account that is exclusively used by the Finance department. The organization’s on-premises data center is connected to the VPC of the Finance department via an AWS Direct Connect connection which uses a private virtual interface. A data repository is hosted in a large EC2 instance which contains hundreds of confidential data in the Finance VPC. The Network team has been instructed to download all of the files in the data repository to the on-premises network. To secure the sensitive financial data, the Finance VPC is not associated to the existing AWS Organization nor connected to the VPCs of other AWS accounts.

In this scenario, which of the following is true regarding the Data Transfer Out charges?

- The member account of the AWS Organization will be charged with the Data Transfer Out costs.
- The Finance department will get the bill for the port hours only, excluding any outbound data transfer charges.
- The master account of the AWS Organization will shoulder the Data Transfer Out charges.
- **The Finance department will shoulder the Data Transfer Out charges.**
    
    **AWS Direct Connect** is a network service that provides an alternative to using the Internet to connect customers’ on-premises sites to AWS. In many circumstances, private network connections can reduce costs, increase bandwidth, and provide a more consistent network experience than Internet-based connections.
    
    AWS Direct Connect has two billing elements:
    
    1. Port hours
    2. Outbound data transfer
    
    Port hour pricing is determined by capacity and connection type (dedicated connection or hosted connection). Data Transfer Out charges for private interfaces and transit virtual interfaces are allocated to the AWS account responsible for the Data Transfer. There are no additional charges to use a multi-account AWS Direct Connect gateway.
    
    ![](https://media.tutorialsdojo.com/public/Product-Page-Diagram_Direct-Connect.0166400894ac5f9c921c3f7a346f55d8c42f492c.png)
    
    For publicly addressable AWS resources (for example, Amazon S3 buckets, Classic EC2 instances, or EC2 traffic that goes through an internet gateway), if the outbound traffic is destined for public prefixes owned by the same AWS payer account and actively advertised to AWS through an AWS Direct Connect public virtual Interface, the Data Transfer Out (DTO) usage is metered toward the resource owner at AWS Direct Connect data transfer rate.
    
    With the introduction of the granular Data Transfer Out allocation feature, the AWS account responsible for this will be charged for the Data Transfer Out performed over a transit/private virtual interface. The AWS account responsible for the Data Transfer Out will be determined based on the customer’s use of the private/transit virtual interface as follows:
    
    - Private virtual interface(s) is used to interface with Amazon Virtual Private Cloud(s) with or without Direct Connect gateway(s). In the case of the private virtual interface, the AWS account owning the AWS resources responsible for the Data Transfer Out will be charged.
    - Transit virtual interface(s) is used to interface with AWS Transit Gateway(s). In the case of the transit virtual interface, the AWS account owning the Amazon Virtual Private Cloud(s) attached to the AWS Transit Gateway associated with the Direct Connect gateway attached to the transit virtual interface will be charged. Please note that all applicable AWS Transit Gateway-specific charges (Data Processing and Attachment) will be in addition to the AWS Direct Connect Data Transfer Out.
    
    Hence, the correct answer is: **The Finance department will shoulder the Data Transfer Out charges.**
    
    The option that says: **The master account of the AWS Organization will shoulder the Data Transfer Out charges** is incorrect because the AWS account of the Finance department is not connected to the AWS Organization.
    
    The option that says: **The Finance department will get the bill for the port hours only, excluding any outbound data transfer charges** is incorrect because the Finance department will also be charged with outbound data transfer bills.
    
    The option that says: **The member account of the AWS Organization will be charged with the Data Transfer Out costs** is incorrect because usually, the AWS Direct Connect data transfer usage will be aggregated to the master account and not on the member account. This option is actually irrelevant since the Finance department is not included in the existing AWS Organization in the first place.
    

The company’s on-premises network has an established AWS Direct Connect connection to its VPC in AWS. A Network Engineer is designing the network infrastructure of a multitier application hosted in an Auto Scaling group of EC2 instances. The application will be accessed by the employees from the on-premises network as well as from the public Internet. The network configuration must automatically update routes in your route table based on your dynamic BGP route advertisement.

What should the Engineer do to implement this network setup?

- Modify the main route table of the VPC to have two default routes. The first route goes to the public Internet via the Internet Gateway while the second route goes to the on-premises network via the virtual private gateway.
- **Enable route propagation in the route table of the VPC and specify the virtual private gateway as the target.**
    
    **Route tables** determine where network traffic is directed. In your VPC route table, you must add a route for your remote network and specify the virtual private gateway as the target. This enables traffic from your VPC that’s destined for your remote network to route via the virtual private gateway and over one of the VPN tunnels. You can enable route propagation for your route table to automatically propagate your network routes to the table for you.
    
    ![](https://media.tutorialsdojo.com/public/2020-05-25_08-12-52-d023463d19326df39af7b88da3549c3a.png)
    
    AWS uses the most specific route in your route table that matches the traffic to determine how to route the traffic (longest prefix match). If your route table has overlapping or matching routes, the following rules apply:
    
    - If propagated routes from a Site-to-Site VPN connection or AWS Direct Connect connection overlap with the local route for your VPC, the local route is most preferred, even if the propagated routes are more specific.
    - If propagated routes from a Site-to-Site VPN connection or AWS Direct Connect connection have the same destination CIDR block as other existing static routes (longest prefix match cannot be applied), AWS prioritizes the static routes whose targets are an internet gateway, a virtual private gateway, a network interface, an instance ID, a VPC peering connection, a NAT gateway, a transit gateway, or a gateway VPC endpoint.
    
    Hence, the correct answer is: **Enable route propagation in the route table of the VPC and specify the virtual private gateway as the target.**
    
    The option that says: **Set up two different route tables in the VPC. The first route table must have a default route to the Internet Gateway and the second table has a route to the virtual private gateway** is incorrect because using two route tables is not required in this scenario. You can use a single route table with a specific route to the on-premises network and enable route propagation.
    
    The option that says: **Disable the default route propagation option in the route table of the VPC and add a specific route to the on-premises network. Choose the virtual private gateway as the target. Enable the route propagation option in the customer gateway** is incorrect. You have to enable route propagation for the route table to automatically propagate the network routes to the on-premises network. You have to enable this in the Amazon VPC and not in the customer gateway. Moreover, this option is not enabled by default.
    
    The option that says: **Modify the main route table of the VPC to have two default routes. The first route goes to the public Internet via the Internet Gateway while the second route goes to the on-premises network via the virtual private gateway** is incorrect because a route table cannot have two default routes. Route propagation should also be enabled in order to satisfy the requirements.
    
- Set up two different route tables in the VPC. The first route table must have a default route to the Internet Gateway and the second table has a route to the virtual private gateway.
- Disable the default route propagation option in the route table of the VPC and add a specific route to the on-premises network. Choose the virtual private gateway as the target. Enable the route propagation option in the customer gateway.

A company has hundreds of applications deployed in three different Amazon VPCs that use IPv6. These applications must be able to connect to the on-premises data center as well as to each other. The connection must have an AES256 encryption and be authenticated with SHA1 or SHA2 hashing functions.

It should also use Diffie-Hellman (DH) groups for key exchange to support Perfect Forward Secrecy (PFS). The solution should automatically rotate the certificate for the AWS-side of the tunnel endpoint using a service-linked role. Some components of the company’s application suite still use IPv4 so the network solution should support both IPv4 and IPv6 protocols.

Which of the following is the MOST OPERATIONALLY EFFICIENT solution that will satisfy the requirements?

- Set up a hub and spoke VPC-to-VPC routing and on-premises network connection by running a virtual SD-WAN appliance in a shared VPC. Create Inbound and Outbound Amazon Route 53 Resolvers to route IPv4 and IPv6 traffic
- Launch a private Multiprotocol Label Switching (MPLS) network on the on-premises data center. Connect all the Amazon VPCs to the MPLS network.
- Set up multiple Inter-Region VPC Peering connections to all Amazon VPCs and to the on-premises data center. Configure split-view DNS (or split-horizon) DNS in the Amazon Route 53 hosted zone to support both IPv4 and IPv6 protocols
- **Launch an AWS Transit Gateway. Set up an AWS Site-to-Site VPN connection with IPv6 support between the Transit Gateway and the customer gateway.**
    
    AWS Site-to-Site VPN creates IPSec tunnels to a virtual gateway or AWS Transit Gateway. Traffic in the tunnel between these endpoints can be encrypted with AES128 or AES256 and use Diffie-Hellman groups for key exchange, providing Perfect Forward Secrecy. AWS Site-to-Site VPN will authenticate with SHA1 or SHA2 hashing functions.
    
    AWS Site-to-Site VPN supports certificate-based authentication by integrating with AWS Certificate Manager Private Certificate Authority. Using digital certificates instead of pre-shared keys for IKE authentication, you can build IPSec tunnels with static or dynamic customer gateway IP addresses.
    
    ![AWS Site-to-Site VPN](https://media.tutorialsdojo.com/public/cgw-static-routing.png)
    
    You can rotate the certificates on the tunnel endpoints on the AWS side by using the Amazon VPC console. When a tunnel endpoint’s certificate is close to expiration, AWS automatically rotates the certificate using the service-linked role.
    
    If you believe that the tunnel credentials for your Site-to-Site VPN connection have been compromised, you can change the IKE pre-shared key. To do so, delete the Site-to-Site VPN connection, create a new one using the same virtual private gateway, and configure the new keys on your customer gateway. You can specify your own pre-shared keys when you create the Site-to-Site VPN connection. You also need to confirm that the tunnel’s inside and outside addresses match because these might change when you recreate the Site-to-Site VPN connection.
    
    ![AWS Site-to-Site VPN](https://media.tutorialsdojo.com/aws-vpn-connection-types-accelerated-VPN.png)
    
    Your Site-to-Site VPN connection on a transit gateway can support either IPv4 traffic or IPv6 traffic inside the VPN tunnels. By default, a Site-to-Site VPN connection supports IPv4 traffic inside the VPN tunnels. You can configure a new Site-to-Site VPN connection to support IPv6 traffic inside the VPN tunnels. Then, if your VPC and your on-premises network are configured for IPv6 addressing, you can send IPv6 traffic over the VPN connection.
    
    If you enable IPv6 for the VPN tunnels for your Site-to-Site VPN connection, each tunnel has two CIDR blocks. One is a size /30 IPv4 CIDR block, and the other is a size /126 IPv6 CIDR block.
    
    Hence, the correct answer is: **Launch an AWS Transit Gateway. Set up an AWS Site-to-Site VPN connection with IPv6 support between the Transit Gateway and the customer gateway.**
    
    The option that says: **Set up multiple Inter-Region VPC Peering connections to all Amazon VPCs and to the on-premises data center. Configure split-view DNS (or split-horizon) DNS in the Amazon Route 53 hosted zone to support both IPv4 and IPv6 protocols** is incorrect because a VPC peering connection is just a networking connection between two VPCs that enables you to route traffic between them using private IPv4 addresses or IPv6 addresses. This can’t be used in establishing a connection to your on-premises data center. Furthermore, the split-view DNS configuration is primarily used to allow access to two versions of your public website (internal and external versions) having the same domain name. Split-view DNS is not applicable for enabling IPv4 and IPv6 traffic.
    
    The option that says: **Launch a private Multiprotocol Label Switching (MPLS) network on the on-premises data center. Connect all the Amazon VPCs to the MPLS network** is incorrect because MPLS is simply an encapsulation protocol, and it can’t be used to connect to your Amazon VPCs. MPLS predetermines the path and uses a label-swapping push, pop, and swap method to direct the traffic to its destination instead of relying on IP lookups to discover a viable “next-hop” at every single router within a path (as in traditional IP networking). You have to set up AWS VPN connections instead.
    
    The option that says: **Set up a hub and spoke VPC-to-VPC routing and on-premises network connection by running a virtual SD-WAN appliance in a shared VPC. Create Inbound and Outbound Amazon Route 53 Resolvers to route IPv4 and IPv6 traffic** is incorrect because an SD-WAN is simply an MPLS alternative that uses multiple transport types to connect a network of SD-WAN appliances which powers their Wide Area Network (WAN). This is not typically used to establish a connection between your on-premises network and to multiple Amazon VPCs. In addition, Inbound and Outbound Resolvers in Amazon Route 53 are meant for resolving custom domains and not for routing IPv4 and IPv6 traffic.
    

A Network Administrator is setting up a public virtual interface to connect the VPC to the company’s on-premises data center via a newly provisioned 10-Gbps AWS Direct Connect connection. The virtual interface should also support Autonomous System (AS) prepending.

Which of the following are the required information that must be set to properly create the virtual interface? (Select THREE.)

- **List of IP prefix CIDRs that will be advertised to AWS.**
- **Virtual local area network (VLAN) ID.**
- BGP local preference community tag.
- Direct Connect Gateway.
- **Border Gateway Protocol Autonomous System Number (BGP ASN).**
- Private Autonomous System Number (ASN).
- **Evaluation**
    
    You can create a transit virtual interface to connect to a transit gateway, a public virtual interface to connect to public resources (non-VPC services), or a private virtual interface to connect to a VPC. To create a virtual interface for accounts within your AWS Organizations or AWS Organizations that are different from yours, create a hosted virtual interface.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-12_11-30-09-f2e3b76f76cc69284b24e40c53eb540e.png)
    
    To create a public virtual interface, you must supply the following:
    
    - For Virtual interface name, enter a name for the virtual interface.
    - For Connection, choose the Direct Connect connection that you want to use for the interface.
    - For VLAN, enter the ID number for your virtual local area network (VLAN).
    - For BGP ASN, enter the Border Gateway Protocol Autonomous System Number of your on-premises peer router for the new virtual interface. The valid values are 1-2147483647.
    - For the ‘Prefixes you want to advertise’ field, enter the IPv4 CIDR destination addresses (separated by commas) to which traffic should be routed over the virtual interface.
    
    You can set a public or private Border Gateway Protocol (BGP) Autonomous System Number (ASN) for your side of the BGP session. If you are using a public ASN, you must own it. If you are using a private ASN, it must be in the 64512 to 65535 range. Autonomous System (AS) prepending does not work if you use a private ASN for a public virtual interface.
    
    Hence, the correct answers are:
    
    - **Virtual local area network (VLAN) ID.**
    - **Border Gateway Protocol Autonomous System Number (BGP ASN)**
    - **List of IP prefix CIDRs that will be advertised to AWS**
    
    The option that says: **Private Autonomous System Number (ASN)** is incorrect because Autonomous System (AS) prepending does not work if you use a private ASN for a public virtual interface. A Border Gateway Protocol Autonomous System Number (BGP ASN) is only needed in this scenario.
    
    The option that says: **Direct Connect Gateway** is incorrect because this information is only applicable for private and transit virtual interfaces.
    
    The option that says: **BGP local preference community tag** is incorrect because this information is not required in creating a public virtual interface.
    

A multinational company has several public websites that were registered using a 3rd-party DNS registrar. The DNS services being used by their websites are from an external service provider, which also includes the Domain Name System Security Extensions (DNSSEC) feature.

The company needs to transfer the domain registration and the DNS services to Amazon Route 53. The migration should have little to no downtime as the websites are already running production workloads.

Which of the following is the most operationally efficient solution with the LEAST amount of downtime?

- Create a new hosted zone and DNS records in Amazon Route 53. Lower the TTL (time to live) setting of the NS (name server) record to 300 and remove the Delegation Signer (DS) record from the parent zone in the current DNS service provider. Transfer domain registration to Amazon Route 53, where the DNSSEC will be automatically re-enabled. Lower the TTL setting of the NS record in the Route 53 hosted zone. Once the TTL expires, update the NS records to use Route 53 name servers and monitor the traffic. Set the TTL to 600 seconds for the NS record once the migration is complete.
- Create a new hosted zone and DNS records in Amazon Route 53 with the DNSSEC feature enabled. Lower the TTL (time to live) setting of the NS (name server) record to 300. Increase the TTL setting of the NS record in the Route 53 hosted zone. Once the TTL expires, update the NS records to use Route 53 name servers and monitor the traffic. Lower the TTL to 600 seconds for the NS record once the migration is complete. Transfer domain registration to Amazon Route 53 and re-enable DNSSEC signing.
- Create a new hosted zone and DNS records in Amazon Route 53. Remove the Delegation Signer (DS) record from the parent zone in the current DNS service provider. Transfer domain registration to Amazon Route 53, where the DNSSEC will be automatically re-enabled. Update the NS records to use Route 53 name servers and monitor the traffic. Configure the TTL to 172800 seconds for the NS record once the migration is complete.
- **Create a new hosted zone and DNS records in Amazon Route 53. Lower the TTL (time to live) setting of the NS (name server) record to 300 and remove the Delegation Signer (DS) record from the parent zone in the current DNS service provider. Lower the TTL setting of the NS record in the Route 53 hosted zone. Once the TTL expires, update the NS records to use Route 53 name servers and monitor the traffic. Increase the TTL to 172800 seconds for the NS record once the migration is complete. Transfer domain registration to Amazon Route 53 and re-enable DNSSEC signing.**
    
    If you’re transferring one or more domain registrations to Route 53, and you’re currently using a domain registrar that doesn’t provide paid DNS service, you need to migrate the DNS service before you migrate the domain. Otherwise, the registrar will stop providing DNS service when you transfer your domains, and the associated websites and web applications will become unavailable on the internet. (You can also migrate the DNS service from the current registrar to another DNS service provider. We don’t require you to use Route 53 as the DNS service provider for domains that are registered with Route 53.)
    
    The process depends on whether you’re currently using the domain:
    
    - If the domain is currently getting traffic—for example, if your users are using the domain name to browse a website or access a web application.
    - If the domain isn’t getting any traffic (or is getting very little traffic)
    
    ![](https://media.tutorialsdojo.com/Route53_DNSSEC.png)
    
    For both options, your domain should remain available during the entire migration process. However, in the unlikely event that there are issues, the first option lets you roll back the migration quickly. With the second option, your domain could be unavailable for a few days.
    
    If you want to migrate DNS service to Amazon Route 53 for a domain that is currently getting traffic—for example, if your users are using the domain name to browse to a website or access a web application — perform the procedures below:
    
    - Get your current DNS configuration from the current DNS service provider (optional but recommended)
    - Step 2: Create a hosted zone
    - Step 3: Create records
    - Step 4: Lower TTL settings
    - Step 5: (If you have DNSSEC configured) Remove the DS record from the parent zone
    - Step 6: Wait for the old TTL to expire
    - Step 7: Update the NS records to use Route 53 name servers
    - Step 8: Monitor traffic for the domain
    - Step 9: Change the TTL for the NS record back to a higher value
    - Step 10: Transfer domain registration to Amazon Route 53
    - Step 11: Re-enable DNSSEC signing (if required)
    
    Ensure that you properly set the TTL of your current DNS service and Amazon Route 53 before you update your DNS records. By default, the typical TTL setting for the NS record is 172800 seconds, which is equivalent to two days. This means that it would take 2 days for your DNS change to be propagated. You have to lower the TTL settings when you conduct the migration and then set it back to its default value after the domain was successfully moved to Amazon Route 53.
    
    ![](https://media.tutorialsdojo.com/ans-c01-nameserver-ttl-tutorialsdojo.png)
    
    Updating the Name Server (NS) records should happen first before you transfer the domain registration of your web domain to Amazon Route 53. If you’ve configured DNSSEC for your domain, you should remove the Delegation Signer (DS) record from the parent zone before you migrate your domain to Route 53. If the parent zone is hosted through Route 53 or another registrar, contact them to remove the DS record. Because it isn’t currently possible to have DNSSEC signing enabled across two providers, you must remove any DS or DNSKEYs to deactivate DNSSEC. This temporarily signals to DNS resolvers to disable DNSSEC validation.
    
    Hence, the correct answer is: **Create a new hosted zone and DNS records in Amazon Route 53. Lower the TTL (time to live) setting of the NS (name server) record to 300 and remove the Delegation Signer (DS) record from the parent zone in the current DNS service provider. Lower the TTL setting of the NS record in the Route 53 hosted zone. Once the TTL expires, update the NS records to use Route 53 name servers and monitor the traffic. Increase the TTL to 172800 seconds for the NS record once the migration is complete. Transfer domain registration to Amazon Route 53 and re-enable DNSSEC signing.**
    
    The option that says: **Create a new hosted zone and DNS records in Amazon Route 53 with the DNSSEC feature enabled. Lower the TTL (time to live) setting of the NS (name server) record to 300. Increase the TTL setting of the NS record in the Route 53 hosted zone. Once the TTL expires, update the NS records to use Route 53 name servers and monitor the traffic. Lower the TTL to 600 seconds for the NS record once the migration is complete. Transfer domain registration to Amazon Route 53 and re-enable DNSSEC signing** is incorrect because you should remove the Delegation Signer (DS) record first on the current DNS service. Not doing this first step will cause DNSSEC issues during migration. Keep in mind that you should set the TTL to a higher value, like 172800 seconds, and not lower as this affects the DNS performance of your website.
    
    The option that says: **Create a new hosted zone and DNS records in Amazon Route 53. Lower the TTL (time to live) setting of the NS (name server) record to 300 and remove the Delegation Signer (DS) record from the parent zone in the current DNS service provider. Transfer domain registration to Amazon Route 53, where the DNSSEC will be automatically re-enabled. Lower the TTL setting of the NS record in the Route 53 hosted zone. Once the TTL expires, update the NS records to use Route 53 name servers and monitor the traffic. Set the TTL to 600 seconds for the NS record once the migration is complete** is incorrect. First off, the DNSSEC feature is not automatically re-enabled in Route 53 after migration. The process of transferring the domain registration to Route 53 should happen after the DNS Service was successfully ported and not before. This may cause some issues during the web domain migration. You should also set the TTL to 172800 seconds for the NS record once the migration is complete, not 600 seconds (5 minutes), to optimize DNS calls.
    
    The option that says: **Create a new hosted zone and DNS records in Amazon Route 53. Remove the Delegation Signer (DS) record from the parent zone in the current DNS service provider. Transfer domain registration to Amazon Route 53, where the DNSSEC will be automatically re-enabled. Update the NS records to use Route 53 name servers and monitor the traffic. Configure the TTL to 172800 seconds for the NS record once the migration is complete** is incorrect because the TTL setting must be lowered on the current DNS service provider before porting the NS records to Route 53. This solution will cause downtime to the websites as the change will only be reflected once the TTL setting (which is usually set to 172800 seconds or 2 days) of the current DNS service provider has elapsed. In addition, the DNSSEC feature is not automatically re-enabled in Amazon Route 53. This feature must be manually configured.
    

An application is hosted in an Auto Scaling group of EC2 instances behind an Application Load Balancer. The Security team detected 300 random IP addresses that submitted bad requests to the application. These malicious web requests generate thousands of HTTP 4xx error codes that significantly affect the application’s performance. Based on the security logs, the Security team predicts that there would be more attacks from new IP addresses that need to be blocked.

What is the MOST suitable solution that the Network Administrator should implement to mitigate this security threat?

- Set up 300 inbound rules in the Security Group of the EC2 instances to block the malicious IP addresses.
- **Integrate AWS WAF to the Application Load Balancer. Configure IP match conditions in AWS WAF to block the malicious IP addresses.**
    
    **AWS WAF** is a web application firewall that lets you monitor the HTTP(S) requests that are forwarded to an Amazon CloudFront distribution, an Amazon API Gateway API, or an Application Load Balancer.
    
    AWS WAF also lets you control access to your content. Based on conditions that you specify, such as the IP addresses where requests originate from or the values of query strings, an Amazon CloudFront distribution, an Amazon API Gateway API, or an Application Load Balancer responds to requests either with the requested content or with an HTTP 403 status code (Forbidden). You can also configure CloudFront to return a custom error page when a request is blocked.
    
    ![](https://media.tutorialsdojo.com/product-page-diagram_AWS-WAF_How-it-Works@2x.452efa12b06cb5c87f07550286a771e20ca430b9.png)
    
    You can add the rules and rule groups in AWS WAF that you want to use to filter web requests. For example, you can specify the IP addresses where the requests originate from and values in the request that are used only by attackers. For each rule, you specify whether you want to block matching web requests or allow them. The rules that are defined inside a rule group have their actions defined inside the rule group.
    
    Hence, the correct answer is: **Integrate AWS WAF to the Application Load Balancer. Configure IP match conditions in AWS WAF to block the malicious IP addresses.**
    
    The option that says: **Set up 300 inbound rules in the Security Group of the EC2 instances to block the malicious IP addresses** is incorrect because security groups are not capable of blocking 300 random IP addresses based on its inbound rules limit. Moreover, it won’t be able to accommodate the new batch of IP addresses that need to be blocked. A better solution is to use AWS WAF, which can block 300 or more IP addresses.
    
    The option that says: **Integrate Amazon inspector to the Application Load Balancer. Set up IP match conditions in Amazon Inspector to block the malicious IP addresses** is incorrect because Amazon Inspector just automatically assesses applications for exposure, vulnerabilities, and deviations from best practices. It cannot be used to block malicious IP addresses, let alone be integrated with an Application Load Balancer.
    
    The option that says: **Add 300 inbound rules in the Network ACL on the subnet, which the EC2 instances are launched, to the block malicious IP addresses** is incorrect. Just like security groups, the number of inbound rules that you can set for a Network ACL is quite limited and therefore, not scalable. You cannot block 300 random IP addresses using a single Network ACL.
    
- Integrate Amazon Inspector to the Application Load Balancer. Set up IP match conditions in Amazon Inspector to block the malicious IP addresses.
- Add 300 inbound rules in the Network ACL on the subnet, where the EC2 instances are launched, to the block the malicious IP addresses.

A Network Engineer has been tasked to protect the company’s publicly accessible online customer portal and to secure the clients’ sensitive financial information. Hackers must be prevented from intercepting DNS queries and from replacing the actual IP addresses of the website with unauthorized IP addresses in the DNS resolvers. The solution should protect the users from being routed to the IP addresses provided by the attackers in the spoofed response that could potentially direct them to fake or phishing websites.

What should the Engineer do to satisfy this requirement?

- Set up a private hosted zone in Amazon Route 53 and launch a BIND DNS Server.
- Enable Server Name Indication (SNI) in Amazon Route 53.
- Set up Perfect Forward Secrecy (PFS) using Diffie-Hellman (DH) group 2 to prevent DNS spoofing.
- **Enable Domain Name System Security Extensions (DNSSEC) in Amazon Route 53.**
    
    Attackers sometimes hijack traffic to Internet endpoints such as web servers by intercepting DNS queries and returning their own IP addresses to DNS resolvers in place of the actual IP addresses for those endpoints. Users are then routed to the IP addresses provided by the attackers in the spoofed response, for example, to fake websites.
    
    You can protect your domain from this type of attack, known as DNS spoofing or a man-in-the-middle attack, by configuring Domain Name System Security Extensions (DNSSEC), a protocol for securing DNS traffic.
    
    ![](https://media.tutorialsdojo.com/Route53_DNSSEC.png)
    
    When you configure DNSSEC for your domain, a DNS resolver establishes a chain of trust for responses from intermediate resolvers. The chain of trust begins with the TLD registry for the domain (your domain’s parent zone) and ends with the authoritative name servers at your DNS service provider. Not all DNS resolvers support DNSSEC. Resolvers that don’t support DNSSEC don’t perform any signature or authenticity validation.
    
    Hence, the correct answer is: **Enable Domain Name System Security Extensions (DNSSEC) in Amazon Route 53.**
    
    The option that says: **Enable Server Name Indication (SNI) in Amazon Route 53** is incorrect because this feature is primarily used to support multiple TLS certificates for your web applications and not for protecting against DNS Spoofing or man-in-the-middle attacks.
    
    The option that says: **Set up a private hosted zone in Amazon Route 53 and launch a BIND DNS Server** is incorrect because a private hosted zone is just a container that holds information about how you want Amazon Route 53 to respond to DNS queries for a specific domain and its subdomains within one or more VPCs.
    
    The option that says: **Set up Perfect Forward Secrecy (PFS) using Diffie-Hellman (DH) group 2 to prevent DNS spoofing** is incorrect because this is simply a feature that provides additional safeguards against the eavesdropping of encrypted data through the use of a unique random session key.
    

A company has a hybrid cloud infrastructure that consists of its Amazon VPC in the us-east-1 (N. Virginia) Region and its corporate network. A single 10-Gbps AWS Direct Connect connection with multiple private virtual interfaces has been established to allow EC2 instances to send data to the on-premises file storage servers. The Network Administrator has been tasked to ensure high resiliency to common connectivity failures which will support the critical production workloads.

What must the Administrator do to satisfy this requirement?

- Create a second 10-Gbps AWS Managed VPN connection between the VPC and the on-premises network.
- Launch a Direct Connect Gateway that connects two public virtual interfaces in the us-east-1 (N. Virginia) Region to the on-premises network.
- Create a second 10-Gbps AWS Direct Connect connection to the existing AWS Direct Connect location.
- **Create a second 10-Gbps AWS Direct Connect connection to another AWS Direct Connect location.**
    
    Amazon Web Services (AWS) offers customers the ability to achieve highly resilient network connections between Amazon Virtual Private Cloud (Amazon VPC) and their on-premises infrastructure. This capability extends customer access to AWS resources in a reliable, scalable, and cost-effective way.
    
    You can achieve maximum resiliency for critical workloads by using separate connections that terminate on separate devices in more than one location:
    
    ![](https://media.tutorialsdojo.com/public/dc-max-resiliency.png)
    
    Highly resilient, fault-tolerant network connections are key to a well-architected system. AWS recommends connecting from multiple data centers for physical location redundancy. When designing remote connections, consider using redundant hardware and telecommunications providers. If you are only using a single AWS Direct Connect location, you have to create another Direct Connect connection to **another** AWS Direct Connect location to improve the connection’s resiliency.
    
    Additionally, it is a best practice to use dynamically routed, active/active connections for automatic load balancing and failover across redundant network connections. Provision sufficient network capacity to ensure that the failure of one network connection does not overwhelm and degrade redundant connections.
    
    Hence, the correct answer is: **Create a second 10-Gbps AWS Direct Connect connection to another AWS Direct Connect location.**
    
    The option that says: **Create a second 10-Gbps AWS Direct Connect connection to the existing AWS Direct Connect location** is incorrect because this setup does not provide high resiliency. If the single AWS Direct Connect location goes down, then there would be no way for the on-premises network to connect to the VPC and vice versa.
    
    The option that says: **Create a second 10-Gbps AWS Managed VPN connection between the VPC and the on-premises network** is incorrect because an AWS Managed VPN connection is not capable of reaching a throughput of 10-Gbps. Multiple VPN connections to the same Virtual Private Gateway are bound by an aggregate throughput limit from AWS to on-premises of up to 1.25 Gbps.
    
    The option that says: **Launch a Direct Connect Gateway that connects two public virtual interfaces in the us-east-1 (N. Virginia) Region to the on-premises network** is incorrect because a Direct Connect gateway simply enables you to use your existing AWS Direct Connect connection to access VPCs in your account on two or more AWS Regions. Moreover, you have to use **private** virtual interfaces in Direct Connect Gateway and not public virtual interfaces.
    

An organization has hundreds of Amazon VPCs for security, billing, regulatory, or other purposes that are located in us-east-1, us-east-2, us-west-1, and us-west-2 AWS Regions. A Network Engineer has been instructed to connect a VPC from the us-east-1 region to another VPC in the us-west-2 region. This will allow the two VPCs to communicate privately and securely with one another for sharing data or application services.

To improve security, the solution must also provide protection for outbound DNS requests from the VPCs to prevent DNS exfiltration of the customer data. This will ensure that no compromised application can use the DNS lookup function to maliciously send data out of the VPC to an external domain that an attacker can control.

What should the Network Engineer do to satisfy the above requirement in the MOST OPERATIONALLY-EFFICIENT WAY? (Select TWO.)

- Use AWS Cloud WAN to connect the two Amazon VPCs. Launch a new Amazon CloudFront distribution with Origin Shield to protect outbound DNS requests and prevent data exfiltration.
- **Set up an Inter-Region VPC peering between the two Amazon VPCs that are located in two different AWS Regions.**
- **Use Amazon Route 53 Resolver DNS Firewall to inspect and protect outbound DNS requests. Use a firewall rule group to define how Route 53 Resolver DNS Firewall inspects and filters traffic coming from your VPC**
- Set up an AWS Transit Gateway with VPC attachments and a multicast domain to connect the two VPCs. Configure AWS Network Firewall to filter out all outbound DNS requests.
- Launch a new private hosted zone in Amazon Route 53. Set up inbound and outbound Route 53 Resolver endpoints to connect the two Amazon VPCs. Use the Network Access Analyzer to analyze and block suspicious outbound DNS requests.
- **Evaluation**
    
    With Amazon VPC, you can provision logically isolated virtual networks to host your AWS resources. You can create multiple VPCs within the same region or in different regions, in the same account, or in different accounts. This is useful for customers who require multiple VPCs for security, billing, regulatory, or other purposes and want to integrate AWS resources between their VPCs more easily. More often than not, these different VPCs need to communicate privately and securely with one another for sharing data or applications.
    
    A VPC peering connection is a networking connection between two VPCs that enables you to route traffic between them privately. Instances in either VPC can communicate with each other as if they are within the same network. You can create a VPC peering connection between your own VPCs, with a VPC in another AWS account, or with a VPC in a different AWS Region.
    
    ![](https://media.tutorialsdojo.com/public/peering_intro_diagram.png)
    
    AWS uses the existing infrastructure of a VPC to create a VPC peering connection; it is neither a gateway nor a VPN connection and does not rely on a separate piece of physical hardware. There is no single point of failure for communication or a bandwidth bottleneck.
    
    With Route 53 Resolver DNS Firewall, you can filter and regulate outbound DNS traffic for your virtual private cloud (VPC). To do this, you create reusable collections of filtering rules in DNS Firewall rule groups, associate the rule groups to your VPC, and then monitor activity in DNS Firewall logs and metrics. Based on the activity, you can adjust the behavior of the DNS Firewall accordingly.
    
    ![](https://media.tutorialsdojo.com/amazon-route-53-dns-firewall.png)
    
    DNS Firewall provides protection for outbound DNS requests from your VPCs. These requests route through Resolver for domain name resolution. A primary use of DNS Firewall protections is to help prevent DNS exfiltration of your data. DNS exfiltration can happen when a bad actor compromises an application instance in your VPC and then uses DNS lookup to send data out of the VPC to a domain that they control.
    
    With DNS Firewall, you can monitor and control the domains that your applications can query. You can deny access to the domains that you know to be bad and allow all other queries to pass through. Alternately, you can deny access to all domains except for the ones that you explicitly trust.
    
    You can also use DNS Firewall to block resolution requests to resources in private hosted zones (shared or local), including VPC endpoint names. It can also block requests for public or private Amazon EC2 instance names.
    
    Hence, the correct answers are:
    
    **– Set up an Inter-Region VPC peering between the two VPCs that are located into two different AWS Regions**
    
    **– Use Amazon Route 53 Resolver DNS Firewall to inspect and protect outbound DNS requests. Use a firewall rule group to define how Route 53 Resolver DNS Firewall inspects and filters traffic coming from your VPC**
    
    The option that says: **Set up an AWS Transit Gateway with VPC attachments and a multicast domain to connect the two VPCs. Configure AWS Network Firewall to filter out all outbound DNS requests** is incorrect. Although you can connect two Amazon VPCs using a Transit Gateway, having the multicast option enabled is not warranted in this scenario. Multicast is a communication protocol used for delivering a single stream of data to multiple receiving computers simultaneously. Using AWS Network Firewall to filter out all outbound DNS requests is actually valid, but a more suitable option to use is the Route 53 Resolver DNS Firewall since the scenario only mentions outbound DNS requests.
    
    The option that says: **Use AWS Cloud WAN to connect the two Amazon VPCs. Launch a new Amazon CloudFront distribution with Origin Shield to protect outbound DNS requests and prevent data exfiltration**is incorrect because the AWS Cloud WAN service is primarily used to build, manage, and monitor global wide area networks and not for establishing connections between two VPCs. Moreover, the Origin Shield feature of Amazon CloudFront is not a security feature. Origin Shield is simply an additional layer in the CloudFront caching infrastructure that helps to minimize your origin’s load, improve its availability, and reduce its operating costs.
    
    The option that says: **Launch a new private hosted zone in Amazon Route 53. Set up inbound and outbound Route 53 Resolver endpoints to connect the two Amazon VPCs. Use the Network Access Analyzer to analyze and block suspicious outbound DNS requests** is incorrect because Route 53 Resolver endpoints are primarily used for resolving domain names and not for connecting VPCs. In addition, the Network Access Analyzer is simply a feature that identifies unintended network access to your resources on AWS. The Network Access Analyzer is not capable of filtering outbound DNS requests, unlike the Route 53 Resolver DNS Firewall.
    

A company uses Amazon Virtual Private Cloud (VPC) IP Address Manager (IPAM) to facilitate IP address allocation when creating VPCs for testing environments. Management wants to automate the deployment of AWS resources. For this reason, the network engineer must generate a VPC component template that can be reused for multiple environments.

Which is the MOST suitable approach that will satisfy the requirement?

- **Create an AWS CloudFormation template of the VPC and use an AWS Lambda function custom resource to request a CIDR block from the IPAM.**
    
    When you associate a Lambda function with a custom resource, the function is invoked whenever the custom resource is created, updated, or deleted. AWS CloudFormation calls a Lambda API to invoke the function and to pass all the request data (such as the request type and resource properties) to the function.
    
    ![AWS CloudFormation template with an AWS Lambda function custom resource](https://media.tutorialsdojo.com/aws-cf-template-custom-resource.jpg)
    
    The power and customizability of Lambda functions in combination with AWS CloudFormation enable a wide range of scenarios, such as dynamically looking up AMI IDs during stack creation, or implementing and using utility functions, such as string reversal functions. In our case, requesting a CIDR block from the IPAM.
    
    Hence, the correct answer is: **Create an AWS CloudFormation template of the VPC and use an AWS Lambda function custom resource to request a CIDR block from the IPAM.**
    
    The option that says: **Create an AWS CloudFormation template of the VPC and use the `!Ref` function to request a CIDR block from the IPAM** is incorrect. The `!Ref` intrinsic function just returns the value of a `Parameter` or `Resource` defined in a CloudFormation template.
    
    The option that says: **Use an AWS System Manager automation runbook to provision the VPC. Use the AWS CLI to request a CIDR block from the IPAM** is incorrect. Automation runbooks are typically more suitable for jobs like restarting EC2 instances or creating an Amazon Machine Image (AMI).
    
    The option that says: **Create an AWS CloudFormation template of the VPC. Provide all existing CIDR blocks from the IPAM in the `Parameters` section and use `!Ref` to get their value ****is incorrect. Although this solution could work, it is not fully ‘automated’ in the sense that you have to manually check for and select an available CIDR block that can be assigned to a VPC. All of this process can be coded in a Lambda function custom resource.
    
- Create an AWS CloudFormation template of the VPC. Provide all existing CIDR blocks from the IPAM in the `Parameters` section and use `!Ref` to get their value.
- Use an AWS System Manager automation runbook to provision the VPC. Use the AWS CLI to request a CIDR block from the IPAM.
- Create an AWS CloudFormation template of the VPC and use the `!Ref` function to request a CIDR block from the IPAM.

A company has multiple Amazon VPCs for each of its department namely Shared Services VPC, Service Consumer VPC, and an Outbound VPC proxy for domain whitelisting and content filtering. These common services ensure data compliance and network security of the company’s entire cloud infrastructure. For every new workload in AWS, the Network Team always creates a new VPC and manually peers it to the Shared VPCs.

What should the team do to automate the VPC Peering process for new AWS workloads?

- Prepare a custom AWS Lambda function that automatically creates peering connections in the new VPC to all of the Shared VPCs.
- **Prepare an AWS CloudFormation template that automatically creates peering connections in the new VPC to all of the Shared VPCs.**
    
    **Amazon Virtual Private Cloud (Amazon VPC)** enables you to launch AWS resources into a virtual network that you’ve defined. A VPC peering connection is a networking connection between two VPCs that enables you to route traffic between them using private IPv4 addresses or IPv6 addresses. Instances in either VPC can communicate with each other as if they are within the same network. You can create a VPC peering connection between your own VPCs, or with a VPC in another AWS account. The VPCs can be in different regions (also known as an inter-region VPC peering connection).
    
    ![](https://media.tutorialsdojo.com/public/peering_intro_diagram.png)
    
    You can peer with a virtual private cloud (VPC) in your or another, AWS account by using `AWS::EC2::VPCPeeringConnection`. This creates a networking connection between two VPCs that enables you to route traffic between them so they can communicate as if they were within the same network. A VPC peering connection can help facilitate data access and data transfer. To establish a VPC peering connection, you need to authorize two separate AWS accounts within a single AWS CloudFormation stack.
    
    Hence, the correct answer is: **Prepare an AWS CloudFormation template that automatically creates peering connections in the new VPC to all of the Shared VPCs.**
    
    The option that says: **Launch a Transit VPC in an AWS Direct Connect location. Set up a network configuration that automatically connects the new VPC to the existing Shared Services, Service Consumer, and Outbound VPCs** is incorrect because you can only launch a Direct Connect connection in an AWS Direct Connect location and not a Transit VPC.
    
    The option that says: **Prepare a custom AWS Lambda function that automatically creates peering connections in the new VPC to all of the Shared VPCs** is incorrect because AWS Lambda, while capable of running code in response to events and automatically managing the compute resources for you, requires specific programming to set up VPC Peering connections. It does not inherently automate the setup of VPC Peering connections to your new VPC.
    
    The option that says: **Launch an AWS Transit Gateway that will automatically connect the new VPC to the existing Shared Services, Service Consumer, and Outbound VPCs by default. The Transit Gateway will immediately create and approve the peering requests to the VPCs without any manual intervention** is incorrect because, by default, AWS Transit Gateway doesn’t automatically create and approve VPC Peering connections by default. You have to manually configure this setup.
    
- Launch a Transit VPC in an AWS Direct Connect location. Set up a network configuration that automatically connects the new VPC to the existing Shared Services, Service Consumer, and Outbound VPCs by default.
- Launch an AWS Transit Gateway that will automatically connect the new VPC to the existing Shared Services, Service Consumer, and Outbound VPCs. The Transit Gateway will immediately create and approve the peering requests to the VPCs without any manual intervention.

A Network Engineer has recently established a 1 Gbps AWS Direct Connect (DX) connection between the company’s on-premises data center and its AWS resources that are located in the US East (N. Virginia) Region. A public virtual interface was used for the DX connection.

Which of the following can the on-premises servers access to use this interface? (Select TWO.)

- **Amazon S3 buckets in any AWS Region excluding the AWS China Region.**
- Any Amazon EC2 instance in the US East (N. Virginia) Region.
- **All AWS public IP addresses globally.**
- All AWS public IP addresses in the US East (N. Virginia) Region only.
- Amazon S3 buckets in the US East (N. Virginia) Region only.
- **Evaluation**
    
    **AWS Direct Connect (DX)** provides three types of virtual interfaces: public, private, and transit. You can create a transit virtual interface to connect to a transit gateway, a public virtual interface to connect to public resources (non-VPC services), or a private virtual interface to connect to a VPC. AWS Direct Connect links your internal network to an AWS Direct Connect location over a standard Ethernet fiber-optic cable. One end of the cable is connected to your router, the other to an AWS Direct Connect router.
    
    With this connection, you can create *virtual interfaces* directly to public AWS services (for example, to Amazon S3) or to Amazon VPC, bypassing internet service providers in your network path. An AWS Direct Connect location provides access to AWS in the Region with which it is associated. You can use a single connection in a public Region or AWS GovCloud (US) to access public AWS services in all other public Regions.
    
    ![](https://media.tutorialsdojo.com/aws_direct_conneect_plus_vpn.png)
    
    To connect to AWS resources that are reachable by a public IP address (such as an Amazon Simple Storage Service bucket) or AWS public endpoints, use a public virtual interface. With a public virtual interface, you can:
    
    - Connect to all AWS public IP addresses globally.
    - Create public virtual interfaces in any DX location to receive Amazon’s global IP routes.
    - Access publicly routable Amazon services in any AWS Region (except the AWS China Region).
    
    Hence, the correct answers are:
    
    - **Amazon S3 buckets in any AWS Region excluding the AWS China Region.**
    - **All AWS public IP addresses globally.**
    
    The option that says: **Amazon S3 buckets in the US East (N. Virginia) Region only** is incorrect. With a public virtual interface, you can connect to any public routable Amazon S3 bucket and not just to a particular AWS Region.
    
    The option that says: **All AWS public IP addresses in the US East (N. Virginia) Region only** is incorrect because this setup actually allows you to connect to all AWS public IP addresses globally.
    
    The option that says: **Any Amazon EC2 instance in the US East (N. Virginia) Region** is incorrect because a public virtual interface is primarily used to connect to public resources (non-VPC services). If you want to connect to your EC2 instances or other VPC resources, you have to use a private virtual interface instead.
    

A legacy application that uses TCP protocol to accept incoming requests is being migrated to AWS. An Auto Scaling group of Amazon EC2 instances behind an ELB will be used to host the application. The IT Network Consultant needs to ensure that the IP addresses of the clients who use the application are recorded for tracking purposes.

What should the Consultant do to fulfill this requirement?

- Use an Application Load Balancer with an HTTP/HTTPS Listener and enable Cross-Zone Load Balancing.
- Use an Application Load Balancer with a TCP Listener and enable Proxy Protocol.
- **Use a Network Load Balancer with a TCP Listener and enable Proxy Protocol.**
    
    Network Load Balancers use Proxy Protocol version 2 to send additional connection information, such as the source and destination. Proxy Protocol version 2 provides a binary encoding of the Proxy Protocol header. The load balancer prepends a proxy protocol header to the TCP data. It does not discard or overwrite any existing data, including any proxy protocol headers sent by the client or any other proxies, load balancers, or servers in the network path. Therefore, it is possible to receive more than one proxy protocol header. Also, if there is another network path to your targets outside of your Network Load Balancer, the first proxy protocol header might not be the one from your Network Load Balancer.
    
    ![Proxy Protocol version 2](https://media.tutorialsdojo.com/public/2020-06-01_13-16-51-d88073a1ba808744f7f8bae874e2c286.png)
    
    If you specify targets by IP address, the source IP addresses provided to your applications are the private IP addresses of the load balancer nodes. If your applications need the IP addresses of the clients, enable Proxy Protocol and get the client IP addresses from the Proxy Protocol header.
    
    If you specify targets by instance ID, the source IP addresses provided to your applications are the client IP addresses. However, if you prefer, you can enable Proxy Protocol and get the client IP addresses from the Proxy Protocol header.
    
    Hence, the correct answer is: **Use a Network Load Balancer with a TCP Listener and enable Proxy Protocol.**
    
    The option that says: **Use a Network Load Balancer with a TCP Listener and enable zonal shift** is incorrect because zonal shift is primarily designed for availability and disaster recovery by temporarily shifting traffic away from a failing Availability Zone. It does not modify client connection metadata or preserve the original client IP address, which is the requirement of this scenario.
    
    The option that says: **Use an Application Load Balancer with a TCP Listener and enable Proxy Protocol** is incorrect because you can’t enable the Proxy Protocol option in ALB.
    
    The option that says: **Use an Application Load Balancer with an HTTP/HTTPS Listener and enable Cross-Zone Load Balancing** is incorrect because you have to typically use a TCP Listener for the application. For Application Load Balancers with HTTP/HTTPS listeners, you must use X-Forwarded-For headers to capture client IP addresses. Then, you must print those client IP addresses in your access logs. However, in this scenario, you can’t use an HTTP/HTTPS Listener since the legacy application is using TCP.
    
- Use a Network Load Balancer with a TCP Listener and enable zonal shift

An online delivery system is hosted in an Auto Scaling group of EC2 instances deployed across multiple Availability Zones with an Application Load Balancer in front to distribute the incoming traffic. A Network Engineer has been instructed to decouple the system and route different types of requests to specific target groups. Each workload type will have a dedicated group of EC2 instances. If there is an incoming request with a URL of <server>/api/electronics, it must be forwarded to a specific target group named “Electronics-Target-Group” and if the request has a URL of <server>/api/books then it should be forwarded to another separate target group named “Books-Target-Group”.

How can the Engineer implement this change in AWS?

- Set up a new listener in the Application Load Balancer with Host conditions for the Electronics and Books target groups.
- Replace your Application Load Balancer with a Network Load Balancer. Create a new listener with Path conditions for the Electronics and Books target groups.
- **Add a listener in the Application Load Balancer with two Path conditions for the Electronics and Books target groups.**
    
    You can use path conditions to define rules that forward requests to different target groups based on the URL in the request (also known as *path-based routing*). Each path condition has one path pattern. If the URL in a request matches the path pattern in a listener rule exactly, the request is routed using that rule.
    
    If you are running microservices, you can route traffic to multiple back-end services using path-based routing. For example, you can route general requests to one target group and requests to render images to another target group.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-16_13-58-25-770c70b9dc22df292226a6f660ef362a.png)
    
    A path pattern is case-sensitive, can be up to 128 characters in length, and can contain any of the following characters. You can include up to three wildcard characters.
    
    A–Z, a–z, 0–9
    
    _ – . $ / ~ ” ‘ @ : +
    
    & (using &amp;)
    
    - (matches 0 or more characters)
    
    ? (matches exactly 1 character)
    
    Example path patterns
    
    `/img/*`
    
    `/js/*`
    
    Each listener has a default rule, and you can optionally define additional rules. Each rule consists of a priority, one or more actions, and one or more conditions. You can add or edit rules at any time.
    
    Hence, the correct answer is: **Add a listener in the Application Load Balancer with two Path conditions for the Electronics and Books target groups.**
    
    The option that says: **Set up a new listener in the Application Load Balancer with Host conditions for the Electronics and Books target groups** is incorrect because host-based routing defines rules that forward requests to different target groups based on the hostname in the host header instead of the URL path. You have to use a Path-based rule instead.
    
    The option that says: **Replace your Application Load Balancer with a Network Load Balancer. Create a new listener with Path conditions for the Electronics and Books target groups** is incorrect because a Network Load Balancer does not support path-based routing. In order to fulfill this task, you must use an Application Load Balancer and create a listener with Path conditions.
    
    The option that says: **Configure a new listener in the Application Load Balancer with two Host Header conditions for the Electronics and Books target groups** is incorrect because Host Header conditions are primarily used to configure rules that route requests based on the HTTP headers for the request and not based on the URL path. This tracks the header of the request, such as “User-Agent”, “Cache-Control” et cetera.
    
- Configure a new listener in the Application Load Balancer with two Host Header conditions for the Electronics and Books target groups.

A company has several VPCs that are connected to an AWS Transit Gateway, each having a VPC attachment with appliance mode support enabled. One of their websites is hosted in an Auto Scaling group of EC2 instances with an Application Load Balancer in front to evenly distribute the load.

The Network Administrator has been instructed to capture information about the IP traffic going to and from the network interfaces of every EC2 instance. This will automate the process of detecting and diagnosing overly restrictive security group rules in the company’s VPC.

The logged traffic must also include the `vpc-id`, `subnet-id`, `instance-id` and `interface-id`information if the traffic originated from one of its VPCs.

What should be done to satisfy this requirement?

- Set up a Network Access Scope using the Network Access Analyzer service. Create custom metrics to capture the `vpc-id`, `subnet-id`, `instance-id` and `interface-id` data.
- Launch an AWS Network Firewall in a centralized deployment model to capture East-West (VPC-to-VPC) or North-South (internet egress and ingress, on-premises) traffic which includes VPC-related information by default
- Utilize the Traffic Mirroring feature to capture VPC-related traffic information such as subnets and network interface data on all VPC attachments of the AWS Transit Gateway. Use the AWS Resource Access Manager (AWS RAM) to capture the `instance-id` data of the Amazon EC2 instances.
- **Use VPC Flow Logs with a custom format to include the `vpc-id`, `subnet-id`, `instance-id` and `interface-id` data fields.**
    
    **VPC Flow Logs** is a feature that enables you to capture information about the IP traffic going to and from network interfaces in your VPC. Flow log data can be published to Amazon CloudWatch Logs and Amazon S3. After you’ve created a flow log, you can retrieve and view its data in the chosen destination.
    
    Flow logs can help you with a number of tasks, such as:
    
    – Diagnosing overly restrictive security group rules
    
    – Monitoring the traffic that is reaching your instance
    
    – Determining the direction of the traffic to and from the network interfaces
    
    Flow log data is collected outside the path of your network traffic and therefore does not affect network throughput or latency. You can create or delete flow logs without any risk of impact on network performance.
    
    ![](https://media.tutorialsdojo.com/amazon-vpc-flow-logs-ans-c01.png)
    
    With a custom format, you specify which fields are included in the flow log records and in which order. This enables you to create flow logs that are specific to your needs and to omit fields that are not relevant. Using a custom format can reduce the need for separate processes to extract specific information from the published flow logs. You can specify any number of the available flow log fields, but you must specify at least one.
    
    When publishing flow log data to Amazon S3, the data type for the fields depends on the flow log format. If the format is plain text, all fields are of type STRING. If the format is Parquet, see the table for the field data types.
    
    If a field is not applicable or cannot be computed for a specific record, the record displays a ‘-‘ symbol for that entry. Metadata fields that do not come directly from the packet header are best-effort approximations, and their values might be missing or inaccurate.
    
    Hence, the correct answer is: **Use VPC Flow Logs with a custom format to include the `vpc-id`, `subnet-id`, `instance-id` And `interface-id` data fields.**
    
    The option says: **Utilize the Traffic Mirroring feature to capture VPC-related traffic information such as subnets and network interface data on all VPC attachments of the AWS Transit Gateway. Using the AWS Resource Access Manager (AWS RAM) to capture the `instance-id` data of the Amazon EC2 instances**is incorrect because traffic Mirroring copies network traffic from the elastic network interfaces of your VPC. This can’t be used in diagnosing overly restrictive security group rules and capturing custom traffic information. In addition, the traffic Mirroring feature is not meant to be integrated with AWS RAM since the latter is primarily used for cross-account resource sharing.
    
    The option says: **Set up a Network Access Scope using the Network Access Analyzer service. Create custom metrics to capture the `vpc-id`, `subnet-id`, `instance-id` and `interface-id` data** is incorrect. The Network Access Analyzer is just a feature that identifies unintended network access to your resources on AWS. This service is commonly used to implement your network access requirements and to identify potential network paths that do not meet your specified needs.
    
    The option that says: **Launch an AWS Network Firewall in a centralized deployment model to capture East-West (VPC-to-VPC) or North-South (internet egress and ingress, on-premises) traffic which includes VPC-related information by default,** is incorrect because an AWS Firewall is a managed service that only simplifies the deployment of essential network protections for all of your Amazon Virtual Private Clouds (VPC). It is not capable of capturing custom network information such as the VPC ID, Subnet ID, Interface ID, or the Instance ID of the incoming request.
    

A company is building its customer web portal in multiple EC2 instances behind an Application Load Balancer. The portal must be accessible on www.tutorialsdojo.com as well as on its tutorialsdojo.com root domain.

How should the Network Engineer set up Amazon Route 53 to satisfy this requirement?

- Set up a non-alias A Record for tutorialsdojo.com with the ALB as the target. For the www.tutorialsdojo.com subdomain, create a CNAME record that points to the ALB.
- Set up a CNAME Record for tutorialsdojo.com with the ALB as the target. For the www.tutorialsdojo.com subdomain, create a CNAME record that points to the ALB.
- Set up a CNAME Record for tutorialsdojo.com with the ALB as the target. For the www.tutorialsdojo.com subdomain, create an Alias A record that points to the ALB.
- **Set up an Alias A Record for tutorialsdojo.com with the ALB as the target. For the www.tutorialsdojo.com subdomain, create a CNAME record that points to the ALB.**
    
    Amazon Route 53 alias records provide a Route 53–specific extension to DNS functionality. Alias records let you route traffic to selected AWS resources, such as CloudFront distributions and Amazon S3 bucket. They also let you route traffic from one record in a hosted zone to another record.
    
    ![](https://media.tutorialsdojo.com/public/dark-mode-04_24AUG2023.png)
    
    Unlike a CNAME record, you can create an alias record at the top node of a DNS namespace, also known as the zone apex. For example, if you register the DNS name tutorialsdojo.com, the zone apex is tutorialsdojo.com. You can’t create a CNAME record for tutorialsdojo.com, but you can create an alias record for tutorialsdojo.com that routes traffic to www.tutorialsdojo.com.
    
    When Route 53 receives a DNS query for an alias record, Route 53 responds with the applicable value for that resource:
    
    **A CloudFront distribution** – Route 53 responds with one or more IP addresses for CloudFront edge servers that can serve your content.
    
    **An Elastic Beanstalk environment** – Route 53 responds with one or more IP addresses for the environment.
    
    **An ELB load balancer** – Route 53 responds with one or more IP addresses for the load balancer.
    
    **An Amazon S3 bucket that is configured as a static website** – Route 53 responds with one IP address for the Amazon S3 bucket.
    
    **Another Route 53 record in the same hosted zone** – Route 53 responds as if the query is for the record that is referenced by the alias record.
    
    Hence, the correct answer is: **Set up an Alias A Record for tutorialsdojo.com with the ALB as the target. For the www.tutorialsdojo.com subdomain, create a CNAME record that points to the ALB.**
    
    The option that says: **Set up a CNAME Record for tutorialsdojo.com with the ALB as the target. For the www.tutorialsdojo.com subdomain, create a CNAME record that points to the ALB** is incorrect. Although the configuration for the subdomain is correct, you still can’t create a CNAME record for the root domain or zone apex. You have to set up an Alias A record instead.
    
    The option that says: **Set up a CNAME Record for tutorialsdojo.com with the ALB as the target. For the www.tutorialsdojo.com subdomain, create an Alias A record that points to the ALB** is incorrect because you can’t create a CNAME record for the root domain or zone apex. The subdomain configuration is technically correct because you can set up an Alias A record for your subdomain in Route 53. The issue here is the use of a CNAME record in the zone apex.
    
    The option that says: **Set up a non-alias A Record for tutorialsdojo.com with the ALB as the target. For the www.tutorialsdojo.com subdomain, create a CNAME record that points to the ALB** is incorrect because a non-alias A Record can only accept IP addresses and not the DNS name of the ALB.
    

A company has a suite of publicly accessible web applications that are hosted in several Amazon EC2 instances. To improve the infrastructure security, the Network Engineer must automate the network configuration analysis of all EC2 instances that regularly checks for ports that are reachable from outside the VPC. This will protect the architecture from malicious activities and port scans by external systems. The solution should also highlight network configurations that allow for potentially malicious access, such as mismanaged security groups, ACLs, IGWs, and other vulnerabilities.

What should the Engineer do to satisfy this requirement?

- **Amazon Inspector**
    
    You can use **Amazon Inspector** to assess your assessment targets (collections of AWS resources) for potential security issues and vulnerabilities. Amazon Inspector compares the behavior and the security configuration of the assessment targets to selected security rule packages. In the context of Amazon Inspector, a rule is a security check that Amazon Inspector performs during the assessment run.
    
    An Amazon Inspector assessment can use any combination of the following rules packages:
    
    Network assessments:
    
    - Network Reachability
    
    Host assessments:
    
    - Common vulnerabilities and exposures
    - Center for Internet Security (CIS) Benchmarks
    - Security best practices for Amazon Inspector
    
    ![](https://media.tutorialsdojo.com/public/2020-05-21_07-57-27-6fb66c9682bdadb39bcf6dc822cff2dd.png)
    
    The rules in the Network Reachability package analyze your network configurations to find security vulnerabilities of your EC2 instances. The findings that Amazon Inspector generates also provide guidance about restricting access that is not secure. The findings generated by these rules show whether your ports are reachable from the Internet through an Internet gateway (including instances behind Application Load Balancers or Classic Load Balancers), a VPC peering connection, or a VPN through a virtual gateway.
    
    These findings also highlight network configurations that allow for potentially malicious access, such as mismanaged security groups, ACLs, IGWs, and so on. These rules help automate the monitoring of your AWS networks and identify where network access to your EC2 instances might be misconfigured. By including this package in your assessment run, you can implement detailed network security checks without having to install scanners and send packets, which are complex and expensive to maintain, especially across VPC peering connections and VPNs.
    
    Hence, the correct answer is: **Amazon Inspector.**
    
    **Bidirectional Forwarding Detection (BFD)** is incorrect because this is just a detection protocol to provide fast forwarding path failure detection times, which allows for a faster routing re-convergence time. This is primarily used in an AWS Direct Connect (DX) connection and not for analyzing your network configurations to find security vulnerabilities in your EC2 instances.
    
    **AWS Security Hub** is incorrect because it only gives you a comprehensive view of your high-priority security alerts and security posture across your AWS accounts.
    
    **Amazon Macie** is incorrect because this is simply a fully managed data security and data privacy service that uses machine learning and pattern matching to discover and protect your sensitive data in AWS. Macie is primarily used in Amazon S3 to identify and alert you for sensitive data in your S3 buckets, such as personally identifiable information (PII).
    
- Amazon Macie
- AWS Security Hub
- Bidirectional Forwarding Detection (BFD)

A Network Administrator is setting up a Site-to-Site VPN connection between the company’s on-premises data center to an Amazon VPC. She has downloaded the configuration file for the customer gateway device from the Amazon VPC Console that contains the device configuration as well as the optional tunnel options.

What are the valid VPN tunnel options that the Administrator can configure for the VPN connection? (Select THREE.)

- Path MTU Discovery (PMTUD)
- Bidirectional Forwarding Detection (BFD)
- **Dead peer detection (DPD) timeout**
- **Pre-shared key (PSK)**
- Multiprotocol Label Switching (MPLS)
- **Phase 2 Diffie-Hellman (DH) group numbers**
- **Evaluation**
    
    You use a Site-to-Site VPN connection to connect your remote network to a VPC. Each Site-to-Site VPN connection has two tunnels, with each tunnel using a unique virtual private gateway public IP address. It is important to configure both tunnels for redundancy. When one tunnel becomes unavailable (for example, down for maintenance), network traffic is automatically routed to the available tunnel for that specific Site-to-Site VPN connection.
    
    ![](https://media.tutorialsdojo.com/public/Multiple_VPN_Tunnels_diagram.png)
    
    When you create a Site-to-Site VPN connection, you download a configuration file specific to your customer gateway device that contains information for configuring the device, including information for configuring each tunnel. You can optionally specify some of the tunnel options yourself when you create the Site-to-Site VPN connection. Otherwise, AWS provides default values.
    
    The following are some of the tunnel options that you can configure for your VPN connection:
    
    **Dead peer detection (DPD) timeout** – The duration after which DPD timeout occurs. You can specify 30 or higher.
    
    **IKE versions** – The IKE versions that are permitted for the VPN tunnel. You can specify one or more of the default values.
    
    **Inside tunnel CIDR** – The range of inside IP addresses for the VPN tunnel. You can specify a size /30 CIDR block from the `169.254.0.0/16` range. The CIDR block must be unique across all Site-to-Site VPN connections that use the same virtual private gateway.
    
    **Phase 1 Diffie-Hellman (DH) group numbers** – The DH group numbers that are permitted for the VPN tunnel for phase 1 of the IKE negotiations. You can specify one or more of the default values.
    
    **Pre-shared key (PSK)** – The pre-shared key (PSK) to establish the initial internet key exchange (IKE) security association between the virtual private gateway and customer gateway.
    
    **Phase 2 Diffie-Hellman (DH) group numbers** – The DH group numbers that are permitted for the VPN tunnel for phase 2 of the IKE negotiations. You can specify one or more of the default values.
    
    Take note that an AWS VPN connection does not support [Path MTU Discovery (PMTUD)](https://tools.ietf.org/html/rfc1191)
    
    Hence, the correct answers are:
    
    - **Pre-shared key (PSK)**
    - **Dead peer detection (DPD) timeout**
    - **Phase 2 Diffie-Hellman (DH) group numbers**
    
    **Bidirectional Forwarding Detection (BFD)** is incorrect because BFD is a detection protocol commonly used in AWS Direct Connect connections that provides fast forwarding path failure detection times which allows for faster routing re-convergence time.
    
    **Multiprotocol Label Switching (MPLS)** is incorrect because this is not a valid tunnel option for a VPN connection. MPLS is just an encapsulation protocol used in many service providers and largescale enterprise networks. Instead of relying on IP lookups to discover a viable “next-hop” at every single router within a path (as in traditional IP networking), MPLS predetermines the path and uses a label-swapping push, pop, and swap method to direct the traffic to its destination.
    
    **Path MTU Discovery (PMTUD)** is incorrect because an AWS VPN connection does not support  PMTUD.
    

After a year of development, the company’s 100-node high-performance computing (HPC) application is now ready to be deployed to an Amazon Elastic Kubernetes Service (Amazon EKS) cluster with Horizontal Pod Autoscaler (HPA). The application must be capable of receiving millions of UDP traffic per second from the public Internet while maintaining low latency.

Which of the following is the most operationally efficient solution that should be implemented to meet the above requirements?

- **Integrate the AWS Load Balancer Controller add-on to the EKS cluster. Launch a Network Load Balancer to load balance network traffic to individual Kubernetes pods.**
    
    Network Load Balancer operates at the connection level (Layer 4), routing connections to targets – Amazon EC2 instances, microservices, and containers – within Amazon Virtual Private Cloud (Amazon VPC) based on IP protocol data. Ideal for load balancing of both TCP and UDP traffic, Network Load Balancer is capable of handling millions of requests per second while maintaining ultra-low latencies. Network Load Balancer is optimized to handle sudden and volatile traffic patterns while using a single static IP address per Availability Zone. It is integrated with other popular AWS services such as Auto Scaling, Amazon EC2 Container Service (ECS), Amazon CloudFormation, and AWS Certificate Manager (ACM).
    
    Network Load Balancer preserves the client-side source IP address, allowing the back-end EC2 instances to see the IP address of the client. This can then be used by applications for further processing.
    
    ![](https://media.tutorialsdojo.com/aws-elb-comparison-table.png)
    
    Network traffic is load balanced at L4 of the OSI model. To load balance application traffic at L7, you deploy a Kubernetes ingress, which provisions an AWS Application Load Balancer. An AWS Network Load Balancer can load balance network traffic to pods deployed to Amazon EC2 IP and instance targets or to AWS Fargate IP targets.
    
    The AWS Load Balancer Controller manages AWS Elastic Load Balancers for a Kubernetes cluster. The controller provisions the following resources:
    
    – An AWS Application Load Balancer (ALB) when you create a Kubernetes Ingress.
    
    – An AWS Network Load Balancer (NLB) when you create a Kubernetes service of type LoadBalancer.
    
    In the past, the Kubernetes network load balancer was used for instance targets, but the AWS Load balancer Controller was used for IP targets. With the AWS Load Balancer Controller version 2.3.0 or later, you can create NLBs using either target type. The AWS Load Balancer Controller controller was formerly named the AWS ALB Ingress Controller. It’s an open-source project managed on GitHub.
    
    Hence, the correct answer is **Integrate the AWS Load Balancer Controller add-on to the EKS cluster. Launch a Network Load Balancer to load balance network traffic to individual Kubernetes pods.**
    
    The option that says: **Install the AWS Load Balancer Controller add-on to the EKS cluster and launch an Application Load Balancer to distribute the incoming traffic to the Kubernetes pods** is incorrect because an ALB is not capable of handling millions of requests per second while maintaining ultra-low latencies, unlike a Network Load Balancer. Moreover, this type of load balancer doesn’t preserve the client-side source IP address.
    
    The option that says: **Launch a Gateway Load Balancer and integrate an Elastic Fabric Adapter (EFA) to each Kubernetes pod deployed in the Amazon EKS cluster** is incorrect because a Gateway Load Balancer simply helps you easily deploy, scale, and manage your third-party virtual appliances. This type of load balancer is not capable of receiving millions of requests per second while maintaining low latency.
    
    The option that says: **Set up the `kube-proxy` Amazon EKS add-on to the cluster and configure the Source Network Address Translation (SNAT) of the Kubernetes pods by setting the `AWS_VPC_K8S_CNI_EXTERNALSNAT` configuration to true** is incorrect because `kube-proxy` just maintains network rules on each Amazon EC2 node of the EKS cluster. In addition, changing the default SNAT configuration is also unwarranted since the scenario didn’t mention any connection between your EKS cluster VPC to another VPC peering, a transit VPC, or a Direct Connect connection. SNAT must only be changed if these aforementioned resources need to initiate communication with your pods using an IPv4 address.
    
- Launch a Gateway Load Balancer and integrate an Elastic Fabric Adapter (EFA) to each Kubernetes pod deployed in the Amazon EKS cluster
- Set up the `kube-proxy` Amazon EKS add-on to the cluster and configure the Source Network Address Translation (SNAT) of the Kubernetes pods by setting the `AWS_VPC_K8S_CNI_EXTERNALSNAT` configuration to true.
- Install the AWS Load Balancer Controller add-on to the EKS cluster and launch an Application Load Balancer to distribute the incoming traffic to the Kubernetes pods.

A serverless application is being built by the Development team using API Gateway, AWS Lambda function, Amazon DynamoDB, and an Amazon ElastiCache cluster launched in a private subnet. The Network team has configured the Lambda function to connect to the private subnet in the VPC to access the ElastiCache cluster. The function will process the data and send a message to an Amazon SQS queue that will store the result in a DynamoDB table. The company only has a single AWS account with one VPC.

What other actions should the Network team implement to complete the required network architecture? (Select THREE.)

- **Associate an IAM role to the Lambda function to access the Amazon SQS queue and the DynamoDB table.**
- **Ensure that the associated security group of the Lambda function allows outbound connections.**
- The Network ACL rules of the ElastiCache cluster and DynamoDB table must be configured to allow traffic from the Elastic IP address of the Lambda function.
- Associate a Security Control Policy (SCP) to the Lambda function to access the Amazon SQS queue and the DynamoDB table.
- **Grant Internet access to the Lambda function by associating a NAT gateway to the VPC where the Amazon ElastiCache cluster is hosted.**
- Grant Internet access to the Lambda function by associating an egress-only gateway to the VPC where the DynamoDB table and ElastiCache cluster are hosted.
- **Evaluation**
    
    One of the scenarios where you may want to use Amazon ElastiCache in a VPC is if you want to run a public-facing web application while still maintaining non-publicly accessible backend servers in a private subnet. You can create a public-facing subnet for your webservers that has access to the Internet and place your backend infrastructure in a private-facing subnet with no Internet access. Your backend infrastructure could include RDS DB Instances and an Amazon ElastiCache Cluster providing the in-memory layer.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-13_07-23-51-92820c3f03e686efc4062e824bce55d4.png)
    
    By default, Lambda runs your functions in a secure VPC with access to AWS services and the Internet. The VPC is owned by Lambda and does not connect to your account’s default VPC. When you connect a function to a VPC in your account, it does not have access to the Internet unless your VPC provides access. AWS Lambda uses the VPC information you provide to set up ENIs that allow your Lambda function to access VPC resources. Each ENI is assigned a private IP address from the IP address range within the subnets you specify but is not assigned any public IP addresses.
    
    ![](https://media.tutorialsdojo.com/public/2019-03-29_11-05-47-723c6ecd7c3cab0735db540d119de420.png)
    
    If your Lambda function requires access to public AWS services such as Amazon DynamoDB and Amazon SQS, you can configure a NAT instance or NAT Gateway inside your VPC. You cannot use an Internet gateway attached to your VPC since that requires the ENI to have public IP addresses.
    
    If your Lambda function needs Internet access, do not attach it to a public subnet or to a private subnet without Internet access. Instead, attach it only to private subnets with Internet access through a NAT Gateway. You should also ensure that the associated security group of the Lambda function allows outbound connections.
    
    Hence, the correct answers in this scenario are:
    
    - **Associate an IAM role to the Lambda function to access the Amazon SQS queue and the DynamoDB table**
    - **Grant Internet access to the Lambda function by associating a NAT gateway to the VPC where the Amazon ElastiCache cluster is hosted.**
    - **Ensure that the associated security group of the Lambda function allows outbound connections.**
    
    The option that says: **Associate a Security Control Policy (SCP) to the Lambda function to access the Amazon SQS queue and the DynamoDB table** is incorrect because the scenario clearly said that the company only has a single AWS account with one VPC. A Service Control Policy (SCPs) is just a type of policy for AWS Organizations that you can use to manage multiple AWS accounts.
    
    The option that says: **Grant Internet access to the Lambda function by associating an egress-only gateway to the VPC where the DynamoDB table and ElastiCache cluster are hosted** is incorrect because you have to use a NAT Gateway instead of an egress-only gateway. DynamoDB is a fully managed service by AWS, therefore, can’t be hosted in your VPC.
    
    The option that says: **The Network ACL rules of the ElastiCache cluster and DynamoDB table must be configured to allow traffic from the Elastic IP address of the Lambda function** is incorrect because you can’t associate an Elastic IP address to your Lambda function. You only have to ensure that the associated security group of the Lambda function allows outbound connections.
    

A company has a suite of public web applications hosted in four Auto Scaling groups. To support HTTPS traffic, a wild card certificate for the *.tutorialsdojo.com domain has been generated from AWS ACM which will be added to a new ELB. The application suite is composed of the following sites:

```

tutorialsdojo.com portal.tutorialsdojo.com app.tutorialsdojo.com mail.tutorialsdojo.com

```

A Network Engineer has been tasked to route the company’s domains and subdomains to the appropriate site using a single load balancer. What must the Engineer do to satisfy this requirement?

- Set up an Application Load Balancer and add the new certificate from AWS Certificate Manager. Associate a corresponding target group for each Auto Scaling group. Create a listener with a Query String condition to route the zone apex (`tutorialsdojo.com`) and the subdomains (`.tutorialsdojo.com`) to appropriate target groups.
- Launch a Network Load Balancer and add the new certificate from AWS Certificate Manager. For each Auto Scaling group, add and associate a corresponding target group. Create a listener with an HTTP Header condition to route the zone apex (`tutorialsdojo.com`) and the subdomains (`.tutorialsdojo.com`) to appropriate target groups.
- Create a Network Load Balancer and add the new certificate from AWS Certificate Manager. For each Auto Scaling group, add and associate a corresponding target group. Set up a listener with a Path condition to route the zone apex (`tutorialsdojo.com`) and the subdomains (`.tutorialsdojo.com`) to appropriate target groups.
- **Launch an Application Load Balancer and add the new certificate from AWS Certificate Manager. Associate a corresponding target group for each Auto Scaling group. Set up a listener with a Host condition to route the zone apex (`tutorialsdojo.com`) and the subdomains (`.tutorialsdojo.com`) to appropriate target groups.**
    
    Before you start using your Application Load Balancer, you must add one or more *listeners*. A listener is a process that checks for connection requests using the protocol and port that you configure. The rules that you define for a listener determine how the load balancer routes request to its registered targets.
    
    You can use an HTTPS listener to offload the work of encryption and decryption to your load balancer so that your applications can focus on their business logic. If the listener protocol is HTTPS, you must deploy at least one SSL server certificate on the listener.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-16_08-14-26-9ab6036ad969f3a9fdb0c6a4d7538581.png)
    
    Each listener has a default rule, and you can optionally define additional rules. Each rule consists of a priority, one or more actions, and one or more conditions. You can add or edit rules at any time. The following are the supported condition types for a rule:
    
    - **host-header** – Route based on the hostname of each request.
    - **http-header** – Route based on the HTTP headers for each request.
    - **http-request-method** – Route based on the HTTP request method of each request.
    - **path-pattern** – Route based on path patterns in the request URLs.
    - **query-string** – Route based on key/value pairs or values in the query strings.
    - **source-ip** – Route based on the source IP address of each request.
    
    You can use host conditions to define rules that route requests based on the hostname in the host header (also known as *host-based routing*). This enables you to support multiple domains using a single load balancer.
    
    Hence, the correct answer is: **Launch an Application Load Balancer and add the new certificate from AWS Certificate Manager. Associate a corresponding target group for each Auto Scaling group. Set up a listener with a Host condition to route the zone apex (`tutorialsdojo.com`) and the subdomains (`*.tutorialsdojo.com`) to the appropriate target groups.**
    
    The option that says: **Create a Network Load Balancer and add the new certificate from AWS Certificate Manager. For each Auto Scaling group, add and associate a corresponding target group. Set up a listener with a Path condition to route the zone apex (`tutorialsdojo.com`) and the subdomains (`*.tutorialsdojo.com`) to the appropriate target groups** is incorrect because a Network Load Balancer cannot have a listener with a Path condition type. You have to use an Application Load Balancer instead.
    
    The option that says: **Set up an Application Load Balancer and add the new certificate from AWS Certificate Manager. Associate a corresponding target group for each Auto Scaling group. Create a listener with a Query String condition to route the zone apex (`tutorialsdojo.com`) and the subdomains (`*.tutorialsdojo.com`) to the appropriate target groups** is incorrect because this just routes the traffic based on key/value pairs or values in the query strings that can be found in the URL (e.g. `<URL>?version=1&sort=DESC` ) and not based on the hostname.
    
    The option that says: **Launch a Network Load Balancer and add the new certificate from AWS Certificate Manager. For each Auto Scaling group, add and associate a corresponding target group. Create a listener with an HTTP Header condition to route the zone apex (`tutorialsdojo.com`) and the subdomains (`*.tutorialsdojo.com`) to the appropriate target groups** is incorrect because you have to use an Application Load Balancer in this scenario. You cannot create a listener with an HTTP Header condition using a Network Load Balancer. In addition, this will only route the traffic based on the HTTP request method of each request and not based on the hostname.
    

A financial company has an online trading platform hosted in AWS that is used by millions of investors worldwide. To comply with the strict security policies, the Network team has been tasked to closely monitor the traffic flows on all EC2 instances across different VPCs by performing deep packet inspection. The solution must check the headers and data of every incoming network packet and block the ones containing a malicious payload.

Which of the following should the team implement to fulfill this requirement?

- Set up VPC Traffic Mirroring to inspect all network traffic and block malicious packet payloads.
- Enable query logging in the Amazon Route 53 console and perform deep packet inspection using the Route 53 Resolver DNS Firewall.
- Use the VPC Reachability Analyzer to perform deep packet inspection of all EC2 instances for every incoming traffic.
- **Set up a separate Security VPC. Enable AWS Network Firewall to filter the network traffic and perform deep packet inspection.**
    
    AWS Network Firewall is a managed service that makes it easy to deploy essential network protections for all of your Amazon Virtual Private Clouds (VPCs). The service can be setup with just a few clicks and scales automatically with your network traffic, so you don’t have to worry about deploying and managing any infrastructure.
    
    AWS Network Firewall’s flexible rules engine lets you define firewall rules that give you fine-grained control over network traffic, such as blocking outbound Server Message Block (SMB) requests to prevent the spread of malicious activity. You can also import rules you’ve already written in common open-source rule formats as well as enable integrations with managed intelligence feeds sourced by AWS partners. AWS Network Firewall works together with AWS Firewall Manager so you can build policies based on AWS Network Firewall rules and then centrally apply those policies across your VPCs and accounts.
    
    ![](https://media.tutorialsdojo.com/aws-network-firewall-diagram.png)
    
    You can use AWS Network Firewall to monitor and protect your Amazon VPC traffic in a number of ways, including the following:
    
    – Pass traffic through only from known AWS service domains or IP address endpoints, such as Amazon S3.
    
    – Use custom lists of known bad domains to limit the types of domain names that your applications can access.
    
    – Perform deep packet inspection on traffic entering or leaving your VPC.
    
    – Use stateful protocol detection to filter protocols like HTTPS, independent of the port used.
    
    To perform inline deep packet inspection, customers typically use ISV firewall solutions in their AWS environment. These security solutions can be hosted in every VPC and account. However, this type of design doesn’t scale well, becomes difficult to manage, and can become costly. Centralizing your security solutions in a VPC and using intra-VPC connectivity helps solve these challenges. An alternative solution is to use the AWS Network Firewall service which is more scalable and easier to use than traditional ISV solutions.
    
    Hence, the correct answer is: **Set up a separate Security VPC. Enable AWS Network Firewall to filter the network traffic and perform deep packet inspection.**
    
    The option that says: **Use the VPC Reachability Analyzer to perform deep packet inspection of all EC2 instances for every incoming traffic** is incorrect because the VPC Reachability Analyzer simply checks the path between a source and destination by building a model of the network configuration, and then checking for reachability based on the configuration. It does not send packets, analyze the data plane or even perform deep packet inspection of the network traffic.
    
    The option that says: **Set up VPC Traffic Mirroring to inspect all network traffic and block malicious packet payloads** is incorrect because Traffic Mirroring is only an Amazon VPC feature that copies network traffic from an elastic network interface to a destination network. This particular feature is not capable of performing deep packet inspection, unlike the AWS Network Firewall service.
    
    The option that says: **Enable query logging in the Amazon Route 53 console and perform deep packet inspection using the Route 53 Resolver DNS Firewall** is incorrect because the query logging feature just logs the information about the public DNS queries that Route 53 receives. The Route 53 Resolver DNS Firewall on the other hand is only capable of filtering and regulating outbound DNS traffic for your virtual private cloud (VPC).
    

A company has an established VPN connection between its on-premises data center and Amazon VPC. An Internet-routable IP address is used as the endpoint for the IPsec tunnel that connects the customer gateway device and the virtual private gateway. The customer device is using NAT traversal (NAT-T). A firewall is also placed between the Internet and the customer gateway device to improve network security. Recently, a firewall configuration change affected the IPSec tunnel and eventually, brought the VPN connection down.

What should you do to properly investigate and resolve this issue? (Select THREE.)

- Allow TCP access over port 4500 in the firewall.
- **Ensure that the IP Protocol 50 or the Encapsulating Security Payload (ESP) is allowed in the firewall to enable the transmission of IPsec packets that contain the encrypted network traffic.**
- **Allow UDP access over port 4500 in the firewall.**
- Verify that UDP over port 50 is allowed in the firewall to enable the transmission of IPsec packets that contain the encrypted network traffic.
- **Ensure that UDP over port 500 is allowed in the firewall to enable the transmission of Internet key exchange (IKE) packets.**
- Verify that the IP Protocol 500 is allowed in the firewall to enable the transmission of Internet key exchange (IKE) packets.
- **Evaluation**
    
    A customer gateway device is a physical or software appliance on your side of a Site-to-Site VPN connection. You or your network administrator must configure the device to work with the Site-to-Site VPN connection.
    
    A VPN connection is composed of your network, the customer gateway, and the actual connection that goes to a virtual private gateway which is attached to your VPC. A single VPN connection has two tunnels that connect the customer gateway device and the virtual private gateway. If there’s a device failure within AWS, the VPN connection automatically fails over to the second tunnel so that the access isn’t interrupted. From time to time, AWS also performs routine maintenance on the virtual private gateway, which might briefly disable one of the two tunnels of your VPN connection. A VPN connection automatically fails over to the second tunnel while this maintenance is performed. When you configure your customer gateway device, it’s therefore important that you configure both tunnels.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-11_11-08-10-32f4a2dd608b80682a1dfcd54b913bd2.png)
    
    You must have an internet-routable IP address to use as the endpoint for the IPsec tunnels that connect your customer gateway device to the virtual private gateway. If a firewall is in place between the internet and your gateway, the rules in the following tables must be in place to establish the IPsec tunnels. The virtual private gateway addresses are in the configuration file.
    
    The UDP over port 500 must be allowed in the firewall to enable the transmission of Internet key exchange (IKE) packets. You should also ensure that the IP Protocol 50 or the Encapsulating Security Payload (ESP) is allowed in the firewall to enable the transmission of IPsec packets that contain the encrypted network traffic.
    
    If you are using NAT traversal (NAT-T) on your device, you must include rules that allow UDP access over port 4500. Check if your device is advertising NAT-T.
    
    Hence, the correct answers are:
    
    - **Ensure that UDP over port 500 is allowed in the firewall to enable the transmission of Internet key exchange (IKE) packets**
    - **Ensure that the IP Protocol 50 or the Encapsulating Security Payload (ESP) is allowed in the firewall to enable the transmission of IPsec packets that contain the encrypted network traffic.**
    - **Allow UDP access over port 4500 in the firewall**
    
    The option that says: **Verify that UDP over port 50 is allowed in the firewall to enable the transmission of IPsec packets that contain the encrypted network traffic** is incorrect because UDP over port 50 is not required. You have to ensure that UDP over port 500 is allowed in the firewall to enable the transmission of Internet key exchange (IKE) packets and not the IPsec packets.
    
    The option that says: **Verify that the IP Protocol 500 is allowed in the firewall to enable the transmission of Internet key exchange (IKE) packets** is incorrect because the IP Protocol 500 is not valid in this scenario. You must verify that the IP Protocol 50 or the Encapsulating Security Payload (ESP) is allowed in the firewall to enable the transmission of IPsec packets that contain the encrypted network traffic and not the Internet key exchange (IKE) packets.
    
    The option that says: **Allow TCP access over port 4500 in the firewall** is incorrect because TCP is actually not needed in a VPN Tunnel. You should allow UDP access over port 4500 in the firewall instead.
    

An application is running on Amazon ECS cluster with an Application Load Balancer in front to distribute the incoming requests to multiple ECS tasks. The X-Ray daemon has been installed to the ECS cluster to easily troubleshoot any application issues as well as monitor traffic from a list of suspected IP addresses that send high levels of spam or invalid requests. The Network Engineer must ensure that AWS X-Ray can read the client IP address of each request.

What should the Engineer do to properly set up the application architecture?

- Enable the Proxy Protocol v2 feature in the Application Load Balancer to preserve the client IP address. The AWS X-Ray will automatically fetch the client IP address from the `ipAddress`header of the request.
- Configure the Application Load Balancer to use path-based routing. AWS X-Ray will automatically fetch the client IP address from the `X-Forwarded-Host` header of the request.
- There is no way for AWS X-Ray to read the client IP address of each request. The Network Engineer must replace this with Amazon Inspector instead.
- **No further actions needed since the `X-Forwarded-For`, `X-Forwarded-Proto`, and `X-Forwarded-Port` headers are already included in the request. The Application Load Balancer honors the connection header from the incoming client request after proxying the response back to the client. AWS X-Ray will automatically fetch the client IP address from the `X-Forwarded-For` header of the request.**
    
    Application Load Balancers and Classic Load Balancers add X-Forwarded-For, X-Forwarded-Proto, and X-Forwarded-Port headers to the request. For front-end connections that use HTTP/2, the header names are in lowercase. Before the request is sent to the target using HTTP/1.1, the following header names are converted to mixed case: X-Forwarded-For, X-Forwarded-Proto, X-Forwarded-Port, Host, X-Amzn-Trace-Id, Upgrade, and Connection. All other header names are in lowercase.
    
    Application Load Balancers and Classic Load Balancers honor the connection header from the incoming client request after proxying the response back to the client.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-02_01-30-09-97ede22f938dab420dac7cd116b6c388.png)
    
    X-Ray gathers information from request and response headers, the code in your application, and metadata about the AWS resources on which it runs. You choose the data to collect by modifying your application configuration or code to instrument incoming requests, downstream requests, and AWS SDK clients.
    
    If a load balancer or other intermediary forwards a request to your application, X-Ray takes the client IP from the `X-Forwarded-For` header in the request instead of from the source IP in the IP packet. The client IP that is recorded for a forwarded request can be forged, so it should not be trusted.
    
    Hence, the correct answer is: **No further actions needed since the `X-Forwarded-For`, `X-Forwarded-Proto`, and `X-Forwarded-Port` headers are already included in the request. The Application Load Balancer honors the connection header from the incoming client request after proxying the response back to the client. AWS X-Ray will automatically fetch the client IP address from the `X-Forwarded-For`header of the request.**
    
    The option that says: **Configure the Application Load Balancer to use path-based routing. AWS X-Ray will automatically fetch the client IP address from the `X-Forwarded-Host` header of the request** is incorrect because the path-based routing feature of the ALB is primarily used to forward requests based on the URL path and not for preserving the client IP address. In addition, the `X-Forwarded-Host` header is only used in identifying the original host where the request originated from and not the IP address.
    
    The option that says: **Enable the Proxy Protocol v2 feature in the Application Load Balancer to preserve the client IP address. AWS X-Ray will automatically fetch the client IP address from the `ipAddress` header of the request ****is incorrect because using Proxy Protocol v2 is only applicable for Network Load Balancers and not for Application Load Balancers. Take note that there is no standard **`ipAddress`** header on the request. You can only get the client IP address from the `X-Forwarded-For` header of the request.
    
    The option that says: ****There is no way for AWS X-Ray to read the client IP address of each request. The Network Engineer must replace this with Amazon Inspector instead ****is incorrect because there is actually a way to read the client IP address from the request. Amazon Inspector is not warranted to be used here since it is simply a security assessment service that automatically assesses applications for exposure, vulnerabilities, and deviations from best practices.
    

A company has an application hosted in multiple AWS Regions. The Network team has been instructed to minimize latency and improve response times of its servers. When Route 53 receives a DNS query for the domain, it must determine which AWS Region will provide the user the lowest latency, and then automatically select the record for that region.

Which of the following configurations must the team use to fulfill this requirement?

- Use Geoproximity-based routing in Route 53 to provide the user with the lowest latency compared to Latency-based routing.
- Set up Weighted routing policy in Route 53. Create weighted records for your resources in multiple AWS Regions to provide the lowest latency to the users than Latency-based routing.
- Set up Geolocation routing policy in Route 53. Create geolocation records for your resources in every AWS Region to provide the lowest latency to the users.
- **Use Latency-based routing in Route 53 by creating latency records for your resources in multiple AWS Regions.**
    
    If your application is hosted in multiple AWS Regions, you can improve performance for your users by serving their requests from the AWS Region that provides the lowest latency. Data about the latency between users and your resources is based entirely on traffic between users and AWS data centers. If you aren’t using resources in an AWS Region, the actual latency between your users and your resources can vary significantly from AWS latency data. This is true even if your resources are located in the same city as an AWS Region.
    
    To use latency-based routing, you create latency records for your resources in multiple AWS Regions. When Route 53 receives a DNS query for your domain or subdomain (tutorialsdojo.com or portal.tutorialsdojo.com), it determines which AWS Regions you’ve created latency records for, determines which region gives the user the lowest latency, and then selects a latency record for that region. Route 53 responds with the value from the selected record, such as the IP address for a web server.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-17_00-24-22-aaab51dd1129b86478312598b2969a1a.png)
    
    The latency between hosts on the Internet can change over time as a result of changes in network connectivity and routing. Latency-based routing is based on latency measurements performed over a period of time, and the measurements reflect these changes. A request that is routed to the Oregon region this week might be routed to the Singapore region next week.
    
    Hence, the correct answer is: **Use Latency-based routing in Route 53 by creating latency records for your resources in multiple AWS Regions.**
    
    The option that says: **Set up Geolocation routing policy in Route 53. Create geolocation records for your resources in every AWS Region to provide the lowest latency to the users** is incorrect because this routing policy is primarily used when you want to route traffic based on the location of your users and not to provide the lowest latency. In addition, you don’t need to create a geolocation record for each and every AWS Region.
    
    The option that says: **Set up Weighted routing policy in Route 53. Create weighted records for your resources in multiple AWS Regions to provide the lowest latency to the users than Latency-based routing** is incorrect because this routing policy is mainly used to route traffic to multiple resources in proportions, or weights, that you specify. It doesn’t provide lower latency than the Latency-based routing.
    
    The option that says: **Use Geoproximity-based routing in Route 53 to provide the user with the lowest latency compared to Latency-based routing** is incorrect because this policy is not better than Latency-based routing. The common use case for Geoproximity-based routing is when you want to route traffic based on the location of your resources and, optionally, shift traffic from resources in one location to resources in another.
    

A company is setting up a central Virtual Private Cloud named VPC A with one subnet and two VPC peering connections. The first connection is between VPC A and VPC B (`pcx-aaaabbbb`) and the second is between VPC A and VPC C (`pcx-aaaacccc`). VPC B and VPC C both have one EC2 instance each with the same `10.0.0.10` IP address as shown in the diagram below:

![](https://td-portal-cdn.tutorialsdojo.com/wp-content/uploads/2020/06/IMG-QUESTION-ANS1.png)

The CIDR Blocks of each VPCs are as follows:

VPC A – `172.16.0.0/16`

VPC B – `10.0.0.0/16`

VPC C – `10.0.0.0/16`

VPC A should have two EC2 instances (E3 and E4) with custom private IP addresses `172.16.0.10` and `172.16.0.22` respectively. The E3 server in VPC A must be able to exclusively connect to E1 in VPC B while the E4 server in VPC A must be able to exclusively connect to the E2 in VPC C. Connections from the E3 server to E2 and from E4 server to E1 must be blocked.

How can the Network Administrator properly set up this architecture?

- Set up two new subnets in VPC A (`172.16.0.0/29` and `172.16.0.16/29`). Relaunch the E3 and E4 to these two subnets respectively with the appropriate custom private IP addresses. Create 2 route tables for VPC A, each with unique route entries for the two EC2 servers in VPC B and VPC C.
- Since VPC B and VPC C have overlapping CIDR blocks, establishing a VPC Peering connection for these three VPCs is not possible. Launch a Transit Gateway instead to interconnect all the VPCs.
- **Create two new subnets in VPC A (`172.16.0.0/28` and `172.16.0.16/28`). Launch the E3 and E4 to these two subnets respectively with the appropriate custom private IP addresses. Create 2 route tables for VPC A, each with unique route entries for the two EC2 servers in VPC B and VPC C.**
    
    A VPC peering connection is a networking connection between two VPCs that enables you to route traffic between them using private IPv4 addresses or IPv6 addresses. Instances in either VPC can communicate with each other as if they are within the same network. You can create a VPC peering connection between your own VPCs, or with a VPC in another AWS account. The VPCs can be in different regions (also known as an inter-region VPC peering connection).
    
    You can configure VPC peering connections so that your route tables have access to the entire CIDR block of the peer VPC. To create a VPC peering connection, first create a request to peer with another VPC. You can request a VPC peering connection with another VPC in your account or with a VPC in a different AWS account. For an inter-region VPC peering connection where the VPCs are in different regions, the request must be made from the region of the requester VPC. To activate the request, the owner of the accepter VPC must accept the request. For an inter-region VPC peering connection, the request must be accepted in the region of the accepter VPC.
    
    ![](https://media.tutorialsdojo.com/public/one-to-two-vpcs-instances-diagram.png)
    
    If you have a VPC peered with multiple VPCs that have overlapping or matching CIDR blocks, ensure that your route tables are configured to avoid sending response traffic from your VPC to the incorrect VPC. AWS currently does not support unicast reverse path forwarding in VPC peering connections that check the source IP of packets and route reply packets back to the source.
    
    For example, VPC A is peered with VPC B and VPC C. VPC B and VPC C have matching CIDR blocks, and their subnets have matching CIDR blocks. The route table for subnet B in VPC B points to the VPC peering connection `pcx-aaaabbbb` to access the VPC A subnet. The VPC A route table is configured to send `10.0.0.0/16` traffic to peering connection `pcx-aaaaccccc`.
    
    Alternatively, depending on your use case, you can create a route to a specific IP address in VPC B to ensure that traffic is routed back to the correct server (the route table uses longest prefix match to prioritize the routes).
    
    Hence, the correct answer is: **Create two new subnets in VPC A (`172.16.0.0/28` and `172.16.0.16/28`). Launch the E3 and E4 to these two subnets respectively with the appropriate custom private IP addresses. Create 2 route tables for VPC A, each with unique route entries for the two EC2 servers in VPC B and VPC C.**
    
    The option that says: **Set up two new subnets in VPC A (`172.16.0.0/29` and `172.16.0.16/29`). Relaunch the E3 and E4 to these two subnets respectively with the appropriate custom private IP addresses. Create 2 route tables for VPC A, each with unique route entries for the two EC2 servers in VPC B and VPC C** is incorrect because the allowed CIDR block size of an Amazon VPC is between a /28 netmask and /16 netmask only. Therefore, using a subnet of `172.16.0.0/29` is invalid.
    
    The option that says: **Launch two new subnets in VPC A (`172.16.0.0/27` and `172.16.0.16/27`). Launch the E3 and E4 to these two subnets respectively with the appropriate custom private IP addresses. Create 3 new route tables for VPC A, with unique route entries for the two EC2 servers in VPC B and VPC C**is incorrect because the expected private IP addresses of E3 and E4 are `172.16.0.10` and `172.16.0.22` respectively. This means that the CIDR block should be /28 to produce 16 hosts: `172.16.0.0` to `172.16.0.15` for the first subnet and`172.16.0.16` to `172.16.0.31` for the second subnet. A CIDR block of /27 has 32 hosts that range from `172.16.0.0` to `172.16.0.31` for the first subnet alone. Thus, the IP addressing setup of this option is wrong.
    
    The option that says: **Since VPC B and VPC C have overlapping CIDR blocks, establishing a VPC Peering connection for these three VPCs is not possible. Launch a Transit Gateway instead to interconnect all the VPCs** is incorrect because a central VPC can actually be peered between two or more VPCs that have overlapping CIDR blocks. What’s invalid is a VPC peering connection between two VPCs with matching or overlapping IPv4 CIDR blocks.
    
- Launch two new subnets in VPC A (`172.16.0.0/27` and `172.16.0.16/27`). Launch the E3 and E4 to these two subnets respectively with the appropriate custom private IP addresses. Create 3 new route tables for VPC A, with unique route entries for the two EC2 servers in VPC B and VPC C.

A company has a hybrid cloud architecture that connects its on-premises Microsoft Active Directory to its Amazon VPC. The company is launching an application that uses Amazon Elastic MapReduce with a fleet of On-Demand EC2 instances. Two AWS Managed Microsoft AD domain controllers as well as the DHCP options set of the VPC have been provisioned and properly configured. The Network Engineer must ensure that the requests destined for the Route 53 private hosted zone are sent to the VPC-provided DNS.

What should the Engineer implement in order to satisfy this requirement?

- Set up an Amazon Connect omnichannel connection to ensure that the requests destined for the Route 53 private hosted zone are sent to the VPC-provided DNS.
- **Set up a new conditional forwarder to the Amazon-provided DNS server.**
    
    **AWS Directory Service** lets you run Microsoft Active Directory (AD) as a managed service. AWS Directory Service for Microsoft Active Directory also referred to as AWS Managed Microsoft AD, is powered by Windows Server 2012 R2. When you select and launch this directory type, it is created as a highly available pair of domain controllers connected to your virtual private cloud (VPC). The domain controllers run in different Availability Zones in a region of your choice. Host monitoring and recovery, data replication, snapshots, and software updates are automatically configured and managed for you.
    
    With AWS Managed Microsoft AD, you can run directory-aware workloads in the AWS Cloud, including Microsoft SharePoint and custom .NET and SQL Server-based applications. You can also configure a trust relationship between AWS Managed Microsoft AD in the AWS Cloud and your existing on-premises Microsoft Active Directory, providing users and groups with access to resources in either domain using single sign-on (SSO).
    
    ![](https://media.tutorialsdojo.com/public/single-forest.png)
    
    You can follow the steps below to integrate your on-premises Microsoft Active Directory and your AWS resources:
    
    1. Connect your on-premises network to the VPC using AWS Direct Connect or a VPN connection, and verify that the new Windows Server instances can resolve the domain’s DNS name.
    2. Promote the new Windows Server instances in your VPC to domain controllers in your Active Directory domain.
    3. Configure your on-premises Active Directory Sites and Services to include sites and subnets that represent the Availability Zones within your VPC, and place the newly promoted domain controllers in their associated sites.
    4. Promote the Windows Server instances in the private subnets to domain controllers in your Active Directory domain.
    5. Ensure that instances can resolve names via AD DNS by statically assign AD DNS servers on Windows instances or setting the `domain-name-servers` field in new DHCP options set in your VPC to include your AWS-based domain controllers hosting Active Directory DNS.
    
    ![](https://media.tutorialsdojo.com/public/2020-05-21_02-19-44-4d5020a42a7cb2033cee9be1a4c492dd.png)
    
    By default, the Microsoft Active Directory-provided DNS doesn’t automatically forward requests to the VPC-provided DNS. You have to configure a DNS forwarder so that requests destined for the Route 53 private hosted zone are sent to the VPC-provided DNS. You can use the Windows DNS Server Tools feature to configure a DNS forwarder.
    
    Hence, the correct answer is: **Set up a new conditional forwarder to the Amazon-provided DNS server.**
    
    The option that says: **Configure a seamless EC2 Domain-Join in the AWS Managed Microsoft AD** is incorrect because the seamless EC2 Domain-Join process only allows you to share a directory between two AWS accounts and not for resolving hostnames in your internal, on-premises Active Directory domain.
    
    The option that says: **Create a new PTR record in the Route 53 private hosted zone that points to the on-premises Microsoft Active Directory** is incorrect because a PTR record in Route 53 simply maps an IP address to the corresponding domain name. It is not capable of connecting your Route 53 private hosted zone to the on-premises Microsoft Active Directory.
    
    The option that says: **Set up an Amazon Connect omnichannel connection to ensure that the requests destined for the Route 53 private hosted zone are sent to the VPC-provided DNS** is incorrect because Amazon Connect is just an easy-to-use omnichannel cloud contact center that helps companies provide superior customer service at a lower cost. This service is not suitable for integrating your on-premises Active Directory and AWS VPC.
    
- Create a new PTR record in the Route 53 private hosted zone that points to the on-premises Microsoft Active Directory.
- Configure a seamless EC2 Domain-Join in the AWS Managed Microsoft AD.

A multinational organization plans to adopt a hybrid cloud infrastructure that requires a dedicated connection between its on-premises data center and virtual private cloud (VPC) in AWS. The connection must allow the cloud-based applications hosted in EC2 instances to fetch data from the organization’s on-premises file servers with a more consistent network experience than Internet-based connections.

Which of the following options should the Network team implement to satisfy this requirement?

- Set up a VPC Peering connection between the VPC and the on-premises data center.
- Set up an AWS VPN CloudHub between the VPC and the on-premises data center.
- Set up an Amazon Connect omnichannel connection between the VPC and the on-premises data center.
- **Set up an AWS Direct Connect connection between the VPC and the on-premises data center.**
    
    AWS Direct Connect is a cloud service solution that makes it easy to establish a dedicated network connection from your premises to AWS. Using AWS Direct Connect, you can establish private connectivity between AWS and your data center, office, or colocation environment, which in many cases, can reduce your network costs, increase bandwidth throughput, and provide a more consistent network experience than Internet-based connections.
    
    ![](https://media.tutorialsdojo.com/public/Product-Page-Diagram_Direct-Connect.0166400894ac5f9c921c3f7a346f55d8c42f492c.png)
    
    Hence, the correct answer is: **Set up an AWS Direct Connect connection between the VPC and the on-premises data center**
    
    The option that says: **Set up a VPC Peering connection between the VPC and the on-premises data center** is incorrect because VPC Peering is an Internet-based connection that is primarily used to connect two or more VPCs. You can’t set up a connection between your VPC and your on-premises data center using VPC Peering.
    
    The option that says: **Set up an AWS VPN CloudHub between the VPC and the on-premises data center** is incorrect because a VPN is an Internet-based connection, unlike Direct Connect, which provides a dedicated connection. An Internet-based connection means that the traffic from the VPC and to the on-premises network traverses the public Internet, which is why it is slow. You should use Direct Connect instead.
    
    The option that says: **Set up an Amazon Connect omnichannel connection between the VPC and the on-premises data center** is incorrect because Amazon Connect is just an easy-to-use omnichannel cloud contact center that helps companies provide superior customer service at a lower cost. This service is not suitable for integrating your VPC and on-premises network.
    

A company has two AWS Direct Connect (DX) connections that enable its on-premises servers to fetch data from their public Amazon S3 buckets at a higher bandwidth. Each DX connection has a public virtual interface with a private Autonomous System Number (ASN). The Network Administrator configured an Active/Passive routing between the DX connections to access the public Amazon S3 endpoints.

Which of the following statements are true regarding this setup? (Select THREE.)

- **The customer gateway is advertising the longer prefix (e.g. `/25`) on the primary connection and the shorter prefix on the secondary connection (e.g. `/24`).**
- This type of connection allows Autonomous System (AS) prepending.
- The customer gateway is advertising the shorter prefix (e.g. `/24`) on the primary connection and the longer prefix on the secondary connection (e.g. `/25`).
- **In this routing configuration, one connection handles traffic and the other is on standby. If the active connection becomes unavailable, all traffic is routed through the passive connection.**
- In this routing configuration, traffic is load-shared between interfaces based on flow. If one connection becomes unavailable, all traffic is routed through the other connection.
- **Autonomous System (AS) prepending does not work in this connection.**
- **Evaluation**
    
    When you use AWS Direct Connect to transport production workloads to and from AWS services, it’s a best practice to create two DX connections through different data centers or providers. You have two options on how to configure your connections:
    
    - **Active/Active** – Traffic is load-shared between interfaces based on flow. If one connection becomes unavailable, all traffic is routed through the other connection.
    - **Active/Passive** – One connection handles traffic, and the other is on standby. If the active connection becomes unavailable, all traffic is routed through the passive connection.
    
    !https://media.tutorialsdojo.com/aws-direct-connect-redundant-connection.jpg
    
    When you configure your public virtual interfaces, you can use one of the following types of autonomous system numbers (ASNs):
    
    - A **public ASN** that you own
    - A **private ASN** in the 64512-65535 range
    
    Take note that Autonomous System (AS) prepending doesn’t work if you use a private ASN for a public virtual interface. The following are the things you have to consider when establishing an Active/Passive connection:
    
    If you’re using a **public ASN**:
    
    - Confirm that your customer gateway is advertising the same prefix (public IP or network that you own) on both Border Gateway Protocol (BGP) sessions.
    - You can use the **BGP AS_Path** attribute such that the prefixes advertised from the secondary connection are prepended with the customer gateway ASN twice. For example, if your customer gateway uses **ASN 123**, it can advertise the prefix on the secondary connection with **AS_Path** set to **123 123**.
    - You can use the **BGP Local Preference** attribute such that the customer gateway accepts routes from the primary connection as **200** and the secondary connection as **100**. A higher Local Preference value is preferred, and the default is 100.
    - The primary connection is considered the primary path. In the event of a failure, traffic is shifted to the secondary connection as a secondary path.
    
    If you’re using a **private ASN**:
    
    - Confirm that your customer gateway is advertising the longer prefix on your primary connection. For example, if you’re advertising prefix X.X.X.0/24, then your customer gateway can advertise two prefixes (X.X.X.0/25 and X.X.X.128/25) on your primary connection and prefix X.X.X.0/24 on your secondary connection.
    - If both interfaces are in the UP state, and the longer prefix is advertised on your primary connection, then traffic is sent to your customer gateway through the primary connection. In the event of a failure, traffic is shifted and sent to the secondary connection.
    
    Hence, the correct answers are:
    
    - **In this routing configuration, one connection handles traffic and the other is on standby. If the active connection becomes unavailable, all traffic is routed through the passive connection.**
    - **Autonomous System (AS) prepending does not work in this connection.**
    - **The customer gateway is advertising the longer prefix (e.g. `/25`) on the primary connection and the shorter prefix on the secondary connection (e.g. `/24`).**
    
    The option that says: **In this routing configuration, traffic is load-shared between interfaces based on flow. If one connection becomes unavailable, all traffic is routed through the other connection** is incorrect because this is the behavior of an Active/Active connection.
    
    The option that says: **This type of connection allows Autonomous System (AS) prepending** is incorrect because the connection is using a private ASN therefore, the autonomous system (AS) prepending does not work.
    
    The option that says: **The customer gateway is advertising the shorter prefix (e.g. `/24`) on the primary connection and the longer prefix on the secondary connection (e.g. `/25`)** is incorrect because this should be the other way around. The customer gateway is advertising the longer prefix for the primary connection and a shorter prefix for the secondary connection.
    

A multinational company has multiple departments, each with their own VPC. Some VPCs are located within the same AWS account while others are in a different account. A Network Administrator has been tasked to peer together the VPCs to enable the departments to access each others’ cloud resources.

Which of the following is a valid VPC peering configuration that the Administrator can implement?

- **One VPC peered with two VPCs using longest prefix match**
    
    A VPC peering connection is a one-to-one relationship between two VPCs. You can create multiple VPC peering connections for each VPC that you own, but transitive peering relationships are not supported. You do not have any peering relationship with VPCs that your VPC is not directly peered with.
    
    ![](https://media.tutorialsdojo.com/public/one-to-two-vpcs-lpm-diagram.png)
    
    You can configure a VPC peering connection to provide access to a part of the CIDR block, a specific CIDR block (if the VPC has multiple CIDR blocks), or a specific instance within the peer VPC. Below is a list of valid VPC Peering configurations that you can implement:
    
    1. Two VPCs peered to two subnets in one VPC
    2. Two VPCs peered to a specific CIDR block in one VPC
    3. One VPC peered to specific subnets in two VPCs
    4. Instances in one VPC peered to instances in two VPCs
    5. One VPC peered with two VPCs using the longest prefix match
    6. Multiple VPC configurations
    
    Every route table contains a local route for communication within the VPC. This route is added by default to all route tables. If your VPC has more than one IPv4 CIDR block, your route tables contain a local route for each IPv4 CIDR block. If you’ve associated an IPv6 CIDR block with your VPC, your route tables contain a local route for the IPv6 CIDR block. You cannot modify or delete these routes in a subnet route table or in the main route table. If your route table has multiple routes, AWS uses the most specific route that matches the traffic (longest prefix match) to determine how to route the traffic.
    
    The following VPC peering connection configurations are not supported:
    
    1. Overlapping CIDR Blocks
    2. Transitive Peering
    3. Edge to Edge Routing Through a Gateway or Private Connection
    
    Hence, the correct answer is: **One VPC peered with two VPCs using longest prefix match.**
    
    The options that say: **A VPC peering connection between two VPCs with matching or overlapping IPv4 CIDR blocks**, **Transitive Peering**, **and Edge to edge routing via a gateway or private connection** are all incorrect because these VPC peering connection configurations are invalid.
    
- Transitive Peering
- A VPC peering connection between two VPCs with matching or overlapping IPv4 CIDR blocks
- Edge to edge routing via a gateway or private connection

An online trade finance system is hosted in two Dedicated EC2 Hosts that are behind an Application Load Balancer. A Network Engineer is tasked to implement a web filtering solution that automatically blocks web requests from a list of blacklisted countries issued by the Financial Action Task Force (FATF).

What should the Engineer implement to satisfy this requirement?

- Use AWS WAF with a rule that blocks an IP Set which contains all IP addresses associated with the blacklisted countries.
- Use Amazon GuardDuty to block web requests from a list of blacklisted countries.
- **Use AWS WAF with a geo match condition.**
    
    If you want to allow or block web requests based on the country that the requests originate from, create one or more geo match conditions. A geo match condition lists countries that your requests originate from. Later in the process, when you create a web ACL, you specify whether to allow or block requests from those countries.
    
    You can use geo match conditions with other AWS WAF Classic conditions or rules to build sophisticated filtering. For example, if you want to block certain countries but still allow specific IP addresses from that country, you could create a rule containing a geo match condition and an IP match condition. Configure the rule to block requests that originate from that country and do not match the approved IP addresses. As another example, if you want to prioritize resources for users in a particular country, you could include a geo match condition in two different rate-based rules. Set a higher rate limit for users in the preferred country and set a lower rate limit for all other users.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-12_09-57-39-e1cea07f73ecc391824a49e88dda98c1.png)
    
    If you are using the CloudFront geo restriction feature to block a country from accessing your content, any request from that country is blocked and is not forwarded to AWS WAF Classic. So if you want to allow or block requests based on geography plus other AWS WAF Classic conditions, you should *not* use the CloudFront geo restriction feature. Instead, you should use an AWS WAF Classic geo match condition.
    
    Hence, the correct answer is: **Use AWS WAF with a geo match condition.**
    
    The option that says: **Use AWS WAF with a rule that blocks an IP Set which contains all IP addresses associated with the blacklisted countries** is incorrect because this is not an appropriate solution to block traffic from specific countries. Take note that an IP address is not assigned to a specific country but to a specific telecommunication provider. You have to use a geo match condition instead.
    
    The option that says: **Set up a geo match condition in the Application Load Balancer that blocks requests from the blacklisted countries** is incorrect because you can’t configure a geo match condition in an Application Load Balancer. You have to use AWS WAF in this scenario.
    
    The option that says: **Use Amazon GuardDuty to block web requests from a list of blacklisted countries** is incorrect because Amazon GuardDuty is just a threat detection service that continuously monitors your AWS accounts and workloads for malicious activity and unauthorized behavior. It is not capable of blocking web requests from specific countries by itself.
    
- Set up a geo match condition in the Application Load Balancer that blocks requests from the blacklisted countries.

An organization is currently using an AWS Client VPN connection to securely access its AWS resources from its on-premises network using OpenVPN-based clients. To achieve a consistent network performance, the organization instructed its Network team to establish a 10 Gbps AWS Direct Connect connection. The Network team contacted a telecommunications provider, which is part of the AWS Direct Connect Partner program, to establish the required network circuits between a Direct Connect location and the company’s data center. The team needs to provide information on how to cross-connect (e.g., Rack, Patch Panel and Port Number) to the provider.

Which of the following is the proper way of getting this information?

- Contact AWS Support by phone or by email and request for the Letter of Authorization and Connecting Facility Assignment (LOA-CFA) document.
- Contact the designated Technical Account Manager (TAM) of the AWS account and request for the Letter of Authorization and Connecting Facility Assignment (LOA-CFA) document.
- **In the AWS Management Console, create a new AWS Direct Connect connection and configure it to go through an AWS Direct Connect partner. After you request for the connection, wait for AWS to send you the Letter of Authorization and Connecting Facility Assignment (LOA-CFA) that contains the requested information then send it to the telecommunications provider.**
    
    **AWS Direct Connect** links your internal network to an AWS Direct Connect location over a standard Ethernet fiber-optic cable. One end of the cable is connected to your router, and the other to an AWS Direct Connect router. With this connection, you can create virtual interfaces directly to public AWS services (for example, to Amazon S3) or to Amazon VPC, bypassing internet service providers in your network path.
    
    To create an AWS Direct Connect dedicated connection, you need the following information:
    
    - AWS Direct Connect location
    - Port speed
    
    You have to work with a partner in the AWS Direct Connect Partner Program to help you establish network circuits between an AWS Direct Connect location and your data center, office, or colocation environment. They can also help provide colocation space within the same facility as the location
    
    ![](https://media.tutorialsdojo.com/public/2020-05-29_08-37-29-5395c4ac0c2e0e7253d3fd59c8c1b009.png)
    
    For the port speed, the possible values are 1Gbps and 10Gbps. You cannot change the port speed after you create the connection request. To change the port speed, you must create and configure a new connection.
    
    After you request for the connection, AWS makes a Letter of Authorization and Connecting Facility Assignment (LOA-CFA) available to you to download or emails you with a request for more information. If you receive a request for more information, you must respond within 7 days, or the connection is deleted. The LOA-CFA is the authorization to connect to AWS and is required by your network provider to order a cross-connect for you. If you do not have equipment in the AWS Direct Connect location, you will not be able to order a cross-connect.
    
    Hence, the correct answer is: **In the AWS Management Console, create a new AWS Direct Connect connection and configure it to go through an AWS Direct Connect partner. After you request for the connection, wait for AWS to send you the Letter of Authorization and Connecting Facility Assignment (LOA-CFA) that contains the requested information then send it to the telecommunications provider.**
    
    The option that says: **Contact the designated Technical Account Manager (TAM) of the AWS account and request for the Letter of Authorization and Connecting Facility Assignment (LOA-CFA) document** is incorrect because the Technical Account Manager (TAM) is simply your designated technical point of contact who provides advocacy and guidance to help plan and build solutions using best practices, coordinate access to subject matter experts and product teams, and proactively keep your AWS environment operationally healthy. This person is not capable of producing the LOA-CFA that you require. You have to create a new AWS Direct Connect connection in the AWS Management Console in order to get the LOA-CFA.
    
    The option that says: **Go to the AWS Direct Connect console and directly download the Letter of Authorization and Connecting Facility Assignment (LOA-CFA) which contains the requested information. Send the document to the telecommunications provider to complete the cross-connect process** is incorrect because the LOA-CFA document is not easily downloadable from the AWS Direct Connect console. You have to create a new Direct Connect connection first before this document could be generated by AWS.
    
    The option that says: **Contact AWS Support by phone or by email and request for the Letter of Authorization and Connecting Facility Assignment (LOA-CFA) document** is incorrect because the AWS Support team is not capable of generating the LOA-CFA document.
    
- Go to the AWS Direct Connect console and directly download the Letter of Authorization and Connecting Facility Assignment (LOA-CFA) which contains the requested information. Send the document to the telecommunications provider to complete the cross connect process.

A company has several EC2 instances in its VPC that are publicly accessible from the Internet. A Network Engineer issued a ping command from his home computer with an IP address of `203.0.113.12` to an EC2 instance with a private IP address of `172.31.16.139` and a public address of `52.181.132.48`. However, there was no ping response. The VPC Flow Logs have the following records:

```
2 123456789010 eni-1235b8ca123456789 203.0.113.12 172.31.16.139 0 0 1 4 336 1432917027 1432917142 ACCEPT OK
2 123456789010 eni-1235b8ca123456789 172.31.16.139 203.0.113.12 0 0 1 4 336 1432917094 1432917142 REJECT OK

```

What is the MOST likely cause of this issue?

- The Network ACL's inbound rules do not allow ICMP traffic.
- The security group's inbound rules do not allow ICMP traffic.
- **The Network ACL's outbound rules do not allow ICMP traffic.**
    
    If you’re using flow logs to diagnose overly restrictive or permissive security group rules or network ACL rules, be aware of the statefulness of these resources. Security groups are stateful — this means that responses to allowed traffic are also allowed, even if the rules in your security group do not permit it. Conversely, network ACLs are stateless, therefore, responses to allowed traffic are subject to network ACL rules.
    
    For example, you use the `ping` command from your home computer (IP address is `203.0.113.12`) to your instance (the network interface’s private IP address is `172.31.16.139`). Your security group’s inbound rules allow ICMP traffic but the outbound rules do not allow ICMP traffic. Because security groups are stateful, the response ping from your instance is allowed. Your network ACL permits inbound ICMP traffic but does not permit outbound ICMP traffic. Because network ACLs are stateless, the response ping is dropped and does not reach your home computer. In a default flow log, this is displayed as two flow log records:
    
    - An `ACCEPT` record for the originating ping that was allowed by both the network ACL and the security group and therefore was allowed to reach your instance.
    - A `REJECT` record for the response ping that the network ACL denied.
    
    ```
    2 123456789010 eni-1235b8ca123456789 203.0.113.12 172.31.16.139 0 0 1 4 336 1432917027 1432917142 ACCEPT OK
    2 123456789010 eni-1235b8ca123456789 172.31.16.139 203.0.113.12 0 0 1 4 336 1432917094 1432917142 REJECT OK
    ```
    
    If your network ACL permits outbound ICMP traffic, the flow log displays two `ACCEPT` records (one for the originating ping and one for the response ping). If your security group denies inbound ICMP traffic, the flow log displays a single `REJECT` record because the traffic was not permitted to reach your instance.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-07_12-43-08-47bdee46894025a7eee973ba543000f0.png)
    
    In this scenario, the ping request from the Network Engineer’s home computer to the EC2 instance failed, and two records from VPC Flow Logs were provided. The logs basically mean that the first one is the record of the traffic flow that goes from the home computer to the EC2 instance, and the latter is the record of the traffic flow that goes back from the EC2 instance back to the home computer.
    
    Apparently, the first one is an `ACCEPT` record and the second one is a `REJECT` record, which means that the incoming traffic was successfully accepted by your EC2 instance, but the response, or the outgoing traffic, was rejected by the network ACL. Take note that security groups are stateful, so once the traffic is allowed by its inbound rules, the traffic will egress even if its outbound rules have a restricted rule. This is the reason why the most likely cause of this issue is the Network ACL’s restrictive outbound rule that blocks the ICMP traffic.
    
    Hence, the correct answer is: **The Network ACL’s outbound rules do not allow ICMP traffic.**
    
    The option that says: **The security group’s inbound rules do not allow ICMP traffic** is incorrect because there is already an ACCEPTED record in the VPC Flow Logs. This means that the ICMP traffic has been allowed to enter and exit from the instance. If your security group denies inbound ICMP traffic, the flow log displays a single `REJECT` record because the traffic was not permitted to reach your instance.
    
    The option that says: **The security group’s outbound rules do not allow ICMP traffic** is incorrect because security groups are stateful. Therefore, the response ping from the EC2 instance will still be allowed without explicitly allowing outgoing ICMP traffic. Take note that there is already an ACCEPTED record in the VPC Flow Logs, which signifies that the traffic has been allowed to enter and exit from the instance.
    
    The option that says: **The Network ACL’s inbound rules do not allow ICMP traffic** is incorrect because the first flow log clearly shows that the incoming traffic was successfully accepted by the Network ACL and the security group of the EC2 instance, which is why the issue lies on the outgoing traffic. The second flow log shows that the response or the outgoing traffic was rejected by the Network ACL.
    
- The security group's outbound rules do not allow ICMP traffic.

An enterprise is extending its on-premises data storage systems using AWS. A Network Engineer established an AWS Direct Connect connection with a Public Virtual Interface (VIF) to the on-premises network to allow low latency access to Amazon S3. The Engineer must ensure that the network connection is properly secured.

Which of the following is a valid security concern about this network architecture?

- The prefixes are always advertised to all public AWS Regions so all Direct Connect customers in the same or different region can access your router as long as they also have a Public VIF. You cannot apply BGP community tags on the public prefixes.
- AWS Direct Connect advertises all public prefixes with the well-known NO_EXPORT BGP community tag to help control the scope (regional or global) and route preference of traffic. However, the NO_EXPORT BGP community tag is only supported for private virtual interfaces and transit virtual interfaces.
- **Prefixes are advertised to all public AWS Regions (global) by default. The Network Engineer must add a BGP community tag to control the scope and route preference of the traffic on public virtual interfaces.**
    
    **AWS Direct Connect** links your internal network to an AWS Direct Connect location over a standard Ethernet fiber-optic cable. One end of the cable is connected to your router, and the other to an AWS Direct Connect router. With this connection, you can create *virtual interfaces* directly to public AWS services (for example, to Amazon S3) or to Amazon VPC, bypassing internet service providers in your network path. An AWS Direct Connect location provides access to AWS in the Region with which it is associated. You can use a single connection in a public Region or AWS GovCloud (US) to access public AWS services in all other public Regions.
    
    ![](https://media.tutorialsdojo.com/public/direct-connect-overview.png)
    
    AWS Direct Connect applies inbound (to your on-premises data center) and outbound (from your AWS Region) routing policies for a public AWS Direct Connect connection. You can also use Border Gateway Protocol (BGP) community tags on advertised Amazon routes and apply BGP community tags on the routes you advertise to Amazon. You can use the NO_EXPORT BGP community tag to help control the scope (Regional or global) and route preference of traffic on public virtual interfaces. If you do not apply any community tags, prefixes are advertised to all public AWS Regions (global) by default.
    
    In Direct Connect, it’s not possible to directly access an S3 bucket through a private virtual interface (VIF) using a Gateway VPC endpoint. Take note that the on-premises traffic can’t traverse the Gateway VPC endpoint. You have to use an Interface VPC endpoint instead if you have a private virtual interface in place.
    
    Hence, the correct answer is: **Prefixes are advertised to all public AWS Regions (global) by default. The Network Engineer must add a BGP community tag to control the scope and route preference of the traffic on public virtual interfaces.**
    
    The option that says: **AWS Direct Connect advertises all public prefixes with the well-known NO_EXPORT BGP community tag to help control the scope (regional or global) and route preference of traffic. However, the NO_EXPORT BGP community tag is only supported for private virtual interfaces and transit virtual interfaces** is incorrect because the NO_EXPORT BGP community tag is also supported for public virtual interfaces.
    
    The option that says: **The prefixes are always advertised to all public AWS Regions so all Direct Connect customers in the same, or different, region can access your router as long as they also have a Public VIF. You cannot apply BGP community tags on the public prefixes** is incorrect because you can actually use Border Gateway Protocol (BGP) community tags on advertised Amazon routes as well as the routes you advertise to Amazon. You can use the NO_EXPORT BGP community tag to help control the scope (Regional or global) and route preference of traffic on public virtual interfaces.
    
    The option that says: **It’s not possible to directly access an S3 bucket through a public virtual interface (VIF) using Direct Connect. You must have a pre-configured VPC endpoint for Amazon S3** is incorrect because it is actually possible to access an S3 bucket through a public virtual interface. This configuration doesn’t require an Amazon Virtual Private Cloud (Amazon VPC) endpoint for Amazon S3 because the on-premises traffic can’t traverse the Gateway VPC endpoint.
    
- It's not possible to directly access an S3 bucket through a public virtual interface (VIF) using Direct Connect. You must have a pre-configured VPC endpoint for Amazon S3.

A company plans to launch an enterprise application on an Amazon EKS cluster and configured with Horizontal Pod Autoscaler on a default VPC. To distribute incoming traffic among the Kubernetes pods, a Network Load Balancer integrated with AWS Load Balancer Controller will be launched.

The network engineer needs to design a solution to enable the security team to inspect traffic entering and exiting the default VPC. This VPC is also attached to an AWS Transit Gateway with multiple attachments to other VPCs in the company’s AWS account, which will access the enterprise application.

Which among the following satisfies the requirements in the MOST operationally efficient way?

- Enable Traffic Mirroring on the Network Load Balancer and forward traffic to the Amazon EKS cluster. Set up a traffic mirror filter to inspect the ingress and egress of data that traverses your Amazon VPC.
- **Set up a firewall using the AWS Network Firewall service at the VPC level. Launch custom rule groups for inspecting ingress and egress traffic in the Network Firewall. Modify the necessary VPC route tables.**
    
    AWS Network Firewall is a stateful, managed network firewall and intrusion detection and prevention service for your virtual private cloud (VPC). With Network Firewall, you can filter traffic at the perimeter of your VPC. This includes traffic going to and coming from an internet gateway, NAT gateway, or over VPN or AWS Direct Connect. Network Firewall uses Suricata — an open-source intrusion prevention system (IPS) for stateful inspection.
    
    The diagram below shows an AWS Network firewall deployed in a single availability zone and traffic flow for a workload in a public subnet:
    
    ![](https://media.tutorialsdojo.com/aws-network-firewall-single-az-deployment-diagram.jpg)
    
    You can use Network Firewall to monitor and protect your Amazon VPC traffic in a number of ways, including the following:
    
    – Pass traffic through only from known AWS service domains or IP address endpoints, such as Amazon S3.
    
    – Use custom lists of known bad domains to limit the types of domain names that your applications can access.
    
    – Perform deep packet inspection on traffic entering or leaving your VPC.
    
    – Use stateful protocol detection to filter protocols like HTTPS, independent of the port used.
    
    Therefore, the correct answer is: **Set up a firewall using the AWS Network Firewall service at the VPC level. Launch custom rule groups for inspecting ingress and egress traffic in the Network Firewall. Modify the necessary VPC route tables.**
    
    The option that says: **Use the Network Access Analyzer service on the application’s VPC for inspecting ingress and egress traffic. Create a new Network Access Scope to filter and analyze all incoming and outgoing requests** is incorrect. Network Access Analyzer is a feature of VPC that reports on unintended access to your AWS resources based on the security and compliance that you set. This service is not capable of performing deep packet inspection on traffic entering or leaving your VPC, unlike AWS Network Firewall.
    
    The option that says: **Enable the appliance mode in the Transit Gateway and launch a new Gateway Load Balancer (GWLB). Associate the GWLB to the Transit Gateway and route the traffic to the Reachability Analyzer service, which will inspect both the ingress and egress of data in the default VPC** is incorrect because enabling the appliance mode in Transit Gateway is only applicable if you have a security appliance hosted in your shared services VPC. All traffic that’s routed between transit gateway attachments is first inspected by the appliance in the shared services VPC. This solution didn’t mention that you have a security appliance deployed. Moreover, using the Reachability Analyzer service for inspecting the incoming and outgoing traffic is wrong as this service is not capable of doing this task. The Reachability Analyzer is simply a configuration analysis tool that enables you to perform connectivity testing between a source resource and a destination resource in your virtual private clouds (VPCs).
    
    The option that says: **Enable Traffic Mirroring on the Network Load Balancer and forward traffic to the Amazon EKS cluster. Set up a traffic mirror filter to inspect the ingress and egress of data that traverses your Amazon VPC** is incorrect. Traffic Mirroring is simply an Amazon VPC feature that you can use to copy network traffic from an elastic network interface. Traffic Mirroring filters can’t inspect the actual packet of incoming and outgoing traffic. You have to use the AWS Network Firewall service instead.
    
- Use the Network Access Analyzer service on the application’s VPC for inspecting ingress and egress traffic. Create a new Network Access Scope to filter and analyze all incoming and outgoing requests.
- Enable the appliance mode in the Transit Gateway and launch a new Gateway Load Balancer (GWLB). Associate the GWLB to the Transit Gateway and route the traffic to the Reachability Analyzer service, which will inspect both the ingress and egress of data in the default VPC.

A financial application is hosted in several EC2 instances behind an Application Load Balancer that only accepts HTTP or HTTPS traffic. A recent misconfiguration incident occurred when a Network team member incorrectly replaced the existing security group with an unused one that allows all inbound traffic. One of the measures to prevent this from happening again is to track unassociated security groups automatically.

Which of the following is the MOST suitable solution that the team should implement?

- Install a CloudWatch Agent in each EC2 instance. Configure the agent to send an email notification if a security group is not attached to the EC2 instance.
- **Use AWS Config and set up the EC2_SECURITY_GROUP_ATTACHED_TO_ENI managed rule to track the security group changes.**
    
    **AWS Config** provides AWS managed rules, which are predefined, customizable rules that AWS Config uses to evaluate whether your AWS resources comply with common best practices. For example, you could use a managed rule to quickly start assessing whether your Amazon Elastic Block Store (Amazon EBS) volumes are encrypted or whether specific tags are applied to your resources. You can set up and activate these rules without writing the code to create an AWS Lambda function, which is required if you want to create custom rules.
    
    The AWS Config console guides you through the process of configuring and activating a managed rule. You can also use the AWS Command Line Interface or AWS Config API to pass the JSON code that defines your configuration of a managed rule.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-19_05-56-34-4ece9af3778821b4f3ef0d511c1f0809.png)
    
    You can customize the behavior of a managed rule to suit your needs. For example, you can define the rule’s scope to constrain which resources trigger an evaluation for the rule, such as EC2 instances or volumes. You can customize the rule’s parameters to define attributes that your resources must have to comply with the rule. For example, you can customize a parameter to specify that your security group should block incoming traffic to a specific port number.
    
    After you activate a rule, AWS Config compares your resources to the conditions of the rule. After this initial evaluation, AWS Config continues to run evaluations each time one is triggered.
    
    In AWS Config, you can use the EC2_SECURITY_GROUP_ATTACHED_TO_ENI Managed Rule to automatically check if your security groups are properly attached to Amazon Elastic Compute Cloud (Amazon EC2) instances or to an elastic network interface. The rule returns NON_COMPLIANT if the security group is not associated with an Amazon EC2 instance or an elastic network interface.
    
    Hence, the correct answer is: **Use AWS Config and set up the EC2_SECURITY_GROUP_ATTACHED_TO_ENI managed rule to track the security group changes.**
    
    The option that says: **Install a CloudWatch Agent in each EC2 instance. Configure the agent to send an email notification if a security group is not attached to the EC2 instance** is incorrect because a CloudWatch agent is only capable of collecting custom and system-level metrics from EC2 instances. This type of agent cannot detect security group changes nor send an email notification.
    
    The option that says: **Set up Amazon Macie to track all security group changes in the VPC** is incorrect because Amazon Macie is a just fully managed data security and data privacy service that uses machine learning and pattern matching to discover and protect your sensitive data in AWS. This is primarily used in checking the data in your Amazon S3 buckets and not for monitoring the security group changes of your EC2 instances.
    
    The option that says: **Configure Systems Manager State Manager to identify and track all changes in the security group association of the EC2 instances** is incorrect because AWS Systems Manager State Manager is simply a secure and scalable configuration management service that automates the process of keeping your Amazon EC2 and hybrid infrastructure in a state that you define. This service is not capable of monitoring the security group association of your EC2 instances.
    
- Configure Systems Manager State Manager to identify and track all changes in the security group association of the EC2 instances.
- Set up Amazon Macie to track all security group changes in the VPC.

A distributed eCommerce system has various modules hosted in several Auto Scaling groups of EC2 instances placed in different AWS Regions. Each Auto Scaling group is behind an Application Load Balancer (ALB), and all ALBs are connected to an AWS Global Accelerator. The Network team plans to bring a portion of their publicly routable IPv4 address range from the corporate on-premises network that will be used in their AWS Organization via Bring Your Own IP (BYOIP).

A `cloud-init` must be configured to run at instance launch when the Auto Scaling group scales out. This should allocate a secondary Elastic Network Interface (ENI), which uses an IPv4 address from the BYOIP pool, and not the IP address provided by the internal Amazon VPC DHCP servers.

To distribute the traffic securely, all ALBs must be configured with an HTTPS listener and path-based routing. The existing cipher and encryption protocol of the ALBs must also be replaced. The BYOIP public IP address space must be onboarded once and then be distributed and shared across AWS Regions and AWS accounts.

Which of the following should be implemented to rectify the security vulnerability and minimize management overhead?

- Use AWS Shield Advanced to block the compromised cipher and encryption protocol in all of the ALBs. Configure the AWS Resource Access Manager (AWS RAM) to enable cross-region and cross-account sharing of the Bring Your Own IP (BYOIP) addresses.
- **Update the security policy of the ALB to replace the compromised cipher and encryption protocol. Use the Amazon VPC IP Address Manager (IPAM) to enable cross-region and cross-account sharing of the Bring Your Own IP (BYOIP) addresses.**
    
    Elastic Load Balancing uses a Secure Socket Layer (SSL) negotiation configuration, known as a security policy, to negotiate SSL connections between a client and the load balancer.
    
    A security policy is a combination of protocols and ciphers. The protocol establishes a secure connection between a client and a server and ensures that all data passed between the client and your load balancer is private. A cipher is an encryption algorithm that uses encryption keys to create a coded message. Protocols use several ciphers to encrypt data over the Internet.
    
    ![](https://media.tutorialsdojo.com/application-load-balancer-security-policy-ans-c01.png)
    
    During the connection negotiation process, the client and the load balancer present a list of ciphers and protocols they each support in order of preference. By default, the first cipher on the server’s list that matches any one of the client’s ciphers is selected for the secure connection.
    
    Application Load Balancers do not support SSL renegotiation for client or target connections. When you create a TLS listener, you must select a security policy. You can update the security policy as needed.
    
    Amazon VPC IP Address Manager (IPAM) is a VPC feature that makes it easier for you to plan, track, and monitor IP addresses for your AWS workloads. You can use IPAM automated workflows to more efficiently manage IP addresses.
    
    ![](https://media.tutorialsdojo.com/amazon-ipam-ip-sharing-accounts-regions-ans-c01.png)
    
    You can use IPAM to do the following:
    
    – Organize IP address space into routing and security domains
    
    – Monitor IP address space that’s in use and monitor resources that are using space against business rules
    
    – View the history of IP address assignments in your organization
    
    – Automatically allocate CIDRs to VPCs using specific business rules
    
    – Troubleshoot network connectivity issues
    
    – Enable cross-region and cross-account sharing of your Bring Your Own IP (BYOIP) addresses
    
    Managing public IP address space with IPAM has the following benefits:
    
    – Improves public IP address utilization across your organization: You can use IPAM to share IP address space across AWS accounts. Without using IPAM, you cannot share your public IP space across AWS Organizations accounts.
    
    – Simplifies the process of bringing public IP space to AWS: You can use IPAM to onboard public IP address space once, and then use IPAM to distribute your public IPs across Regions. Without IPAM, you have to onboard your public IPs for each AWS Region.
    
    Hence, the correct answer is: **Update the security policy of the ALB to replace the compromised cipher and encryption protocol. Use the Amazon VPC IP Address Manager (IPAM) to enable cross-region and cross-account sharing of the Bring Your Own IP (BYOIP) addresses.**
    
    The option says: **Use AWS Shield Advanced to block the compromised cipher and encryption protocol in all of the ALBs. Configure the AWS Resource Access Manager (AWS RAM) to enable cross-region and cross-account sharing of the Bring Your Own IP (BYOIP) addresses** is incorrect because AWS Shield is primarily used to protect your AWS resources against DDoS attacks. It can’t be used to automatically block compromised cipher and encryption protocol in all of your ELBs. You have to manually update the security policy of your ELBs to satisfy the requirement. Furthermore, the use of AWS RAM is just partially correct. Although this service allows you to share resources to different AWS accounts, it is not capable of directly sharing BYOIP address space on selected AWS Regions and member accounts within your AWS Organization.
    
    The option says: **Use Amazon GuardDuty to block the compromised cipher and encryption protocol in all of the ALBs. Launch an AWS Transit Gateway to connect all AWS Regions in the AWS Organization and set up an Internet Group Management Protocol (IGMP) Multicast to enable cross-region and cross-account sharing of the BYOIP addresses** is incorrect because Amazon GuardyDuty simply offers continuous monitoring of your AWS accounts and workloads to protect your resources against malicious or unauthorized activities. This service just continuously analyzes CloudTrail Logs, VPC Flow Logs, DNS Logs, and other AWS logs to identify and detect any possible security threat to your AWS resources. However, it is not capable of blocking compromised security policies in your ELB. Take note that an AWS Transit Gateway is meant to connect VPCs, VPNs, and Direct Connect Gateways but not AWS Regions. In fact, an AWS Transit Gateway is a regional resource and can’t directly connect to resources situated in another AWS Region.
    
    The option says: **Update the policy of the ALB’s security group to replace the compromised cipher and encryption protocol. Use the Amazon Route 53 Resolver Inbound Endpoint to onboard the BYOIP addresses range and a Route 53 Resolver Outbound Endpoint to share them across AWS Regions and AWS accounts** is incorrect because you have to update the security policy of the ELB and not the policy of its security group. Moreover, the Amazon Route 53 Resolver Endpoints are meant to resolve the DNS queries that go in or out of your VPCs. The Route 53 Resolver Inbound Endpoint resolves DNS records in your private hosted zone while the Route 53 Resolver Outbound Endpoint resolves DNS records hosted on a remote network (e.g., on-premises network) from the resources in your VPC.
    
- Use Amazon GuardDuty to block the compromised cipher and encryption protocol in all of the ALBs. Launch an AWS Transit Gateway to connect all AWS Regions in the AWS Organization and set up an Internet Group Management Protocol (IGMP) Multicast to enable cross-region and cross-account sharing of the BYOIP addresses.
- Update the policy of the ALB’s security group to replace the compromised cipher and encryption protocol. Use the Amazon Route 53 Resolver Inbound Endpoint to onboard the BYOIP addresses range and a Route 53 Resolver Outbound Endpoint to share them across AWS Regions and AWS accounts.

A startup has a public website (`www.tutorialsdojo.com`) located in VPC A and an internal payroll web portal (`payroll.tutorialsdojo.com`) in VPC B. The Network Administrator has been assigned to use the same domain name (tutorialsdojo.com) for both the internal and external web applications. The two websites have different authentication for internal and external users.

What should the Administrator do to accomplish this assignment?

- Launch a BIND DNS server using an Amazon EC2 instance with the tutorialsdojo.com domain name. Update the existing DHCP Options Set of each existing VPC to use the new BIND server as the DNS provider. Create routes for the public website and the internal payroll portal.
- Set up a subdomain delegation using private hosted zones and Route 53 Resolver rules. Create two different hosted zones for the public website and the internal payroll portal.
- **Use Amazon Route 53 to configure split-view DNS by creating public and private hosted zones that have the same name. Associate the VPCs with the private hosted zone. Records created in the public hosted zone control how Internet traffic is routed, and records in the private hosted zone control how traffic is routed in your Amazon VPCs.**
    
    A hosted zone is a container for records, and records contain information about how you want to route traffic for a specific domain, such as tutorialsdojo.com and its subdomains (labs.tutorialsdojo.com, portal.tutorialsdojo.com). A hosted zone and the corresponding domain have the same name. There are two types of hosted zones:
    
    - Public hosted zones – contain records that specify how you want to route traffic on the Internet.
    - Private hosted zones – contain records that specify how you want to route traffic in an Amazon VPC.
    
    ![](https://media.tutorialsdojo.com/public/2020-07-02_09-19-14-b811a0888fab51759cc1f3764978c40e.png)
    
    You can use Route 53 to configure split-view DNS, also known as split-horizon DNS. In split-view DNS, you use the same domain name (tutorialsdojo.com) for internal uses (accounting.tutorialsdojo.com) and external uses, such as your public website (www.tutorialsdojo.com). You might also want to use the same subdomain name internally and externally but serve different content or require different authentication for internal and external users.
    
    To configure split-view DNS, you perform the following steps:
    
    - Create public and private hosted zones that have the same name. (Split-view DNS still works if you’re using another DNS service for the public hosted zone.)
    - Associate one or more Amazon VPCs with the private hosted zone. Route 53 Resolver uses the private hosted zone to route DNS queries in the specified VPCs.
    - Create records in each hosted zone. Records in the public hosted zone control how internet traffic is routed and records in the private hosted zone control how traffic is routed in your Amazon VPCs.
    
    If you need to perform name resolution of both your VPC and on-premises workloads, you can use Route 53 Resolver.
    
    Hence, the correct answer is: **Use Amazon Route 53 to configure split-view DNS by creating public and private hosted zones that have the same name. Associate the VPCs with the private hosted zone. Records created in the public hosted zone control how Internet traffic is routed, and records in the private hosted zone control how traffic is routed in your Amazon VPCs.**
    
    The option that says: **Set up a subdomain delegation using private hosted zones and Route 53 Resolver rules. Create two different hosted zones for the public website and the internal payroll portal** is incorrect because subdomain delegation is primarily used for scenarios where you have to use different DNS services for the domain and the subdomain. You have to set up a split-view DNS configuration for this scenario.
    
    The option that says: **Use AWS Cloud Map to split-view DNS hosted zone. Associate the VPCs with the public hosted zone. Records created in the private hosted zone control how Internet traffic is routed, and records in the public hosted zone control how traffic is routed in your Amazon VPCs** is incorrect because you can’t use AWS Cloud Map to create a split-view DNS hosted zone. Moreover, you have to associate your VPCs with the private hosted zone. A public hosted zone controls how Internet traffic is routed and not the private one.
    
    The option that says: **Launch a BIND DNS server using an Amazon EC2 instance with the [tutorialsdojo.com](http://tutorialsdojo.com/)domain name. Update the existing DHCP Options Set of each existing VPC to use the new BIND server as the DNS provider. Create routes for the public website and the internal payroll portal** is incorrect because a BIND DNS server is not needed for this task. In addition, you can’t update an existing DHCP Options Set of a VPC.
    
- Use AWS Cloud Map to split-view DNS hosted zone. Associate the VPCs with the public hosted zone. Records created in the private hosted zone control how Internet traffic is routed, and records in the public hosted zone control how traffic is routed in your Amazon VPCs.

A government agency is storing all sensitive voting information in its Amazon S3 bucket that contains personally identifiable information (PII). The Network Engineer has been assigned to only allow one authorized Elastic IP address to access the bucket. The authorized Elastic IP is currently attached to an EC2 instance in the us-east-1 Region while the S3 bucket resides in the us-west-2 Region.

What should the Engineer do to accomplish this task?

- **Modify the S3 bucket policy to include an `aws:SourceIp` condition that blocks all incoming traffic unless the request comes from an authorized IP address.**
    
    Buckets and objects are Amazon S3 resources. By default, only the resource owner can access these resources. The resource owner refers to the AWS account that creates the resource. Bucket policies and user policies are two access policy options available for granting permission to your Amazon S3 resources. Both use JSON-based access policy language.
    
    ![](https://media.tutorialsdojo.com/amazon-s3-vpc-endpoint.PNG)
    
    The `aws:SourceIp` condition key can be used in a policy to allow principals to make requests only from within a specified IP range. However, this policy denies access if an AWS service makes calls on the principal’s behalf. In this case, you can use `aws:SourceIp` with the [`aws:ViaAWSService`](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_condition-keys.html#condition-keys-viaawsservice) key to ensure that the source IP restriction applies only to requests made directly by a principal.
    
    Hence, the correct answer is: **Modify the S3 bucket policy to include an `aws:SourceIp` condition that blocks all incoming traffic unless the request comes from an authorized IP address.**
    
    The option that says: **Set the Network ACL of the S3 bucket to only allow access from the authorized Elastic IP address** is incorrect because Amazon S3 is not located inside a VPC hence, it doesn’t use Network ACLs. The correct way is to set the S3 bucket policy. Amazon S3 has an ACL that defines which AWS accounts or groups are granted access to the bucket, but it doesn’t filter based on a specific IP address or port like what a Network ACL does.
    
    The option that says: **Change the security group of the Amazon S3 bucket to only allow access from the authorized Elastic IP address** is incorrect because Amazon S3 doesn’t use security groups. You have to configure the bucket policy in order to meet the requirements of the scenario.
    
    The option that says: **Configure the attached IAM policy of the Elastic IP address to include the `aws:SourceIp` condition to only allow access to the S3 bucket in the us-west-2 Region** is incorrect because it is not required to configure an IAM Policy to the Elastic IP address. The `aws:SourceIp`condition must be set in the bucket policy.
    
- Set the network ACL of the S3 bucket to only allow access from the authorized Elastic IP address.
- Change the security group of the Amazon S3 bucket to only allow access from the authorized Elastic IP address.
- Configure the attached IAM policy of the Elastic IP address to include the `aws:SourceIp`condition to only allow access to the S3 bucket in the us-west-2 Region.

A company has an on-premises network in California and an Amazon VPC in US West (N. California). It has various applications and workloads which are using trusted IP addresses that its customers have whitelisted in their respective firewalls. The Network Administrator has been assigned to migrate these applications to AWS without requiring the customers to change their IP address whitelists.

What should the Administrator do to properly migrate the on-premises applications?

- Create a list of Elastic IP addresses to map the whitelisted IP address range in the on-premises network.
- Launch an Amazon CloudFront web distribution with an integrated AWS WAF. Set up an IP match condition to whitelist a specific IP address range in the company's VPC.
- Use AWS Global Accelerator to generate static anycast IP addresses.
- **Set up a Route Origin Authorization (ROA) document. Provision and advertise the whitelisted IP address range to the company's AWS account.**
    
    You can bring part or all of your public IPv4 address range from your on-premises network to your AWS account. You continue to own the address range, but AWS advertises it on the Internet. After you bring the address range to AWS, it appears in your account as an address pool. You can create an Elastic IP address from your address pool and use it with your AWS resources, such as EC2 instances, NAT gateways, and Network Load Balancers. This is also called “Bring Your Own IP Addresses (BYOIP)”.
    
    To ensure that only you can bring your address range to your AWS account, you must authorize Amazon to advertise the address range and provide proof that you own the address range.
    
    ![](https://media.tutorialsdojo.com/public/EC2_ENI_management_network.png)
    
    A Route Origin Authorization (ROA) is a document that you can create through your Regional internet registry (RIR), such as the American Registry for Internet Numbers (ARIN) or Réseaux IP Européens Network Coordination Centre (RIPE). It contains the address range, the ASNs that are allowed to advertise the address range, and the expiration date.
    
    The ROA authorizes Amazon to advertise an address range under a specific AS number. However, it does not authorize your AWS account to bring the address range to AWS. To authorize your AWS account to bring an address range to AWS, you must publish a self-signed X509 certificate in the RDAP remarks for the address range. The certificate contains a public key, which AWS uses to verify the authorization-context signature that you provide. You should keep your private key secure and use it to sign the authorization-context message.
    
    Hence, the correct answer is: **Set up a Route Origin Authorization (ROA) document. Provision and advertise the whitelisted IP address range to the company’s AWS account.**
    
    The option that says: **Create a list of Elastic IP addresses to map the whitelisted IP address range in the on-premises network** is incorrect because you cannot map the IP address of your on-premises network, which you are migrating to AWS, to an EIP address of your VPC. You must authorize Amazon to advertise the address range that you own to satisfy the requirement.
    
    The option that says: **Launch an Amazon CloudFront web distribution with an integrated AWS WAF. Set up an IP match condition to whitelist a specific IP address range in the company’s VPC** is incorrect because the IP match condition in CloudFront is primarily used in allowing or blocking the incoming web requests based on the IP addresses that the requests originate from. This is the opposite of what is being asked in the scenario where you have to migrate your suite of applications from your on-premises network and advertise the address range that you own in your VPC.
    
    The option that says: **Use AWS Global Accelerator to generate static anycast IP addresses** is incorrect because these IP addresses just serve as fixed entry points to your applications hosted in one or more AWS Region. You can’t use this to bring part or all of your public IPv4 address range from your on-premises network to your AWS account.
    

A multinational investment bank has recently provisioned an AWS Direct Connect (DX) connection from its on-premises data center to allow access to its various AWS resources. The Network Administrator has been instructed to configure the DX connection to allow the on-premises servers to connect to the EC2 resources in several Amazon VPCs and to fetch the data stored in the S3 buckets in various AWS Regions.

Which of the following should the Administrator do to complete the task with the LEAST amount of configuration overhead?

- Utilize a single public virtual interface to connect all of the VPC to the DX connection. Use multiple public virtual interfaces to access the Amazon S3 buckets across all AWS Regions.
- **Set up a public virtual interface to access all the Amazon S3 buckets across all AWS Regions. Use a Direct Connect Gateway and a private virtual interface to connect the multiple VPCs.**
    
    You can use an **AWS Direct Connect** gateway to connect your AWS Direct Connect connection over a private virtual interface to one or more VPCs in any account that is located in the same or different Regions. You associate a Direct Connect gateway with the virtual private gateway for the VPC. Then, you create a private virtual interface for your AWS Direct Connect connection to the Direct Connect gateway. You can attach multiple private virtual interfaces to your Direct Connect gateway.
    
    ![](https://media.tutorialsdojo.com/public/dx-gateway.png)
    
    To connect to your resources hosted in an Amazon Virtual Private Cloud (Amazon VPC) using their private IP addresses, use a private virtual interface. With a private virtual interface, you can:
    
    - Connect VPC resources (such as Amazon Elastic Compute Cloud (Amazon EC2) instances or load balancers) on your private IP address or endpoint.
    - Connect a private virtual interface to a DX gateway. Then, associate the DX gateway with one or more virtual private gateways in any AWS Region (except the AWS China Region).
    - Connect to multiple VPCs in any AWS Region (except the AWS China Region) because a virtual private gateway is associated with a single VPC.
    
    You must create one of the following virtual interfaces to begin using your AWS Direct Connect connection.
    
    - Private virtual interface: A private virtual interface should be used to access an Amazon VPC using private IP addresses.
    - Public virtual interface: A public virtual interface can access all Amazon S3 buckets or AWS public services using public IP addresses.
    - Transit virtual interface: A transit virtual interface should be used to access one or more Amazon VPC Transit Gateways associated with Direct Connect gateways. You can use transit virtual interfaces with 1/2/5/10 Gbps AWS Direct Connect connections.
    
    Hence, the correct answer is: **Set up a public virtual interface to access all the Amazon S3 buckets across all AWS Regions. Use a Direct Connect Gateway and a private virtual interface to connect the multiple VPCs.**
    
    The option that says: **Set up multiple private virtual interfaces to connect all of the VPC to the DX connection. Use a public virtual interface to access the S3 buckets across all AWS Regions** is incorrect. Although this setup will work, it entails a lot of configuration overhead to maintain multiple private virtual interfaces. A better solution is to set up a Direct Connect Gateway to connect the VPCs and use a single private virtual interface that goes to the on-premises network.
    
    The option that says: **Use a private virtual interface to access all the S3 buckets across all AWS Regions. Set up a Direct Connect Gateway and utilize a public virtual interface to connect the multiple VPCs** is incorrect because you have to use a public virtual interface to access the S3 buckets and a private virtual interface for the VPCs.
    
    The option that says: **Utilize a single public virtual interface to connect all of the VPC to the DX connection. Use multiple public virtual interfaces to access the Amazon S3 buckets across all AWS Regions** is incorrect because a private virtual interface is needed to connect the on-premises data center to an Amazon VPC. In addition, it is not necessary to use multiple public virtual interfaces to access the Amazon S3 buckets across all AWS Regions. A single public virtual interface is sufficient enough to provide you access to multiple S3 buckets across different Regions.
    
- Set up multiple private virtual interfaces to connect all of the VPC to the DX connection. Use a public virtual interface to access the S3 buckets across all AWS Regions.
- Use a private virtual interface to access all the S3 buckets across all AWS Regions. Set up a Direct Connect Gateway and utilize a public virtual interface to connect the multiple VPCs.

A bank has a hybrid cloud architecture in which its on-premises network and Amazon VPC is connected via an AWS Direct Connect connection using a private virtual interface. The Network Administrator has been assigned to set up a highly available Amazon AppStream 2.0 solution that provides the bank employees with instant access to their virtual desktop applications anytime and anywhere. The On-Demand AppStream 2.0 fleet must be able to communicate with the cloud resources both in the existing Amazon VPC and the company’s on-premises data center.

What should the Administrator do to complete this assignment?

- Launch two subnets into the existing Amazon VPC. Launch a transit virtual interface on the AWS Direct Connect connection to allow the on-premises users to connect to the AppStream's Gateway VPC endpoint.
- Launch two subnets into the existing VPC. Set up a new public virtual interface on the AWS Direct Connect connection to allow the on-premises users to connect to the AppStream's Interface VPC endpoint.
- **Set up two subnets into the existing Amazon VPC. Launch another private virtual interface on the AWS Direct Connect connection to allow the on-premises users to connect to the AppStream's Interface VPC endpoint.**
    
    **Amazon AppStream 2.0** is a fully managed application streaming service that provides users with instant access to their desktop applications from anywhere. AppStream 2.0 manages the AWS resources required to host and run your applications, scales automatically, and provides access to your users on demand. AppStream 2.0 provides users access to the applications they need on the device of their choice, with a responsive, fluid user experience that is indistinguishable from natively installed applications.
    
    If you use Amazon Virtual Private Cloud to host your AWS resources, you can establish a private connection between your VPC and AppStream 2.0. You can use this connection to enable AppStream 2.0 to communicate with your resources on your VPC without going through the public Internet.
    
    ![](https://media.tutorialsdojo.com/public/How%20AppStream%202.0%20works.e44338a1389a2066d577c2581b25218bbcbd52c1.png)
    
    Interface endpoints are powered by AWS PrivateLink, a technology that lets you keep streaming traffic within a VPC that you specify by using private IP addresses. When you use the VPC with an AWS Direct Connect or AWS Virtual Private Network (VPN) tunnel, you can keep the streaming traffic within your network.
    
    You can use an interface VPC endpoint in your AWS account to restrict all network traffic between your Amazon VPC and AppStream 2.0 to the Amazon network. After you create this endpoint, you configure your AppStream 2.0 stack or image builder to use it.
    
    You can choose the VPCs to which your Amazon AppStream 2.0 instances (fleet and image builders) connect. When you create your fleet or launch Image Builder, you can specify one or more subnets in your VPC. If you have a VPC with a VPN connection to your on-premises network then Amazon AppStream 2.0 instances in your fleet can communicate with your on-premises network. You retain the usual control you have over network access within your VPC, using all the normal configuration options such as security groups, network access control lists, and routing tables.
    
    Hence, the correct answer is: **Set up two subnets into the existing Amazon VPC. Launch another private virtual interface on the AWS Direct Connect connection to allow the on-premises users to connect to the AppStream’s Interface VPC endpoint.**
    
    The option that says: **Launch two subnets into the existing VPC. Set up a new public virtual interface on the AWS Direct Connect connection to allow the on-premises users to connect to the AppStream’s Interface VPC endpoint** is incorrect because you have to use a private virtual interface in order to connect to your VPC.  A public virtual interface is only used to connect to AWS resources that are reachable by a public IP address such as an S3 bucket or AWS public endpoints.
    
    The option that says: **Set up two subnets into the existing Amazon VPC. Establish a VPC Peering connection between the AppStream’s Interface VPC endpoint to the existing VPC. The existing Direct Connect connection will allow the users from the on-premises network to access the AppStream fleet** is incorrect because a VPC Peering connection is only used between two VPCs, not between an Interface VPC Endpoint and a VPC.
    
    The option that says: **Launch two subnets into the existing Amazon VPC. Launch a transit virtual interface on the AWS Direct Connect connection to allow the on-premises users to connect to the AppStream’s Gateway VPC endpoint** is incorrect because a transit virtual interface is primarily used to connect to your resources hosted in an Amazon VPC through a transit gateway. Moreover, you have to use an Interface VPC endpoint for the AppStream and not a Gateway VPC endpoint as this type is only used for Amazon S3 and DynamoDB.
    
- Set up two subnets into the existing Amazon VPC. Establish a VPC Peering connection between the AppStream's Interface VPC endpoint to the existing VPC. The existing Direct Connect connection will allow the users from the on-premises network to access the AppStream fleet.

A data analytics company is planning to launch a high performance computing (HPC) application in AWS. The Network Engineer has been tasked to set up the architecture that provides increased network performance for the application, which will be using several EC2 instances.

Which of the following should the Engineer implement to meet the above requirement? (Select TWO.)

- **Enable Enhanced Networking on all EC2 instances.**
- Deploy multiple EC2 instances with the same instance type in a partition placement group that uses two Availability Zones.
- Set up the EC2 instances to use equal-cost multi-path routing (ECMP) to improve network performance.
- **Launch multiple EC2 instances with the same instance type in a placement group located in a single Availability Zone.**
- Modify the DHCP Options Set of the VPC to increase the default network maximum transmission unit (MTU) of the EC2 instances to 9001.
- **Evaluation**
    
    When you launch a new EC2 instance, the EC2 service attempts to place the instance in such a way that all of your instances are spread out across underlying hardware to minimize correlated failures. You can use placement groups to influence the placement of a group of interdependent instances to meet the needs of your workload. Depending on the type of workload, you can create a placement group using one of the following placement strategies:
    
    - **Cluster** – packs instances close together inside an Availability Zone. This strategy enables workloads to achieve the low-latency network performance necessary for tightly-coupled node-to-node communication that is typical of HPC applications.
    - **Partition** – spreads your instances across logical partitions such that groups of instances in one partition do not share the underlying hardware with groups of instances in different partitions. This strategy is typically used by large distributed and replicated workloads, such as Hadoop, Cassandra, and Kafka.
    - **Spread** – strictly places a small group of instances across distinct underlying hardware to reduce correlated failures.
    
    A cluster placement group is a logical grouping of instances within a single Availability Zone. A cluster placement group can span peered VPCs in the same Region. Instances in the same cluster placement group enjoy a higher per-flow throughput limit of up to 10 Gbps for TCP/IP traffic and are placed in the same high-bisection bandwidth segment of the network.
    
    ![](https://media.tutorialsdojo.com/placement-group-cluster.png)
    
    To provide the lowest latency and the highest packet-per-second network performance for your placement group, choose an instance type that supports enhanced networking.
    
    Enhanced networking uses single root I/O virtualization (SR-IOV) to provide high-performance networking capabilities on supported instance types. SR-IOV is a method of device virtualization that provides higher I/O performance and lower CPU utilization when compared to traditional virtualized network interfaces. Enhanced networking provides higher bandwidth, higher packet per second (PPS) performance, and consistently lower inter-instance latencies. There is no additional charge for using enhanced networking.
    
    Hence, the correct answers are:
    
    - **Enable Enhanced Networking on all EC2 instances.**
    - **Launch multiple EC2 instances with the same instance type in a placement group located in a single Availability Zone.**
    
    The option that says: **Modify the DHCP Options Set of the VPC to increase the default network maximum transmission unit (MTU) of the EC2 instances to 9001 is** incorrect because the MTU is set in the EC2 instance and not in the VPC. In addition, you can’t modify an already existing DHCP Options Set.
    
    The option that says: **Deploy multiple EC2 instances with the same instance type in a partition placement group that uses two Availability Zones** is incorrect because a partition placement group doesn’t improve network performance. This type of placement group distributes your EC2 instances across logical partitions such that groups of instances in one partition do not share the underlying hardware with groups of instances in different partitions.
    
    The option that says: **Set up the EC2 instances to use equal-cost multi-path routing (ECMP) to improve network performance** is incorrect because ECMP is commonly used in establishing network connectivity between virtual private clouds (VPCs) and your on-premises network using multiple VPN connections that are associated with a Transit Gateway.
    

An application is hosted in an EBS-backed On-Demand EC2 Instance that is publicly accessible by its external customers over the Internet. To save on operating costs, the instance is stopped every Friday night since the application is not used over the weekend. Every Monday morning, the application is restarted again using the `StartInstances` API. The auto-assigned public IP address associated to the instance changes every time it is stopped and restarted. The customers are always updating their firewall to whitelist the application’s new public IP address whenever it is changed.

What should you do to have a static public IP address that doesn’t change even when the instance is restarted? (Select TWO.)

- **Allocate an Elastic IP address to the EC2 instance.**
- Attach an Elastic Network Interface (ENI) with a static public IP address to the EC2 instance.
- Allocate a static Amazon router peer IP to the EC2 instance.
- **Use AWS Global Accelerator to generate an endpoint with a static Anycast IP address for the EC2 instance.**
- Attach a VPC endpoint to the EC2 instance
- **Evaluation**
    
    An Elastic IP address is a static IPv4 address designed for dynamic cloud computing and is associated with your AWS account. With an Elastic IP address, you can mask the failure of an instance or software by rapidly remapping the address to another instance in your account.
    
    An Elastic IP address is a public IPv4 address that is reachable from the Internet. If your instance does not have a public IPv4 address, you can associate an Elastic IP address with your instance to enable communication with the Internet. For example, this allows you to connect to your instance from your local computer.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-19_03-14-40-785a3448b5804bd7d602f423fe69293e.png)
    
    **AWS Global Accelerator** is a service that improves the availability and performance of your applications with local or global users. It provides static IP addresses that act as a fixed entry point to your application endpoints in a single or multiple AWS Regions, such as your Application Load Balancers, Network Load Balancers, or Amazon EC2 instances.
    
    Hence, the correct answers are:
    
    - **Allocate an Elastic IP address to the EC2 instance.**
    - **Use AWS Global Accelerator to generate an endpoint with a static Anycast IP address for the EC2 instance.**
    
    The option that says: **Allocate a static Amazon router peer IP to the EC2 instance** is incorrect because an Amazon router peer IP is only used in an AWS Direct Connect connection.
    
    The option that says: **Attach an Elastic Network Interface (ENI) with a static public IP address to the EC2 instance** is incorrect because you can’t create a static public IP address using an ENI.
    
    The option that says: **Attach a VPC endpoint to the EC2 instance** is incorrect. A VPC endpoint simply enables you to privately connect your VPC to supported AWS services and VPC endpoint services powered by AWS PrivateLink, without requiring an Internet gateway, NAT device, VPN connection, or AWS Direct Connect connection.
    

An Amazon S3 bucket contains high-resolution images and videos that must be accessed by an application hosted in the company’s on-premises data center. To avoid any network latency over the Internet, the company tasked its Network Engineer to set up an AWS Direct Connect connection to achieve a hybrid cloud network architecture.

How can the Engineer satisfy this requirement?

- Use a private virtual interface for the Direct Connect connection with a Gateway VPC Endpoint
- Create a new VPC endpoint for Amazon S3.
- Configure Bidirectional Forwarding Detection (BFD) in the Direct Connect connection.
- **Create a public virtual interface for the Direct Connect connection.**
    
    In Direct Connect, it’s not possible to directly access an S3 bucket through a private virtual interface (VIF) using a Gateway VPC endpoint. Take note that the on-premises traffic can’t traverse the Gateway VPC endpoint, so you have to use an Interface VPC endpoint instead if you have a private virtual interface in place.
    
    For a private virtual interface, AWS advertises the VPC CIDR only over the Border Gateway Protocol (BGP) neighbor. AWS can’t advertise or suppress specific subnet blocks in the Amazon VPC for a private virtual interface.
    
    ![](https://media.tutorialsdojo.com/public/direct-connect-overview.png)
    
    After the BGP is up and established, the Direct Connect router advertises all global public IP prefixes, including Amazon S3 prefixes. Traffic heading to Amazon S3 is routed through the Direct Connect public virtual interface through a private network connection between AWS and your data center or corporate network.
    
    Hence, the correct answer is: **Create a public virtual interface for the Direct Connect connection.**
    
    The option that says: **Use a private virtual interface for the Direct Connect connection with a Gateway VPC Endpoint** is incorrect. A private virtual interface is commonly used to enable access to your VPC and private AWS resources. Using a Gateway VPC Endpoint is not necessary if you have a private virtual interface already.
    
    The option that says: **Create a new VPC endpoint for Amazon S3** is incorrect because a VPC endpoint is primarily used to privately connect your VPC to supported AWS services and VPC endpoint services without requiring an Internet gateway, NAT device, or AWS Direct Connect connection. With a VPC Endpoint, the traffic between your VPC and Amazon S3 does not leave the Amazon network. It ensures that the S3 bucket is inaccessible to any external network, such as your on-premises data center, thus, this option is incompatible with the AWS Direct Connect.
    
    The option that says: **Configure Bidirectional Forwarding Detection (BFD) in the Direct Connect connection** is incorrect. This is not a required option in order for the on-premises application to access the Amazon S3 bucket. BFD is simply a detection protocol that provides fast forwarding path failure detection times, which allows for a faster routing re-convergence time.
    

An organization has two AWS Direct Connect (DX) connections from the on-premises network to its Amazon VPC, which provides redundancy and high availability. By default, the Border Gateway Protocol (BGP) waits for three keep-alive failures of 90 seconds to trigger a failover. The organization instructed the Network team to configure the primary DX connection to failover to the secondary connection in less than a second to minimize any downtime. The solution should provide fast forwarding path failure detection that allows for a faster routing reconvergence time.

What must the team do to satisfy this requirement?

- Enable Dead Peer Detection (DPD) in the customer router by setting a minimum interval of 300 ms for the DPD liveness detection time and a multiplier value of three.
- **Enable Bidirectional Forwarding Detection (BFD) in the customer router by setting a minimum interval of 300 ms for the BFD liveness detection time and a multiplier value of three.**
    
    If you have established a second AWS Direct Connect connection, traffic will failover to the second link automatically. You can enable Bidirectional Forwarding Detection (BFD) when configuring your connections to ensure fast detection and failover. To achieve high availability, it’s highly recommended to have connections at multiple AWS Direct Connect locations.
    
    BFD is a detection protocol to provide fast forwarding path failure detection times, which allows for a faster routing re-convergence time. BFD is a mechanism independent of media, routing protocols, or data. It’s a best practice to enable BFD for fast link failure detection and failover when connecting to AWS services over DX connections and AWS VPNs. Enabling BFD for your DX connection allows the Border Gateway Protocol (BGP) neighbor relationship to be torn down quickly after notifications from BFD. Otherwise, by default, BGP waits for three keep-alive failures of 90 seconds.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-28_06-19-56-0b4927b92c225d6ec7a4f3a9fa58a24b.png)
    
    Asynchronous BFD is automatically enabled for each Direct Connect virtual interface but will not take effect until it’s configured on your router. AWS has set the BFD liveness detection minimum interval to 300 and the BFD liveness detection multiplier to 3.
    
    Hence, the correct answer is: **Enable Bidirectional Forwarding Detection (BFD) in the customer router by setting a minimum interval of 300 ms for the BFD liveness detection time and a multiplier value of three.**
    
    The option that says: **Configure Dead Peer Detection (DPD) to the customer router by enabling the Internet Control Message Protocol (ICMP) redirect messages** is incorrect because DPD is primarily used in a VPN connection only. It enables VPN devices to rapidly identify when a network condition prevents the delivery of packets across the public Internet. You have to use Bidirectional Forwarding Detection (BFD) instead.
    
    The option that says: **Configure Bidirectional Forwarding Detection (BFD) Echo mode to the customer router by enabling the Internet Control Message Protocol (ICMP) redirect messages** is incorrect because using the echo mode is only optional. You have to set the minimum interval time and the multiplier value to enable BFD. Moreover, you actually have to disable the ICMP redirect messages in order to enable the echo mode.
    
    The option that says: **Enable Dead Peer Detection (DPD) in the customer router by setting a minimum interval of 300 ms for the DPD liveness detection time and a multiplier value of three** is incorrect because you have to enable Bidirectional Forwarding Detection (BFD) for this particular situation. DPD is only used for dynamically-routed VPN connections.
    
- Configure Dead Peer Detection (DPD) to the customer router by enabling the Internet Control Message Protocol (ICMP) redirect messages.
- Configure Bidirectional Forwarding Detection (BFD) Echo mode to the customer router by enabling the Internet Control Message Protocol (ICMP) redirect messages.

A Network Engineer has been tasked to set up an HTTP proxy server using a single EC2 instance in the company’s VPC for domain whitelisting and content filtering. The proxy server will also be used to filter all traffic initiated by the EC2 instances to the company’s on-premises network. The Engineer must ensure that the solution provides an aggregate network bandwidth of up to 100 Gbps.

Which of the following should the Engineer do to satisfy this requirement?

- Launch the proxy server using a large EC2 instance type. Attach multiple Elastic Network Interfaces (ENI) to the instance to provide an aggregate network bandwidth of up to 100 Gbps.
- **Launch an Accelerated Computing EC2 Instance type such as the P3 or higher. Install the ENA Linux Kernel Driver in the instance then enable Enhanced Networking.**
    
    Enhanced networking uses single root I/O virtualization (SR-IOV) to provide high-performance networking capabilities on supported instance types. SR-IOV is a method of device virtualization that provides higher I/O performance and lower CPU utilization when compared to traditional virtualized network interfaces. Enhanced networking provides higher bandwidth, higher packet per second (PPS) performance, and consistently lower inter-instance latencies. There is no additional charge for using enhanced networking.
    
    ![](https://media.tutorialsdojo.com/public/efa_stack.png)
    
    Depending on your instance type, enhanced networking can be enabled using one of the following mechanisms:
    
    - Elastic Network Adapter (ENA)
    - Intel 82599 Virtual Function (VF) interface
    
    Amazon EC2 P3 instances deliver high-performance compute in the cloud with up to 8 NVIDIA® V100 Tensor Core GPUs and up to 100 Gbps of networking throughput for machine learning and HPC applications. These instances deliver up to one petaflop of mixed-precision performance per instance to significantly accelerate machine learning and high-performance computing applications. Amazon EC2 P3 instances have been proven to reduce machine learning training times from days to minutes, as well as increase the number of simulations completed for high-performance computing by 3-4x.
    
    Hence, the correct answer is: **Launch an Accelerated Computing EC2 Instance type such as the P3 or higher. Install the ENA Linux Kernel Driver in the instance then enable Enhanced Networking.**
    
    The option that says: **Launch the proxy server using a large EC2 instance type. Attach multiple Elastic Network Interfaces (ENIs) to the instance to provide an aggregate network bandwidth of up to 100 Gbps** is incorrect because having multiple ENIs is not enough to gain a network bandwidth of up to 100 Gbps. You must enable Enhanced Networking.
    
    The option that says: **Launch the proxy server using any EC2 instance type. Use an Elastic Fabric Adapter (EFA) to automatically provision a network bandwidth of up to 100 Gbps** is incorrect because not all EC2 instance types support EFA. You must ensure that you deploy the right EC2 instance type/size that provides the high network bandwidth that you require.
    
    The option that says: **Launch the proxy server in a Cluster Placement group to accelerate the network performance of the traffic between the on-premises network and the Amazon VPC** is incorrect because a placement group can only provide a low-latency network performance on the traffic between the EC2 instances that belong to the same placement group.
    
- Launch the proxy server using any EC2 instance type. Use an Elastic Fabric Adapter (EFA) to automatically provision a network bandwidth of up to 100 Gbps.
- Launch the proxy server in a Cluster Placement group to accelerate the network performance of the traffic between the on-premises network and the Amazon VPC.

A startup is testing its prototype mobile application that fetches data from a GraphQL API, which is hosted on an Amazon EC2 instance. The Network Engineer noticed that the public IP address of the EC2 instance is always changing whenever the instance has been stopped or terminated. The mobile application loses connection to the API whenever there is a change in the IP address. The Engineer must ensure that the public IP address of the instance does not change regardless of the EC2 instance status.

Which of the following should the Engineer do to satisfy the requirement?

- Modify the EC2 instance to use Enhanced Networking.
- **Create an Elastic IP address and allocate it to the EC2 instance.**
    
    An Elastic IP address is a static IPv4 address designed for dynamic cloud computing. An Elastic IP address is associated with your AWS account.
    
    ![](https://media.tutorialsdojo.com/public/2020-07-04_02-35-27-d88d495d526e52b0d1bf4f9391751e0d.png)
    
    With an Elastic IP address, you can mask the failure of an instance or software by rapidly remapping the address to another instance in your account. An Elastic IP address is a public IPv4 address, which is reachable from the Internet. If your instance does not have a public IPv4 address, you can associate an Elastic IP address with your instance to enable communication with the Internet; for example, to connect to your instance from your local computer.
    
    Hence, the correct answer is: **Create an Elastic IP address and allocate it to the EC2 instance.**
    
    The option that says: **Create a new Elastic Network Interface (ENI) and configure a static public IP address. Attach the ENI to the instance** is incorrect because an ENI is just a logical networking component in a VPC that represents a virtual network card. The public IP address that the ENI provides is not static and will also be changed when the instance was restarted or terminated.
    
    The option that says: **Associate the EC2 instance to a CloudFront web distribution and use the Dedicated IP Custom SSL feature for the instance to have a static public IP address** is incorrect because this CloudFront feature is primarily used if you need to deliver content to browsers that don’t support SNI. This option will not provide a static public IP address to the instance.
    
    The option that says: **Modify the EC2 instance to use Enhanced Networking** is incorrect because the Enhanced Networking feature simply improves the network bandwidth of the instance.
    
- Associate the EC2 instance to a CloudFront web distribution and use the Dedicated IP Custom SSL feature for the instance to have a static public IP address.
- Create a new Elastic Network Interface (ENI) and configure a static public IP address. Attach the ENI to the instance.

A Systems Administrator has been tasked to set up a notification system that tracks every change in the AWS IP address ranges. How can the Administrator implement the solution with the LEAST effort?

- **In Amazon SNS, create a new subscription to the `arn:aws:sns:us-east-1:806199016981:AmazonIpSpaceChanged` Topic ARN.**
    
    Amazon Web Services (AWS) publishes its current IP address ranges in JSON format. To view the current ranges, you have to download the `.json` file. To maintain and track history, you have to save successive versions of the `.json` file on your system. To determine whether there have been changes since the last time that you saved the file, you can check the publication time in the current file and compare it to the publication time in the last file that you saved.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-30_11-19-30-2a13e98621bb04b1e646c9397a9d4892.png)
    
    Whenever there is a change to the AWS IP address ranges, Amazon sends notifications to subscribers of the `AmazonIpSpaceChanged` topic, which has an ARN of `arn:aws:sns:us-east-1:806199016981:AmazonIpSpaceChanged` in Amazon SNS. You can subscribe to this topic to easily get notified about any AWS public IP address ranges.
    
    Hence, the correct answer is: **In Amazon SNS, create a new subscription to the `arn:aws:sns:us-east-1:806199016981:AmazonIpSpaceChanged` Topic ARN.**
    
    The option that says: **Create a Lambda function that reads the `https://ip-ranges.amazonaws.com/ip-ranges.json` endpoint and send a notification using Amazon SNS. Set up a CloudWatch Events rule to run the function every hour** is incorrect. Although this solution could work, it entails a lot of effort to implement. Remember that the scenario requires a solution that has the LEAST amount of effort to execute.
    
    The option that says: **Launch an EC2 Spot Instance that downloads the `ip-ranges.json` file from the `https://ip-ranges.amazonaws.com` endpoint every hour, using a cron job. Install and use a CloudWatch agent to send out the notification** is incorrect because you can simply subscribe to the `AmazonIpSpaceChanged` topic in Amazon SNS to satisfy the requirement. Moreover, a CloudWatch agent is not capable of sending notifications. You have to use Amazon SNS instead.
    
    The option that says: **Use the AWS managed `AmazonIpSpaceChanged` queue in Amazon SQS. Subscribe the queue to an Amazon SNS Topic which will be used for the notification** is incorrect because there is no `AmazonIpSpaceChanged` queue in Amazon SQS. You have to use the `AmazonIpSpaceChanged`Topic in Amazon SNS.
    
- Create a Lambda function that reads the `https://ip-ranges.amazonaws.com/ip-ranges.json` endpoint and send a notification using Amazon SNS. Set up a CloudWatch Events rule to run the function every hour.
- Launch an EC2 Spot Instance that downloads the `ip-ranges.json` file from the `https://ip-ranges.amazonaws.com` endpoint every hour, using a cron job. Install and use a CloudWatch agent to send out the notification.
- Use the AWS managed `AmazonIpSpaceChanged` queue in Amazon SQS. Subscribe the queue to an Amazon SNS Topic which will be used for the notification.

The Network Team has recently established an AWS Direct Connect connection between the corporate on-premises data center and its Transit VPC with multiple private virtual interfaces. The CISO mandates that the networking cost of its hybrid cloud architecture should always be monitored.

Which of the following are valid Direct Connect billing charges that the team should monitor? (Select TWO.)

- **Data Transfer Out (DTO)**
- **Port hours**
- Instance hours
- Data Transfer In (DTI)
- BGP hours
- **Evaluation**
    
    **AWS Direct Connect** is a network service that provides an alternative to using the Internet to connect customers’ on-premises sites to AWS. In many circumstances, private network connections can reduce costs, increase bandwidth, and provide a more consistent network experience than Internet-based connections.
    
    AWS Direct Connect has two billing elements:
    
    1. Port hours
    2. Outbound data transfer
    
    Port hour pricing is determined by capacity and connection type (dedicated connection or hosted connection). Data Transfer Out charges for private interfaces and transit virtual interfaces are allocated to the AWS account responsible for the Data Transfer. There are no additional charges to use a multi-account AWS Direct Connect gateway.
    
    ![](https://media.tutorialsdojo.com/public/Product-Page-Diagram_Direct-Connect.0166400894ac5f9c921c3f7a346f55d8c42f492c.png)
    
    For publicly addressable AWS resources (for example, Amazon S3 buckets, Classic EC2 instances, or EC2 traffic that goes through an internet gateway), if the outbound traffic is destined for public prefixes owned by the same AWS payer account and actively advertised to AWS through an AWS Direct Connect public virtual Interface, the Data Transfer Out (DTO) usage is metered toward the resource owner at AWS Direct Connect data transfer rate.
    
    With the introduction of the granular Data Transfer Out allocation feature, the AWS account responsible for this will be charged for the Data Transfer Out performed over a transit/private virtual interface. The AWS account responsible for the Data Transfer Out will be determined based on the customer’s use of the private/transit virtual interface as follows:
    
    - Private virtual interface(s) is used to interface with Amazon Virtual Private Cloud(s) with or without Direct Connect gateway(s). In the case of the private virtual interface, the AWS account owning the AWS resources responsible for the Data Transfer Out will be charged.
    - Transit virtual interface(s) is used to interface with AWS Transit Gateway(s). In the case of the transit virtual interface, the AWS account owning the Amazon Virtual Private Cloud(s) attached to the AWS Transit Gateway associated with the Direct Connect gateway attached to the transit virtual interface will be charged. Please note that all applicable AWS Transit Gateway-specific charges (Data Processing and Attachment) will be in addition to the AWS Direct Connect Data Transfer Out.
    
    Hence, the correct answers are:
    
    - **Port hours**
    - **Data Transfer Out (DTO)**
    
    **Instance hours** is incorrect because this is only applicable to Amazon EC2 instances.
    
    **BGP hours** is incorrect because there is no such billing attribute like this in AWS. Border Gateway Protocol (BGP) is just a routing protocol used to route traffic across the Internet.
    
    **Data Transfer In (DTI)** is incorrect because the charge for this usage is $0.00 per GB in all locations. Technically, this is free of charge and should not be monitored by the team.
    

A Network Administrator is setting up an automated monitoring system that checks whether all Elastic IP addresses are attached to EC2 instances. The monitoring solution should verify that the Elastic IP addresses are also used by the elastic network interfaces (ENIs).

What should the Administrator do to complete this task?

- **Set up and use the EIP-ATTACHED managed rule in AWS Config.**
    
    **AWS Config** provides AWS managed rules, which are predefined, customizable rules that AWS Config uses to evaluate whether your AWS resources comply with common best practices. For example, you could use a managed rule to quickly start assessing whether your Amazon Elastic Block Store (Amazon EBS) volumes are encrypted or whether specific tags are applied to your resources. You can set up and activate these rules without writing the code to create an AWS Lambda function, which is required if you want to create custom rules. The AWS Config console guides you through the process of configuring and activating a managed rule. You can also use the AWS Command Line Interface or AWS Config API to pass the JSON code that defines your configuration of a managed rule.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-19_05-07-04-31802f8737c514d9a5ee948f19800b0f.png)
    
    You can customize the behavior of a managed rule to suit your needs. For example, you can define the rule’s scope to constrain which resources trigger an evaluation for the rule, such as EC2 instances or volumes. You can customize the rule’s parameters to define attributes that your resources must have to comply with the rule. For example, you can customize a parameter to specify that your security group should block incoming traffic to a specific port number.
    
    After you activate a rule, AWS Config compares your resources to the conditions of the rule. After this initial evaluation, AWS Config continues to run evaluations each time one is triggered. The evaluation triggers are defined as part of the rule, and they can include the following types:
    
    - **Configuration changes** – AWS Config triggers the evaluation when any resource that matches the rule’s scope changes in configuration. The evaluation runs after AWS Config sends a configuration item change notification.
    - **Periodic** – AWS Config runs evaluations for the rule at a frequency that you choose (for example, every 24 hours).
    
    The EIP-ATTACHED AWS Config Managed Rule checks whether all Elastic IP addresses that are allocated to a VPC are attached to EC2 instances or in-use elastic network interfaces (ENIs). Results might take up to 6 hours to become available after an evaluation occurs.
    
    Hence, the correct answer is: **Set up and use the EIP-ATTACHED managed rule in AWS Config.**
    
    The option that says: **Enable Detailed Monitoring in CloudWatch** is incorrect because the detailed monitoring feature simply increases the frequency of your monitoring metrics from a 5-minute period to a 1-minute period.
    
    The option that says: **Install an Amazon Inspector agent to all EC2 instances and configure the agent to send a notification if the instance doesn’t have an Elastic IP address** is incorrect because an Amazon Inspector is not capable of doing this. A more suitable option is to use AWS Config.
    
    The option that says: **Use Amazon GuardDuty in the VPC and enable the EIP-ATTACHED managed rule** is incorrect because Amazon GuardDuty doesn’t have an EIP-ATTACHED rule. Amazon GuardDuty simply offers continuous monitoring of your AWS accounts and workloads to protect against malicious or unauthorized activities.
    
- Enable Detailed Monitoring in CloudWatch.
- Use Amazon GuardDuty in the VPC and enable the EIP-ATTACHED managed rule.
- Install an Amazon Inspector agent to all EC2 instances and configure the agent to send a notification if the instance doesn't have an Elastic IP address.

An Amazon Virtual Private Cloud (VPC) has a fleet of EC2 instances hosted in a private subnet that connects to an Amazon S3 bucket via its VPC Gateway Endpoint. The VPC doesn’t have an attached Internet Gateway since the application is only used internally. You were instructed to configure the security group of the instances to only allow traffic to and from the public IP ranges of the S3 endpoint. To get the required IP ranges, you used the information provided by the `aws ec2 describe-prefix-lists` AWS CLI command. The solution worked but after a few weeks, you noticed that the outgoing requests are timing out and the instances can’t connect to the bucket any longer.

Which of the following is the most suitable solution that you can do to solve this issue with LEAST effort?

- Create a new customer-managed prefix-list that contains the public IP ranges of the S3 endpoint. Modify the outbound VPC security group rule to use the prefix-list in connecting to the Gateway VPC endpoint.
- **Configure the outbound VPC security group rule to use the AWS-managed prefix-list in connecting to the Gateway VPC endpoint.**
    
    The **AWS prefix list ID** logically represents the range of public IP addresses used by the service. All instances in subnets associated with the specified route tables automatically use the endpoint to access the service. Subnets that are not associated with the specified route tables do not use the endpoint. This enables you to keep resources in other subnets separate from your endpoint.
    
    To view the current public IP address range for a service, you can use the [describe-prefix-lists](https://docs.aws.amazon.com/cli/latest/reference/ec2/describe-prefix-lists.html) command:
    
    ![](https://media.tutorialsdojo.com/public/2020-07-01_10-02-58-ea16280180938c95bb272bb3d6e219cf.png)
    
    A prefix list is a set of one or more CIDR blocks. There are two types of prefix lists:
    
    - **AWS-managed prefix-list** — Represents the IP address ranges for an AWS service. You can reference an AWS-managed prefix-list in your VPC security group rules and in subnet route table entries. For example, you can reference an AWS-managed prefix-list in an outbound VPC security group rule when connecting to an AWS service through a gateway VPC endpoint. You cannot create, modify, share, or delete an AWS-managed prefix list.
    - **Customer-managed prefix-list** — A set of IPv4 or IPv6 CIDR blocks that you define and manage. You can reference the prefix-list in your VPC security group rules and in subnet route table entries. This enables you to manage the IP addresses that you frequently use for these resources in a single group, instead of repeatedly referencing the same IP addresses in each resource. You can share your prefix list with other AWS accounts, enabling those accounts to reference the prefix-list in their own resources.
    
    ![](https://media.tutorialsdojo.com/public/2020-07-01_10-13-29-11ba9ae5c9ee3d9d78e874593f2242d4.png)
    
    In this scenario, you can use the AWS-managed prefix-lists in your security group to allow access to the VPC Gateway Endpoint of the Amazon S3 bucket. AWS will automatically update the underlying public IP ranges that are associated with these prefix-lists.
    
    Hence, the correct answer is: **Configure the outbound VPC security group rule to use the AWS-managed prefix-list in connecting to the Gateway VPC endpoint.**
    
    The option that says: **Create a new customer-managed prefix-list that contains the public IP ranges of the S3 endpoint. Modify the outbound VPC security group rule to use the prefix-list in connecting to the Gateway VPC endpoint** is incorrect. Although this option may work, you still have to manually update the prefix-list whenever the AWS public IP ranges were changed. A more suitable solution here is to use AWS-managed prefix-list.
    
    The option that says: **Set up a CloudFront Function for the Amazon S3 bucket to have a static public IP range that doesn’t change** is incorrect because a CloudFront Function simply allows you to write lightweight functions in JavaScript for high-scale, latency-sensitive CDN customizations. Using CloudFront Functions in this situation is not applicable.
    
    The option that says: **Create a Lambda function that updates the security group. Use the AWS-managed `AmazonIpSpaceChanged` topic in Amazon SNS as the trigger for the function** is incorrect. Although this can technically work, this solution still entails a lot of effort to implement. You can simply use an AWS-managed prefix-list for this scenario.
    
- Set up a CloudFront Function for the Amazon S3 bucket to have a static public IP range that doesn't change.
- Create a Lambda function that updates the security group. Use the AWS-managed `AmazonIpSpaceChanged` topic in Amazon SNS as the trigger for the function.

An organization is preparing a CloudFormation template that uses the `Fn::Cidr` intrinsic function within a subnet resource to fetch available CIDR ranges. The CIDR block of the organization’s Amazon VPC is `192.168.0.0/24` and the intrinsic function must produce 6 CIDRs with a `/27`subnet mask.

Which of the following options provides the correct combination of parameter values that will yield the expected result?

- **`{ "Fn::Cidr" : [ "192.168.0.0/24", "6", "5" ]}`**
    
    **AWS CloudFormation** provides several built-in functions that help you manage your stacks. You can use intrinsic functions in your templates to assign values to properties that are not available until runtime.
    
    You can use intrinsic functions only in specific parts of a template. Currently, you can use these in resource properties, outputs, metadata attributes, and update policy attributes, as well as to conditionally create stack resources.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-22_09-41-32-bd5d5713e5fcc19f561fba4a0f0d29f7.png)
    
    The intrinsic function `Fn::Cidr` returns an array of CIDR address blocks. The number of CIDR blocks returned is dependent on the `count` parameter. This function has three parameters:
    
    **ipBlock** – The user-specified CIDR address block to be split into smaller CIDR blocks.
    
    **count** – The number of CIDRs to generate. Valid range is between 1 and 256.
    
    **cidrBits** – The number of subnet bits for the CIDR. For example, specifying a value “8” for this parameter will create a CIDR with a mask of “/24”.
    
    The subnet bits value is the inverse of the subnet mask. To calculate the required host bits for a given subnet bits number, subtract the subnet bits from 32 for IPv4 or 128 for IPv6. So if you want to specify a mask of /24 for the intrinsic function, you have to subtract 32 and 24, which will get you a value of 8.
    
    Hence, the correct answer is: **`{ "Fn::Cidr" : [ "192.168.0.0/24", "6", "5" ]}`**
    
    The option that says: **`{ "Fn::Cidr" : [ "192.168.0.0/24", "6", "27" ]}`** is incorrect because the third parameter signifies the number of subnet bits for the CIDR and not the netmask. You have to subtract the netmask (27) from 32 to get the correct value (32 – 27 = 5).
    
    The option that says: **`{ "Fn::Cidr" : [ "192.168.0.0/24", "5", "6" ]}`** is incorrect because the value of the 2nd and 3rd parameters are swapped. This will actually produce 5 CIDR Blocks with a /26 subnet mask each (32 – 6 = 26).
    
    The option that says: **`{ "Fn::Cidr" : [ "192.168.0.0/24", "27", "5"]}`** is incorrect. Although the value of the cidrBits parameter is correct, the count value (number of CIDRs to generate) of the 2nd parameter is wrong.
    
- `{ "Fn::Cidr" : [ "192.168.0.0/24", "5", "6" ]}`
- `{ "Fn::Cidr" : [ "192.168.0.0/24", "27", "5" ]}`
- `{ "Fn::Cidr" : [ "192.168.0.0/24", "6", "27" ]}`

A company has an internal MySQL database server that runs on an Amazon EC2 instance in the corporate VPC. The Network team must create a custom `db.tutorialsdojo.com` DNS record that will route the traffic to the instance in which the MySQL database is running. The database is only used by the application servers that are launched in the private subnet of the same VPC.

What should the team use to complete this task?

- **Private Hosted Zone**
    
    A hosted zone is a container for records, and records contain information about how you want to route traffic for a specific domain, such as tutorialsdojo.com and its subdomains (labs.tutorialsdojo.com, portal.tutorialsdojo.com). A hosted zone and the corresponding domain have the same name. There are two types of hosted zones:
    
    Public hosted zones – contain records that specify how you want to route traffic on the Internet.
    
    Private hosted zones – contain records that specify how you want to route traffic in an Amazon VPC.
    
    ![](https://media.tutorialsdojo.com/public/2020-07-02_09-19-14-b811a0888fab51759cc1f3764978c40e.png)
    
    A private hosted zone is a container that holds information about how you want Amazon Route 53 to respond to DNS queries for a domain and its subdomains within one or more VPCs that you create with the Amazon VPC service. Here’s how private hosted zones work:
    
    1. You create a private hosted zone, such as example.com, and specify the VPCs that you want to associate with the hosted zone.
    2. You create records in the hosted zone that determine how Route 53 responds to DNS queries for your domain and subdomains within and among your VPCs. For example, suppose you have a database server that runs on an EC2 instance in one of the VPCs that you associated with your private hosted zone. You create an A or AAAA record, such as db.tutorialsdojo.com, and you specify the IP address of the database server.
    3. When an application submits a DNS query for db.example.com, Route 53 returns the corresponding IP address. The application must also be running on an EC2 instance in one of the VPCs that you associated with the tutorialsdojo.com private hosted zone.
    4. The application uses the IP address that it got from Route 53 to establish a connection with the database server.
    
    If you want to route traffic for your domain on the internet, you use a Route 53 *public hosted* zone.
    
    Hence, the correct answer is: **Private Hosted Zone.**
    
    **Public Hosted Zone** is incorrect because this is primarily used if you want to route traffic for your domain on the public Internet. The scenario says that the database is only used by the application servers that are launched in the private subnet of the same VPC.
    
    **AWS Landing Zone** is incorrect because this service simply helps customers to quickly set up a secure, multi-account AWS environment based on AWS best practices.
    
    **Availability Zone** is incorrect because this is just a component of the AWS Global Infrastructure. It can have one or more discrete data centers with redundant power, networking, and connectivity in an AWS Region.
    
- Availability Zone
- Public Hosted Zone
- AWS Landing Zone

A company has several Amazon VPCs with Amazon Route 53 Resolvers and a DNS Firewall. The company needs to manually modify the Network Time Protocol (NTP) settings of its Amazon EC2 instances for testing purposes. The instances must use the Amazon Time Sync Service instead of an external NTP source.

The Amazon VPC must also be set to allow the Amazon Route 53 Resolver queries in the event that the Route 53 Resolver DNS Firewall is impaired, unresponsive, or not available in the zone. This approach favors high availability over security, which is what the company requires.

Which of the following options must the Network Engineer configure to satisfy the above requirements?

- Configure the `169.254.169.253` IP address in the NTP settings. Enable the “fail closed” option in the VPC settings in the Route 53 Resolver.
- Use the `169.254.169.254` IP address in the NTP settings. Keep the default “fail open” VPC settings in the Route 53 Resolver DNS Firewall.
- **Set the `169.254.169.123` IP address in the NTP settings. Enable the “fail open” option in the VPC settings in the Route 53 Resolver.**
    
    The DNS Firewall configuration for your VPC determines whether Route 53 Resolver allows queries through or blocks them during failures, for example, when DNS Firewall is impaired, unresponsive, or not available in the zone. Resolver enforces a VPC’s firewall configuration whenever you have one or more DNS Firewall rule groups associated with the VPC.
    
    ![](https://media.tutorialsdojo.com/amazon_dns_firewall_fail_open_fail_closed_vpc.jpg)
    
    You can configure a VPC to “fail open” or “fail closed”
    
    - By default, the failure mode is closed, which means that Resolver blocks any queries for which it doesn’t receive a reply from DNS Firewall and sends a SERVFAIL DNS response. This approach favors security over availability.
    - If you enable fail open, Resolver allows queries through if it doesn’t receive a reply from DNS Firewall. This approach favors availability over security.
    
    A consistent and accurate time reference is crucial for many server tasks and processes. Most system logs include a timestamp that you can use to determine when problems occur and in what order the events take place. If you use the AWS CLI or an AWS SDK to make requests from your instance, these tools sign requests on your behalf. If your instance’s date and time are not set correctly, the date in the signature may not match the date of the request, and AWS rejects the request. It is recommended that you use Coordinated Universal Time (UTC) for your Windows instances. However, you can use a different time zone if you want.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-11_07-29-24-2904275fdb4704e3646cddfe55c0e202.png)
    
    Amazon provides the Amazon Time Sync Service, which is accessible from all EC2 instances and is also used by other AWS services. Amazon recommends that you configure your instance to use the Amazon Time Sync Service. This service uses a fleet of satellite-connected and atomic reference clocks in each AWS Region to deliver accurate current time readings of the Coordinated Universal Time (UTC) global standard.
    
    The Amazon Time Sync Service automatically smooths any leap seconds that are added to UTC. This service is available at the `169.254.169.123` IP address for any instance running in a VPC, and your instance does not require Internet access to use it. Starting with the August 2018 release, Windows AMIs use the Amazon Time Sync Service by default.
    
    Hence, the correct answer is: **Set the `169.254.169.123` IP address in the NTP settings. Enable the “fail open” option in the VPC settings in the Route 53 Resolver.**
    
    The option that says: **Use the `169.254.169.254` IP address in the NTP settings. Keep the default “fail open” VPC settings in the Route 53 Resolver DNS Firewall** is incorrect because 169.254.169.254 is a link-local IP address for retrieving the instance metadata. It’s important to know that the default VPC settings in the Route 53 Resolver DNS Firewall is “fail closed” and not “fail open”, which is why this option is wrong.
    
    The option that says: **Modify the NTP settings to use the `127.0.0.1` IP address. Use the default “fail closed” VPC settings in the Route 53 Resolver DNS Firewall** is incorrect because the 127.0.0.1 IP is simply the loopback address for the local host and not for the NTP. it is true that the “failed closed” VPC setting is the default value, but the correct configuration for the Route 53 Resolver DNS Firewall must be “fail open” in this particular situation since the priority is availability rather than security.
    
    The option that says: **Configure the `169.254.169.253` IP address in the NTP settings. Enable the “fail closed” option in the VPC settings in the Route 53 Resolver** is incorrect because 169.254.169.253 is just the IP address of the Amazon-provided DNS server for the Route 53 Resolver. The “fail closed” option is primarily used if you want to focus on security rather than the availability of your network, which is the total opposite of what is asked in the scenario.
    
- Modify the NTP settings to use the `127.0.0.1` IP address. Use the default “fail closed” VPC settings in the Route 53 Resolver DNS Firewall.

An AWS Consultant is creating a CloudFormation template that automatically establishes a VPC peering connection between two VPCs. The requester VPC located at us-east-1 is owned by the company and the accepter VPC in us-west-1 belongs to another AWS account.

What should the Consultant do in order to implement this task?

- In the CloudFormation template, verify that the `AWS::EC2::Route` resource contains the necessary `AWS::EC2::SecurityGroup` and `AWS::EC2NetworkInterface` properties.
- **Create an `AWS::EC2::VPCPeeringConnection` resource in the template and set up a cross-account access role for the peering connection. Ensure that the `PeerRoleArn` and `PeerRegion` properties are properly supplied.**
    
    You can peer with a virtual private cloud (VPC) in another AWS account by using [AWS::EC2::VPCPeeringConnection](https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-ec2-vpcpeeringconnection.html). This creates a networking connection between two VPCs that enables you to route traffic between them so they can communicate as if they were within the same network. A VPC peering connection can help facilitate data access and data transfer.
    
    ![](https://media.tutorialsdojo.com/public/peering-intro-diagram.png)
    
    To establish a VPC peering connection, you need to authorize two separate AWS accounts within a single AWS CloudFormation stack. If you’re creating a VPC peering connection between VPCs in different accounts, make sure that you configure the PeerRoleArn property to pass your cross-account IAM role from your accepter account in your AWS CloudFormation template.
    
    Hence, the correct answer is: **Create an `AWS::EC2::VPCPeeringConnection` resource in the template and set up a cross-account access role for the peering connection. Ensure that the `PeerRoleArn` and `PeerRegion` properties are properly supplied.**
    
    The option that says: **Add an `AWS::VPC::VPCGatewayAttachment` resource in the CloudFormation template and set up a cross-account access role for the peering connection. Verify that the `PeerRoleArn` and `PeerRegion` properties are properly configured** is incorrect because the `AWS::VPC::VPCGatewayAttachment` entity is primarily used to attach an internet gateway, or a virtual private gateway to a VPC. You have to use the `AWS::EC2::VPCPeeringConnection` entity instead.
    
    The option that says: **In the CloudFormation template, verify that the `AWS::EC2::Route` resource contains the necessary `AWS::EC2::SecurityGroup` and `AWS::EC2NetworkInterface` properties**is incorrect because the `AWS::EC2::Route` entity is only used to create a specific route in the VPC. This option is not enough to have an automated way of establishing a VPC Peering connection.
    
    The option that says: **Set up an `AWS::EC2::VPCPeeringConnection` resource in the template and a Service Control Policy (SCP) for the peering connection** is incorrect because you have to set up a cross-account access role for the peering connection and not an SCP. You also have to ensure that the `PeerRoleArn` and `PeerRegion` properties are also supplied.
    
- Set up an `AWS::EC2::VPCPeeringConnection` resource in the template and a Service Control Policy (SCP) for the peering connection.
- Add an `AWS::VPC::VPCGatewayAttachment` resource in the CloudFormation template and set up a cross-account access role for the peering connection. Verify that the `PeerRoleArn`and `PeerRegion` properties are properly configured.

A company is using various AWS resources on its cloud architecture such as Amazon EC2 instances, S3 buckets, DynamoDB tables, RDS databases, and many others. There is a new policy that mandates all traffic between the VPC and other public AWS services to not leave the Amazon network. It should also block access from another AWS Region.

What type of VPC endpoint should you use for the Amazon S3 and DynamoDB?

- Regional API endpoint
- **Gateway Endpoint**
    
    A **VPC endpoint** enables you to privately connect your VPC to supported AWS services and VPC endpoint services powered by PrivateLink without requiring an Internet gateway, NAT device, VPN connection, or AWS Direct Connect connection. Instances in your VPC do not require public IP addresses to communicate with resources in the service. Traffic between your VPC and the other service does not leave the Amazon network.
    
    Endpoints are virtual devices. They are horizontally scaled, redundant, and highly available VPC components that allow communication between instances in your VPC and services without imposing availability risks or bandwidth constraints on your network traffic.
    
    ![](https://media.tutorialsdojo.com/amazon-s3-vpc-endpoint.PNG)
    
    There are two types of VPC endpoints:
    
    1. Interface endpoint
    2. Gateway endpoint
    
    An **interface endpoint** is an elastic network interface with a private IP address from the IP address range of your subnet that serves as an entry point for traffic destined to a supported service.
    
    ![](https://media.tutorialsdojo.com/Interface-vs-Gateway-VPC-Endpoint.png)
    
    Interface endpoints are powered by AWS PrivateLink, a technology that enables you to privately access services by using private IP addresses. AWS PrivateLink restricts all network traffic between your VPC and services to the Amazon network. You do not need an internet gateway, a NAT device, or a virtual private gateway.
    
    A **gateway endpoint** is a gateway that you specify as a target for a route in your route table for traffic destined to a supported AWS service. The following AWS services are supported:
    
    – Amazon S3
    
    – DynamoDB
    
    Gateway endpoints don’t allow access from another AWS Region while Interface endpoints allow access from a VPC in another AWS Region using VPC peering or AWS Transit Gateway. It is also cheaper to use gateway endpoints than interface endpoints.
    
    Hence, the correct answer is **Gateway Endpoint.**
    
    **Interface Endpoint** is incorrect. Although Amazon S3 and DynamoDB can use this type of VPC endpoint, it still fails to meet the requirement of blocking access requests from another AWS Region. By default, interface endpoints allow access to your resource from a VPC in another AWS Region using VPC peering or AWS Transit Gateway.
    
    The options: **Edge-optimized endpoint** and **Regional API endpoint** are both incorrect because these are types of API Gateway endpoints and not VPC Endpoints.
    
- Interface Endpoint
- Edge-optimized endpoint

A startup has a Wordpress application hosted in an Amazon EC2 instance with an Amazon RDS for MySQL database that is located within the same subnet. The Network team configured the custom inbound and outbound rules for the security groups and network ACLs. The QA team noticed the application can’t access the database. However, the static HTML files of the application can be successfully viewed via HTTP or HTTPS.

What must the Network team do to rectify this issue?

- Modify the outbound rules of the network ACL to allow port 1433 traffic to the RDS database.
- **Modify the outbound rules of the EC2 instance to allow port 3306 traffic to the RDS security group.**
    
    A **security group** acts as a virtual firewall for your instance to control inbound and outbound traffic. When you launch an instance in a VPC, you can assign up to five security groups to the instance. Security groups act at the instance level, not the subnet level. Therefore, each instance in a subnet in your VPC can be assigned to a different set of security groups.
    
    If you launch an instance using the Amazon EC2 API or a command-line tool and you don’t specify a security group, the instance is automatically assigned to the default security group for the VPC. If you launch an instance using the Amazon EC2 console, you have the option to create a new security group for the instance.
    
    For each security group, you add *rules* that control the inbound traffic to instances and a separate set of rules that control the outbound traffic.
    
    ![](https://media.tutorialsdojo.com/public/con-VPC-sec-grp.png)
    
    Security groups are stateful – if you send a request from your instance, the response traffic for that request is allowed to flow in regardless of inbound security group rules. Responses to allowed inbound traffic are allowed to flow out, regardless of outbound rules.
    
    By default, a security group includes an outbound rule that allows all outbound traffic. You can remove the rule and add outbound rules that allow specific outbound traffic only. If your security group has no outbound rules, no outbound traffic originating from your instance is allowed.
    
    Hence, the correct answer is: **Modify the outbound rules of the EC2 instance to allow port 3306 traffic to the RDS security group.**
    
    The option that says: **Modify the outbound rules of the network ACL to allow port 3306 traffic to the RDS database** is incorrect because the EC2 instance connects to the RDS database within the subnet. This means that configuring the network ACL is not required to access the database.
    
    The option that says: **Modify the outbound rules of the EC2 instance to allow port 1433 traffic to the RDS security group** is incorrect because port 1433 is used for MS SQL and not for MySQL.
    
    The option that says: **Modify the outbound rules of the network ACL to allow port 1433 traffic to the RDS database** is incorrect because the issue here is the restrictive outbound rule of the EC2 security group. Moreover, 1433 is the wrong port for MySQL since this is primarily used for MS SQL.
    
- Modify the outbound rules of the EC2 instance to allow port 1433 traffic to the RDS security group.
- Modify the outbound rules of the network ACL to allow port 3306 traffic to the RDS database.

A leading insurance firm decided to adopt a hybrid cloud infrastructure that will connect its on-premises data center to its VPC located in the US East (N. Virginia) Region. A 10 Gbps AWS Direct Connect (DX) connection was established between the firm’s data center and an AWS Direct Connect Location using a private virtual interface. An AWS Direct Connect Gateway and a Virtual Private Gateway were also associated with the virtual interface to allow access to multiple VPCs and other resources.

Which of the following can be done using this connection? (Select TWO.)

- Connect to any S3 bucket in any AWS Region.
- **Connect VPC resources such as EC2 instances or load balancers via a private IP address or endpoint.**
- Connect to multiple VPCs in the US East (N. Virginia) Region only.
- Connect to any S3 bucket in the US East (N. Virginia) Region only.
- **Connect to multiple VPCs in any AWS Region (except the AWS China Region).**
- **Evaluation**
    
    You can use an AWS Direct Connect gateway to connect your AWS Direct Connect connection over a private virtual interface to one or more VPCs in any account that is located in the same or different Regions. You associate a Direct Connect gateway with the virtual private gateway for the VPC. Then, you create a private virtual interface for your AWS Direct Connect connection to the Direct Connect gateway. You can attach multiple private virtual interfaces to your Direct Connect gateway.
    
    ![](https://media.tutorialsdojo.com/public/dx-gateway.png)
    
    To connect to your resources hosted in an Amazon Virtual Private Cloud (Amazon VPC) using their private IP addresses, use a private virtual interface. With a private virtual interface, you can:
    
    - Connect VPC resources (such as Amazon Elastic Compute Cloud (Amazon EC2) instances or load balancers) on your private IP address or endpoint.
    - Connect a private virtual interface to a DX gateway. Then, associate the DX gateway with one or more virtual private gateways in any AWS Region (except the AWS China Region).
    - Connect to multiple VPCs in any AWS Region (except the AWS China Region), because a virtual private gateway is associated with a single VPC.
    
    Hence, the correct answers are:
    
    - **Connect VPC resources such as EC2 instances or load balancers via a private IP address or endpoint.**
    - **Connect to multiple VPCs in any AWS Region (except the AWS China Region)**
    
    The option that says: **Connect to any S3 bucket in the US East (N. Virginia) Region only** is incorrect because a private virtual interface is primarily used to access VPC resources via private IP addresses and not AWS resources (such as Amazon S3) that are reachable by a public IP address.
    
    The option that says: **Connect to any S3 bucket in any AWS Region** is incorrect because just as mentioned in the previous option, a private virtual interface is mainly used to connect to a VPC and not to public resources (non-VPC services) such as Amazon S3.
    
    The option that says: **Connect to multiple VPCs in the US East (N. Virginia) Region only** is incorrect because, through the Direct Connect Gateway, you can connect to one or more VPCs that are located in the same or different Regions.
    

A company is in the process of integrating its on-premises data center with its AWS resources for its hybrid cloud infrastructure. The company has several workloads that leverage both VPCs and on-premises resources. An AWS Direct Connect connection has been established to connect the two environments.

The cloud resources in AWS need to resolve DNS records hosted on-premises, and conversely, these on-premises resources have to resolve the server names hosted on AWS. Resolving DNS queries between the on-premises resources and VPCs are done through Route 53 Resolver endpoints and conditional forwarding rules. The Resolver must block any DNS queries for which it doesn’t receive a reply from DNS Firewall and sends a `SERVFAIL` DNS response to prioritize network security over availability.

What should the Network Engineer do to satisfy the above requirement?

- Turn on the SiteLink feature in the AWS Direct Connect to build network segments inside AWS and attach these to separate Virtual routing and forwarding (VRF) contexts on the customer routers.
- Enable the “fail open” option of the DNS Firewall configuration for the Amazon VPC in the Amazon Route 53 Resolver console
- **Use the default failure mode of the DNS Firewall configuration for the Amazon VPC in the Amazon Route 53 Resolver console**
    
    The DNS Firewall configuration for your VPC determines whether Route 53 Resolver allows queries through or blocks them during failures, for example, when DNS Firewall is impaired, unresponsive, or not available in the zone. Resolver enforces a VPC’s firewall configuration whenever you have one or more DNS Firewall rule groups associated with the VPC.
    
    ![](https://media.tutorialsdojo.com/amazon_dns_firewall_fail_open_fail_closed_vpc.jpg)
    
    You can configure a VPC to “fail open” or “fail closed”
    
    - By default, the failure mode is closed, which means that Resolver blocks any queries for which it doesn’t receive a reply from DNS Firewall and sends a SERVFAIL DNS response. This approach favors security over availability.
    - If you enable fail open, Resolver allows queries through if it doesn’t receive a reply from DNS Firewall. This approach favors availability over security.
    
    Since the default failure mode in the DNS Firewall configuration of the Amazon Route 53 Resolver is “fail closed”, the correct answer is: **Use the default failure mode of the DNS Firewall configuration for the Amazon VPC in the Amazon Route 53 Resolver console**
    
    The option that says: **Launch a new AWS Network Firewall and leverage on the Network Firewall REST API to block any DNS queries which don’t have any reply from the DNS Firewall** is incorrect because the DNS firewall and the DNS Resolver Firewall are quite different. It’s not appropriate to use the Network Firewall REST API to block requests going through the DNS Resolver firewall.
    
    The option that says: **Turn on the SiteLink feature in the AWS Direct Connect to build network segments inside AWS and attach these to separate Virtual routing and forwarding (VRF) contexts on the customer routers** is incorrect because the SiteLink is simply a new feature of AWS Direct Connect (DX) which makes it easy to send data from one Direct Connect location to another, bypassing AWS Regions.
    
    The option that says: **Enable the “fail open” option of the DNS Firewall configuration for the Amazon VPC in the Amazon Route 53 Resolver console** is incorrect because the “fail open” configuration will cause the Route 53 Resolver to allow queries through even if it doesn’t receive a reply from DNS Firewall. This contradicts to the requirement of the scenario.
    
- Launch a new AWS Network Firewall and leverage on the Network Firewall REST API to block any DNS queries which don’t have any reply from the DNS Firewall.

A company has an extensive cloud network composed of multiple VPCs, VPN Connections, and Direct Connect gateways that are all connected in an AWS Transit Gateway. One of its VPCs has a public-facing enterprise application in the us-east-2 region. Management wants to create an internal version of the website that can only be accessed within the VPC. Requests coming from both external and internal clients must resolve to the same domain name.

What are the steps needed to meet the requirements?

- Launch a private hosted zone on Route 53 with the same name as the public hosted zone. Configure a Route 53 Resolver outbound endpoint for mapping internal queries to the website’s domain name. Set up a new Transit Gateway Connect peer for all the attachments of the existing AWS Transit Gateway and use the Generic Routing Encapsulation (GRE) tunnel protocol.
- **Set up a private hosted zone on Route 53 with the same name as the public hosted zone. Attach the private hosted zone to the VPC and add the necessary records for routing.**
    
    You can use Route 53 to configure split-view DNS, also known as split-horizon DNS. In split-view DNS, you use the same domain name (tutorialsdojo.com) for internal uses (accounting.tutorialsdojo.com) and external uses, such as your public website (www.tutorialsdojo.com). You might also want to use the same subdomain name internally and externally, but serve different content or require different authentication for internal and external users.
    
    ![](https://media.tutorialsdojo.com/route-53-private-hosted-zone.jpg)
    
    To configure split-view DNS, you perform the following steps:
    
    1. Create public and private hosted zones that have the same name. (Split-view DNS still works if you’re using another DNS service for the public hosted zone.)
    2. Associate one or more Amazon VPCs with the private hosted zone. Route 53 Resolver uses the private hosted zone to route DNS queries in the specified VPCs.
    3. Create records in each hosted zone. Records in the public hosted zone control how internet traffic is routed, and records in the private hosted zone control how traffic is routed in your Amazon VPCs.
    
    Hence, the correct answer is: **Set up a private hosted zone on Route 53 with the same name as the public hosted zone. Attach the private hosted zone to the VPC and add the necessary records for routing**
    
    The option that says: **Deploy a private hosted zone on Route 53 with the same name as the public hosted zone. Configure a Route 53 Resolver inbound endpoint for mapping internal queries to the website’s domain name. Enable Internet Group Management Protocol (IGMPv2) support in AWS Transit Gateway** is incorrect. This solution will only allow DNS records hosted on a remote network to be resolved from a VPC. Take note that the Internet Group Management Protocol (IGMPv2) is usually used for multicasting, which is not explicitly mentioned nor alluded to in the given scenario.
    
    The option that says: **Launch a private hosted zone on Route 53 with the same name as the public hosted zone. Configure a Route 53 Resolver outbound endpoint for mapping internal queries to the website’s domain name. Set up a new Transit Gateway Connect peer for all the attachments of the existing AWS Transit Gateway and use the Generic Routing Encapsulation (GRE) tunnel protocol** is incorrect. The use of a Resolver outbound endpoint in Amazon Route53 will only allow DNS queries originating from an on-premises network to be resolved. purpose of simplifying connections between separate networks. There’s also no need to use Generic Routing Encapsulation (GRE) tunnel protocol to meet the requirements since this protocol is commonly used if you have a variety of network layer protocols that you need to support. Creating a private hosted zone on Route 53 with the same name as the public hosted zone should suffice.
    
    The option that says: **Create a Transit Gateway Multicast Domain and add a new record with an IP-based routing policy. Set the VPC’s CIDR block as the source and the private website’s IP as the target in the multicast domain** is incorrect. Multicast is a communication protocol used for delivering a single stream of data to multiple receiving computers simultaneously. Although a Transit Gateway does supports multicast, the use of this feature in this scenario is not warranted since we only need to limit application traffic within the company’s network.
    
- Deploy a private hosted zone on Route 53 with the same name as the public hosted zone. Configure a Route 53 Resolver inbound endpoint for mapping internal queries to the website's domain name. Enable Internet Group Management Protocol (IGMPv2) support in AWS Transit Gateway.
- Create a Transit Gateway Multicast Domain and add a new record with an IP-based routing policy. Set the VPC's CIDR block as the source and the private website's IP as the target in the multicast domain.

A company currently has an Amazon Virtual Private Cloud (VPC) with a `10.0.0.0/16` CIDR block. It has two subnets with `10.0.0.0/17` and `10.0.128.0/17` CIDR blocks. There was a recent production issue where an Auto Scaling group cannot launch a new EC2 instance because all available IP addresses are already used up. In order to host more applications, the Network Engineer has been instructed to add more subnets to the VPC.

What should the Engineer do to complete this task?

- Enable Enhanced Networking in the VPC.
- You must create a new VPC since it is not possible to increase the size of the existing VPC. Set up a VPC Peering connection to integrate the two VPCs.
- **Associate secondary IPv4 CIDR blocks to the Amazon VPC.**
    
    A **virtual private cloud (VPC)** is a virtual network dedicated to your AWS account. It is logically isolated from other virtual networks in the AWS Cloud. You can launch your AWS resources, such as Amazon EC2 instances, into your VPC.
    
    When you create a VPC, you must specify a range of IPv4 addresses for the VPC in the form of a Classless Inter-Domain Routing (CIDR) block; for example, `10.0.0.0/16`. This is the primary CIDR block for your VPC.
    
    You can associate secondary IPv4 CIDR blocks with your VPC to increase its size. When you associate a CIDR block with your VPC, a route is automatically added to your VPC route tables to enable routing within the VPC (the destination is the CIDR block, and the target is local).
    
    ![](https://media.tutorialsdojo.com/public/vpc-multiple-cidrs.jpg)
    
    Take note that the scenario says that the VPC has a CIDR block of 10.0.0.0/16 which has two subnets with CIDR blocks of 10.0.0.0/17 and 10.0.128.0/17. These two subnets together have used up all available IP addresses of the VPC. This is why the staff is seeing an error whenever she tries to add a new subnet.
    
    Hence, the correct answer is: **Associate secondary IPv4 CIDR blocks to the Amazon VPC.**
    
    The option that says: **You must create a new VPC since it is not possible to increase the size of the existing VPC. Set up a VPC Peering connection to integrate the two VPCs** is incorrect because you can associate secondary CIDR blocks to the VPC to increase its size. Creating a new VPC is not required.
    
    The option that says: **Enable Enhanced Networking in the VPC** is incorrect because this feature is primarily used to improve the network performance of EC2 instances and not to increase the size of the VPC.
    
    The option that says: **Go to the VPC Console and manually increase the size of the existing CIDR block to `10.0.0.0/15`** is incorrect because you can’t manually increase the size of your existing CIDR block. Moreover, the allowed block size is between a `/28` netmask and `/16` netmask only.
    
- Go to the VPC Console and manually increase the size of the existing CIDR block to `10.0.0.0/15`.

An organization has recently established a private hosted zone using Amazon Route 53. To improve network monitoring, the Network Administrator has been instructed to configure health checks for the record sets within the zone which are used by the EC2 instances.

What should the Administrator do to meet these requirements?

- Set up a CloudWatch metric that checks the status of the `ErrorPortAllocation` metric. Configure a rule in CloudWatch Events to track the metric and then create a health check that is based on the data stream of the rule.
- Configure AWS Personal Health Dashboard in the AWS account that will automatically generate health checks for the record sets within the private hosted zone.
- **Create a CloudWatch metric that checks the status of the EC2 `StatusCheckFailed` metric. Using CloudWatch Alarms, add an alarm to the metric then create a health check that is based on the data stream for the alarm.**
    
    A **private hosted zone** is a container that holds information about how you want Amazon Route 53 to respond to DNS queries for a domain and its subdomains within one or more VPCs that you create with the Amazon VPC service.
    
    ![](https://media.tutorialsdojo.com/public/2020-07-02_07-03-19-78696778348f81060659f9846892d687.png)
    
    If you’re creating failover records in a private hosted zone, take note of the following:
    
    - Route 53 health checkers are outside the VPC. To check the health of an endpoint within a VPC by IP address, you must assign a public IP address to the instance in the VPC.
    - You can configure a health checker to check the health of an external resource that the instance relies on, such as a database server.
    - You can create a CloudWatch metric, associate an alarm with the metric, and then create a health check that is based on the data stream for the alarm. For example, you might create a CloudWatch metric that checks the status of the EC2 `StatusCheckFailed` metric, add an alarm to the metric and then create a health check that is based on the data stream for the alarm.
    
    Hence, the correct answer is: **Create a CloudWatch metric that checks the status of the EC2 `StatusCheckFailed` metric. Using CloudWatch Alarms, add an alarm to the metric then create a health check that is based on the data stream for the alarm.**
    
    The option that says: **Enable Amazon GuardDuty in the AWS account that will automatically generate health checks for the record sets within the private hosted zone** is incorrect because GuardDuty doesn’t generate any health checks for your account. Amazon GuardDuty only offers continuous monitoring of your AWS accounts and workloads to protect against malicious or unauthorized activities.
    
    The option that says: **Set up a CloudWatch metric that checks the status of the `ErrorPortAllocation`metric. Configure a rule in CloudWatch Events to track the metric and then create a health check that is based on the data stream of the rule** is incorrect because the `ErrorPortAllocation` metric simply shows the number of times the NAT gateway could not allocate a source port. This is not related to the private hosted zones in Amazon Route 53.
    
    The option that says: **Configure AWS Personal Health Dashboard in the AWS account that will automatically generate health checks for the record sets within the private hosted zone** is incorrect because AWS Personal Health Dashboard just provides alerts and remediation guidance when AWS is experiencing events that may impact you. It doesn’t generate health checks automatically.
    
- Enable Amazon GuardDuty in the AWS account that will automatically generate health checks for the record sets within the private hosted zone.

A Network Engineer has been tasked to set up an automated solution to check whether all Elastic IP addresses are properly attached to the Amazon EC2 instances. The solution must verify that the Elastic IP addresses are also in-use by the elastic network interfaces (ENIs).

How can the Engineer satisfy the above requirements?

- **Set up AWS Config to track all configuration changes of specific AWS resources. Use the EIP-attached AWS Config Managed Rule to check all the changes and allocations of the Elastic IP addresses.**
    
    **AWS Config** provides *AWS managed rules*, which are predefined, customizable rules that AWS Config uses to evaluate whether your AWS resources comply with common best practices. For example, you could use a managed rule to quickly start assessing whether your Amazon Elastic Block Store (Amazon EBS) volumes are encrypted or whether specific tags are applied to your resources. You can set up and activate these rules without writing the code to create an AWS Lambda function, which is required if you want to create custom rules. The AWS Config console guides you through the process of configuring and activating a managed rule. You can also use the AWS Command Line Interface or AWS Config API to pass the JSON code that defines your configuration of a managed rule.
    
    ![](https://media.tutorialsdojo.com/public/how-AWSconfig-works.png)
    
    You can customize the behavior of a managed rule to suit your needs. For example, you can define the rule’s scope to constrain which resources trigger an evaluation for the rule, such as EC2 instances or volumes. You can customize the rule’s parameters to define attributes that your resources must have to comply with the rule. For example, you can customize a parameter to specify that your security group should block incoming traffic to a specific port number.
    
    After you activate a rule, AWS Config compares your resources to the conditions of the rule. After this initial evaluation, AWS Config continues to run evaluations each time one is triggered. The evaluation triggers are defined as part of the rule, and they can include the following types:
    
    - **Configuration changes** – AWS Config triggers the evaluation when any resource that matches the rule’s scope changes in configuration. The evaluation runs after AWS Config sends a configuration item change notification.
    - **Periodic** – AWS Config runs evaluations for the rule at a frequency that you choose (for example, every 24 hours).
    
    The EIP-attached AWS Config Managed Rule checks whether all Elastic IP addresses that are allocated to a VPC are attached to EC2 instances or in-use elastic network interfaces (ENIs). The results might take up to 6 hours to become available after an evaluation occurs.
    
    Hence, the correct answer is: **Set up AWS Config to track all configuration changes of specific AWS resources. Use the EIP-attached AWS Config Managed Rule to check all the changes and allocations of the Elastic IP addresses.**
    
    The option that says: **Set up AWS Firewall Manager to automatically check all the changes and allocations of the Elastic IP addresses** is incorrect because this service is just a security management service that allows you to centrally configure and manage firewall rules across your accounts and applications in AWS Organization. It is not capable of automatically checking all the changes and allocations of the Elastic IP addresses in your VPC.
    
    The option that says: **Set up AWS Systems Manager to check all the changes and allocations of the Elastic IP addresses** is incorrect. This service just provides a unified user interface so you can view operational data from multiple AWS services and allows you to automate operational tasks across your AWS resources. It doesn’t cover the Elastic IP addresses that are attached to the Amazon EC2 instances.
    
    The option that says: **Set up AWS Shield Advanced to check all the changes and allocations of the Elastic IP addresses in the VPC** is incorrect because this service is primarily used to mitigate Denial of Service (DoS) attacks to your AWS resources and not for monitoring your Elastic IP addresses.
    
- Set up AWS Shield Advanced to check all the changes and allocations of the Elastic IP addresses in the VPC.
- Set up AWS Firewall Manager to automatically check all the changes and allocations of the Elastic IP addresses.
- Set up AWS Systems Manager to check all the changes and allocations of the Elastic IP addresses.

A Network Administrator noticed that there are developers who have been adding and removing rules to security groups of EC2 instances and Application Load Balancers in order to do their testing. These changes have caused security and unavailability issues on the web applications in the Production environment. The Administrator has been tasked to lock down certain permissions for the AWS account and automate auditing for any changes made to the security groups. A notification system is also required so that the Administrator can be notified if someone violated the rule.

Which of the following should the Administrator implement in this scenario?

- Use Amazon GuardDuty to automatically detect if there is a security group change, and associate an Amazon SES to allow notifications. Use Amazon CloudWatch to track and identify API calls from the developers.
- **Set up a new AWS Config rule to automatically detect if there is a security group change. Associate an Amazon SNS topic to the AWS Config rule for the notifications. Create restrictive IAM user policies to lock down the developer permissions. Enable AWS CloudTrail to track and identify API calls from the developers.**
    
    **AWS Config** is a service that enables you to assess, audit, and evaluate the configurations of your AWS resources. Config continuously monitors and records your AWS resource configurations and allows you to automate the evaluation of recorded configurations against desired configurations. With Config, you can review changes in configurations and relationships between AWS resources, dive into detailed resource configuration histories, and determine your overall compliance against the configurations specified in your internal guidelines. This enables you to simplify compliance auditing, security analysis, change management, and operational troubleshooting.
    
    ![](https://media.tutorialsdojo.com/product-page-diagram-Config_how-it-works.bd28728a9066c55d7ee69c0a655109001462e25b.png)
    
    You can configure AWS Config to stream configuration changes and notifications to an Amazon SNS topic. For example, when a resource is updated, you can get a notification sent to your email so that you can view the changes. You can also be notified when AWS Config evaluates your custom or managed rules against your resources.
    
    Hence, the correct answer is: **Set up a new AWS Config rule to automatically detect if there is a security group change. Associate an Amazon SNS topic to the AWS Config rule for the notifications. Create restrictive IAM user policies to lock down the developer permissions. Enable AWS CloudTrail to track and identify API calls from the developers.**
    
    The option that says: **Use Amazon GuardDuty to automatically detect if there is a security group change, and associate an Amazon SES to allow notifications. Use Amazon CloudWatch to track and identify API calls from the developers** is incorrect because Amazon GuardDuty is a threat detection service that is primarily used to continuously monitor malicious activity and unauthorized behavior in your AWS accounts and workloads. In this scenario, you have to use Amazon SNS instead of Amazon SES since this is just a cost-effective email service. Moreover, Amazon CloudWatch is not capable of tracking all API calls made by the developers. You have to enable AWS CloudTrail in order to satisfy the requirements.
    
    The option that says: **Create a new AWS Config rule to automatically detect if there is a security group change. Associate an Amazon SES to the AWS Config rule for the notifications. Create restrictive IAM user policies to lock down the developer permissions. Use AWS X-Ray to track all API calls made by the developers** is incorrect because Amazon SES is an email service and not a notification service. Therefore, you have to use Amazon SNS to send out notifications from AWS Config. For tracking all API calls, AWS X-Ray is not a suitable service to use since it is meant to be utilized for debugging your application. AWS CloudTrail must be used to properly track all API calls made by the developers.
    
    The option that says: **Use AWS Shield to automatically detect if there is a security group change, and associate an Amazon SNS Topic to allow notifications. Configure Amazon Connect to track and identify all API calls that the developers are using** is incorrect because AWS Shield is just a managed Distributed Denial of Service (DDoS) protection service that safeguards applications running on AWS. Amazon Connect is not relevant for this scenario since it is just an easy-to-use omnichannel cloud contact center that helps companies provide superior customer service at a lower cost.
    
- Create a new AWS Config rule to automatically detect if there is a security group change. Associate an Amazon SES to the AWS Config rule for the notifications. Create restrictive IAM user policies to lock down the developer permissions. Use AWS X-Ray to track all API calls made by the developers.
- Use AWS Shield to automatically detect if there is a security group change, and associate an Amazon SNS Topic to allow notifications. Configure Amazon Connect to track and identify all API calls that the developers are using.

A Network Engineer recently launched a private hosted zone to control how Amazon Route 53 routes traffic to the company domain and subdomains within one or more VPCs. The company is also using a 3rd party application hosted in a VPC that is owned by an external AWS account. The Engineer has been tasked to integrate the external VPC to the hosted zone of the company.

What should the Engineer do to complete this task?

- Associating a VPC from an external AWS account to a private hosted zone is not possible. As a workaround, convert the existing private hosted zone to a public type. Associate the external VPC to the new public hosted zone.
- Establish an AWS Direct Connect connection between the primary VPC of the company's AWS account to the external VPC of the 3rd party application. Associate the external VPC to the private hosted zone.
- **Use the `CreateVPCAssociationAuthorization` action of the Amazon Route 53 API to authorize the association of the VPC in the external AWS account. Associate the external VPC to the private hosted zone.**
    
    A hosted zone is a container for records, and records contain information about how you want to route traffic for a specific domain, such as tutorialsdojo.com and its subdomains (labs.tutorialsdojo.com, portal.tutorialsdojo.com). A hosted zone and the corresponding domain have the same name. There are two types of hosted zones:
    
    - Public hosted zones – contain records that specify how you want to route traffic on the Internet.
    - Private hosted zones – contain records that specify how you want to route traffic in an Amazon VPC.
    
    ![](https://media.tutorialsdojo.com/public/2020-06-26_08-39-26-1a3561c747fd4bfb2bd5483b8053d46c.png)
    
    If you want to associate a VPC that you created with one AWS account with a private hosted zone that you created with a different account, perform the following procedure:
    
    1. Using the account that created the hosted zone, authorize the association of the VPC with the private hosted zone by using one of the following methods:
    
    - Using the AWS CLI via the *create-vpc-association-authorization* command.
    - Using AWS SDK or AWS Tools for Windows PowerShell
    - Using Amazon Route 53 API via the *CreateVPCAssociationAuthorization* action.
    
    2. Using the account that created the VPC, associate the VPC with the hosted zone. As with authorizing the association, you can use the AWS SDK, Tools for Windows PowerShell, the AWS CLI, or the Route 53 API. If you’re using the API, use the AssociateVPCWithHostedZone action.
    
    3. (Optional but recommended step) – Delete the authorization to associate the VPC with the hosted zone. Deleting the authorization does not affect the association, it just prevents you from reassociating the VPC with the hosted zone in the future. If you want to reassociate the VPC with the hosted zone, you’ll need to repeat steps 1 and 2 of this procedure.
    
    When associating a VPC from an external AWS account, you have to be mindful of the following:
    
    - If you want to associate multiple VPCs that you created with one account with a hosted zone that you created with a different account, you must submit one authorization request for each VPC.
    - When you authorize the association, you must specify the hosted zone ID, so the private hosted zone must already exist.
    - You can’t use the Route 53 console either to authorize the association of a VPC with a private hosted zone or to make the association.
    
    Hence, the correct answer is: **Use the `CreateVPCAssociationAuthorization` action of the Amazon Route 53 API to authorize the association of the VPC in the external AWS account. Associate the external VPC to the private hosted zone.**
    
    The option that says: **Associating a VPC from an external AWS account to a private hosted zone is not possible. As a workaround, convert the existing private hosted zone to a public type. Associate the external VPC to the new public hosted zone** is incorrect because you can actually associate a VPC from an external AWS account to your private hosted zone. There is no need to convert the hosted zone to a public type.
    
    The option that says: **Establish an AWS Direct Connect connection between the primary VPC of the company’s AWS account to the external VPC of the 3rd party application. Associate the external VPC to the private hosted zone** is incorrect because AWS Direct Connect is primarily used to integrate your on-premises network to your VPC, and not to connect your own VPC to a VPC owned by another AWS account.
    
    The option that says: **Establish a VPC Peering between the company’s VPC and the external VPC. Associate the external VPC to the private hosted zone** is incorrect because you can’t authorize the association of an external VPC to your private hosted zone just by using VPC Peering. You have to use the *create-vpc-association-authorization* AWS CLI command or the *CreateVPCAssociationAuthorization* action of the Amazon Route 53 API. Alternatively, you can also use AWS SDK or AWS Tools for Windows PowerShell to complete the authorization.
    
- Establish a VPC Peering between the company's VPC and the external VPC. Associate the external VPC to the private hosted zone.

A telecommunications company operates a secure Amazon Virtual Private Cloud (Amazon VPC) and must ensure that only authorized public IP addresses can be accessed from the application subnet. A network administrator has modified the route table linked to the application’s subnet to route traffic toward these public IP addresses through an internet gateway.

To enhance security, the company wants to implement email notifications whenever a user modifies the route table by adding a default route (0.0.0.0/0 or ::/0) that directs all outbound traffic through the internet gateway. Additionally, the company wants to ensure that only authorized AWS Identity and Access Management (IAM) users can modify the route table and receive an alert if an unauthorized user attempts to make changes.

Which approach will meet this requirement with minimal operational effort?

- Enable AWS CloudTrail to log all modifications to the route table. Create an Amazon EventBridge rule that filters events for unauthorized users attempting to modify the route table. Use Amazon SES to send an email notification when a violation is detected.
- Use an AWS Config rule with the `internet-gateway-authorized-vpc-only` managed rule to monitor route table configurations. Create an Amazon EventBridge rule that triggers when a violation occurs and forwards the event to an Amazon SNS topic to send an email notification.
- Configure an AWS Lambda function triggered by Amazon EC2 CreateRoute API calls. The function checks for unrestricted routes (0.0.0.0/0 or ::/0), pointing to the internet gateway and sends an email notification via Amazon SES when such a route is added.
- **Set up an AWS Config rule using the `no-unrestricted-route-to-igw` managed rule to automatically detect unauthorized default routes. Configure an Amazon EventBridge rule to capture the compliance event and forward it to an Amazon SNS topic for email notifications.**
    
    **AWS Config** is a service that enables you to assess, audit, and evaluate the configurations of your AWS resources. It continuously monitors and records your AWS resource configurations, allowing you to automate the evaluation of recorded configurations against desired configurations. This helps in ensuring compliance with internal policies and regulatory standards.
    
    ![no-unrestricted-route-to-igw manage rule](https://media.tutorialsdojo.com/public/TD-no-unrestricted-route-to-igw-02-13-2025.png)
    
    One of the managed rules provided by AWS Config is the `no-unrestricted-route-to-igw` rule. This rule checks whether public routes in the route tables of your Amazon Virtual Private Cloud (VPC)  point to an Internet Gateway (IGW). Specifically, it identifies routes with destination CIDR blocks of ‘0.0.0.0/0’ or ‘::/0’ that direct traffic to an IGW, which could potentially expose your resources to the public internet. By using this rule, you can ensure that your VPC’s route tables do not have unrestricted routes to an IGW, thereby enhancing the security of your network architecture.
    
    To automate the response to compliance changes detected by AWS Config, you can integrate it with Amazon EventBridge. EventBridge allows you to set up rules that capture AWS Config compliance events and trigger specific actions, such as sending notifications or invoking AWS Lambda functions. For instance, you can create an EventBridge rule that listens for compliance changes related to `no-unrestricted-route-to-igw`and forwards these events to an Amazon Simple Notification Service (SNS) topic. This setup enables you to receive email notifications whenever a non-compliant configuration is detected, allowing for prompt remediation actions.
    
    Hence, the correct answer is: **Set up an AWS Config rule using the `no-unrestricted-route-to-igw`managed rule to automatically detect unauthorized default routes. Configure an Amazon EventBridge rule to capture the compliance event and forward it to an Amazon SNS topic for email notifications.**
    
    The option that says: **Use an AWS Config rule with the `internet-gateway-authorized-vpc-only`managed rule to monitor route table configurations. Create an Amazon EventBridge rule that triggers when a violation occurs and forwards the event to an Amazon SNS topic to send an email notification** is incorrect. While this option leverages AWS Config and EventBridge effectively, the `internet-gateway-authorized-vpc-only` rule is not tailored to the specific use case of detecting unauthorized default routes (0.0.0.0/0 or ::/0) pointing to an internet gateway. This rule only intends to ensure that an internet gateway is only used in authorized VPCs, not specifically for enforcing restrictions on default routes.
    
    The option that says: **Configure an AWS Lambda function triggered by Amazon EC2 CreateRoute API calls. The function checks for unrestricted routes (0.0.0.0/0 or ::/0), pointing to the internet gateway and sends an email notification via Amazon SES when such a route is added** is incorrect. Although it is feasible, this solution primarily requires manual configuration and custom development for every route modification, making it less efficient than using a managed rule like `no-unrestricted-route-to-igw` provided by AWS Config. Additionally, it introduces complexity in monitoring, handling API calls, and manually sending notifications via Amazon SES.
    
    The option that says: **Enable AWS CloudTrail to log all modifications to the route table. Create an Amazon EventBridge rule that filters events for unauthorized users attempting to modify the route table. Use Amazon SES to send an email notification when a violation is detected** is incorrect. While AWS CloudTrail is an excellent service for logging and auditing API calls made to your AWS resources, it does not offer built-in rules for detecting specific configuration violations, such as unauthorized routes pointing to an internet gateway. This approach would require manual filtering of CloudTrail logs and custom logic to identify unauthorized route modifications, adding operational overhead.