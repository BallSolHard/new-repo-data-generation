import type { ReferenceQuestionSet } from '@/lib/types/reference-question';

/**
 * High-quality reference questions for AWS SAA-C03.
 * These serve as few-shot examples injected into LLM prompts to calibrate
 * question style, difficulty, option phrasing, and explanation depth.
 *
 * 3-5 questions per domain, covering MCQ and multiple-select types.
 */
export const saaC03Samples: ReferenceQuestionSet = {
  certificationCode: 'SAA-C03',
  questions: [
    // ─── Domain 1: Design Secure Architectures ───
    {
      id: 'ref-saa-c03-d1-001',
      certificationCode: 'SAA-C03',
      domainId: 'domain-1',
      text: 'A company is migrating a critical application to AWS. The security team requires that all API calls across multiple AWS accounts are logged and stored in a centralized location for audit purposes. The logs must be tamper-proof and retained for 7 years. Which combination of steps meets these requirements?',
      type: 'multiple',
      options: [
        'Enable AWS CloudTrail in each account and configure it to deliver logs to a centralized S3 bucket in a dedicated logging account',
        'Apply an S3 Object Lock policy in compliance mode on the centralized logging bucket with a 7-year retention period',
        'Enable Amazon CloudWatch Logs in each account and stream all logs to the centralized account using cross-account subscriptions',
        'Create an IAM policy that denies s3:DeleteObject and s3:PutBucketPolicy on the logging bucket for all accounts except the root account',
      ],
      correctAnswer: [0, 1],
      explanation: 'AWS CloudTrail provides a complete audit trail of API calls across AWS accounts. By delivering trail logs to a centralized S3 bucket in a dedicated logging account, you consolidate audit data. S3 Object Lock in compliance mode ensures logs cannot be deleted or overwritten during the retention period, making them tamper-proof. CloudWatch Logs (Option C) is for application and system logs, not API call auditing. While the IAM deny policy (Option D) adds protection, it can be modified by administrators and does not provide the same legal-hold guarantee as S3 Object Lock compliance mode.',
      difficulty: 'intermediate',
      tags: ['cloudtrail', 's3-object-lock', 'multi-account', 'audit', 'compliance'],
      source: 'custom',
      certTier: 'associate',
      genMode: 'simulation',
    },
    {
      id: 'ref-saa-c03-d1-002',
      certificationCode: 'SAA-C03',
      domainId: 'domain-1',
      text: 'A solutions architect needs to design a secure architecture for a web application running on Amazon EC2 instances in a private subnet. The application must access Amazon DynamoDB and Amazon S3 without sending traffic over the public internet. Which solution meets this requirement with the LEAST operational overhead?',
      type: 'mcq',
      options: [
        'Create a NAT gateway in a public subnet and update the private subnet route table to route DynamoDB and S3 traffic through it',
        'Create gateway VPC endpoints for DynamoDB and S3, and update the private subnet route table to use the endpoints',
        'Configure an AWS Site-to-Site VPN connection to route traffic to DynamoDB and S3 through the corporate data center',
        'Deploy an internet gateway and assign Elastic IP addresses to the EC2 instances to allow direct access to DynamoDB and S3',
      ],
      correctAnswer: '{1}',
      explanation: 'Gateway VPC endpoints for S3 and DynamoDB allow instances in private subnets to access these services without traversing the public internet, NAT gateway, or VPN. Gateway endpoints are free and are added as route table entries — they require no additional infrastructure to manage. A NAT gateway (Option A) would work but adds cost and still routes through the AWS public network edge. Site-to-Site VPN (Option C) adds unnecessary complexity and latency. An internet gateway with Elastic IPs (Option D) would expose the instances to the internet, violating the private subnet requirement.',
      difficulty: 'intermediate',
      tags: ['vpc-endpoints', 'dynamodb', 's3', 'private-subnet', 'security'],
      source: 'custom',
      certTier: 'associate',
      genMode: 'drill',
    },
    {
      id: 'ref-saa-c03-d1-003',
      certificationCode: 'SAA-C03',
      domainId: 'domain-1',
      text: 'A company stores sensitive customer data in Amazon S3. A compliance requirement mandates that all data must be encrypted at rest using keys managed by the company, with the ability to rotate keys annually and audit key usage. Which encryption approach should the solutions architect recommend?',
      type: 'mcq',
      options: [
        'Use SSE-S3 encryption with Amazon S3-managed keys and enable automatic key rotation',
        'Use SSE-KMS encryption with an AWS KMS customer managed key (CMK) and enable automatic annual key rotation',
        'Use client-side encryption with keys stored in AWS Secrets Manager, implementing manual key rotation',
        'Use SSE-C encryption with customer-provided keys, uploading new keys annually for rotation',
      ],
      correctAnswer: '{1}',
      explanation: 'SSE-KMS with a customer managed key (CMK) satisfies all requirements: the company manages the key through KMS (key policy control), AWS KMS supports automatic annual key rotation, and all key usage is logged in AWS CloudTrail for auditing. SSE-S3 (Option A) uses AWS-managed keys — the company has no control or visibility over key management. Client-side encryption with Secrets Manager (Option C) works but introduces significant operational overhead for key management and rotation. SSE-C (Option D) requires the company to provide and manage keys with every request, creating substantial operational burden and no built-in rotation.',
      difficulty: 'intermediate',
      tags: ['s3', 'encryption', 'kms', 'cmk', 'key-rotation', 'compliance'],
      source: 'custom',
      certTier: 'associate',
      genMode: 'drill',
    },

    // ─── Domain 2: Design Resilient Architectures ───
    {
      id: 'ref-saa-c03-d2-001',
      certificationCode: 'SAA-C03',
      domainId: 'domain-2',
      text: 'A company runs a critical e-commerce platform that experiences unpredictable traffic spikes during flash sales. The architecture uses Amazon EC2 instances behind an Application Load Balancer with an Amazon RDS MySQL database. During peak events, database read latency increases significantly, causing timeouts. Which architecture change provides the MOST improvement for read performance during traffic spikes?',
      type: 'mcq',
      options: [
        'Migrate from RDS MySQL to Amazon Aurora MySQL and enable Aurora Auto Scaling for read replicas',
        'Implement Amazon ElastiCache for Redis as a caching layer between the application and the database',
        'Upgrade the RDS instance to a larger instance type with Provisioned IOPS (io1) storage',
        'Enable RDS Multi-AZ deployment to distribute read traffic across the primary and standby instances',
      ],
      correctAnswer: '{1}',
      explanation: 'ElastiCache for Redis provides sub-millisecond response times for frequently accessed data, dramatically reducing read load on the database during traffic spikes. By caching query results, session data, and frequently accessed product information, the majority of read requests never reach the database. Aurora with Auto Scaling read replicas (Option A) improves read capacity but replicas take minutes to provision and still have higher latency than in-memory caching. Upgrading instance types (Option C) has a ceiling and requires downtime. Multi-AZ (Option D) is for high availability — the standby instance does NOT serve read traffic.',
      difficulty: 'intermediate',
      tags: ['elasticache', 'redis', 'caching', 'rds', 'performance', 'scaling'],
      source: 'custom',
      certTier: 'associate',
      genMode: 'simulation',
    },
    {
      id: 'ref-saa-c03-d2-002',
      certificationCode: 'SAA-C03',
      domainId: 'domain-2',
      text: 'A company needs to process customer order events in real-time. The system must handle sudden spikes of up to 10x normal traffic without losing any messages. Order processing takes 2-5 seconds per message. The current synchronous processing causes failures during high-traffic periods. Which architecture provides the MOST reliable solution?',
      type: 'mcq',
      options: [
        'Use Amazon API Gateway to receive orders, invoke an AWS Lambda function synchronously to process each order, and configure Lambda reserved concurrency to handle peak load',
        'Use Amazon API Gateway to receive orders, send messages to an Amazon SQS queue, and process messages using an Auto Scaling group of EC2 instances polling the queue',
        'Use an Application Load Balancer with a target group of EC2 instances in an Auto Scaling group, scaling based on CPU utilization',
        'Use Amazon Kinesis Data Streams to ingest orders and process them with AWS Lambda configured with the Kinesis trigger',
      ],
      correctAnswer: '{1}',
      explanation: 'SQS provides a fully managed message buffer that absorbs traffic spikes without losing messages. The queue decouples ingestion from processing — messages persist until successfully processed. EC2 instances in an Auto Scaling group can scale based on the ApproximateNumberOfMessagesVisible metric, ensuring processing capacity matches demand. Lambda with synchronous invocation (Option A) can hit concurrency limits during 10x spikes and has a 15-minute timeout constraint. ALB with CPU-based scaling (Option C) reacts slowly to sudden spikes and may drop requests during scale-out. Kinesis (Option D) requires shard capacity planning and doesn\'t provide the same at-least-once delivery guarantees for individual message processing that SQS offers.',
      difficulty: 'intermediate',
      tags: ['sqs', 'decoupling', 'auto-scaling', 'resilience', 'event-driven'],
      source: 'custom',
      certTier: 'associate',
      genMode: 'simulation',
    },
    {
      id: 'ref-saa-c03-d2-003',
      certificationCode: 'SAA-C03',
      domainId: 'domain-2',
      text: 'A company requires a disaster recovery strategy for a business-critical application currently running in us-east-1. The application uses Amazon RDS PostgreSQL and Amazon S3. The recovery time objective (RTO) is 1 hour, and the recovery point objective (RPO) is 15 minutes. Which disaster recovery strategy meets these requirements with the LOWEST cost?',
      type: 'mcq',
      options: [
        'Multi-site active/active: Deploy the full application stack in us-west-2 with Amazon Route 53 active-active routing',
        'Warm standby: Run a scaled-down version of the application in us-west-2 with an RDS cross-Region read replica and S3 cross-Region replication',
        'Pilot light: Maintain an RDS cross-Region read replica in us-west-2 with S3 cross-Region replication, and pre-configured but stopped EC2 instances',
        'Backup and restore: Use AWS Backup to create automated backups of RDS and S3 to us-west-2 and restore when needed',
      ],
      correctAnswer: '{2}',
      explanation: 'Pilot light meets the 1-hour RTO and 15-minute RPO requirements at the lowest cost. The RDS cross-Region read replica provides near-real-time data replication (well within the 15-minute RPO). S3 cross-Region replication keeps object data synchronized. During a disaster, the read replica is promoted to primary (takes minutes), and pre-configured EC2 instances are started and scaled (achievable within 1 hour). Multi-site active/active (Option A) exceeds the requirements and is the most expensive — running full infrastructure in two regions. Warm standby (Option B) meets requirements but costs more than pilot light since it runs a scaled-down application continuously. Backup and restore (Option D) is cheapest but cannot meet a 15-minute RPO since RDS backup frequency and restore times would exceed this.',
      difficulty: 'advanced',
      tags: ['disaster-recovery', 'pilot-light', 'rds-replica', 'cross-region', 'rto', 'rpo'],
      source: 'custom',
      certTier: 'associate',
      genMode: 'simulation',
    },

    // ─── Domain 3: Design High-Performing Architectures ───
    {
      id: 'ref-saa-c03-d3-001',
      certificationCode: 'SAA-C03',
      domainId: 'domain-3',
      text: 'A media company stores 50 TB of video assets in Amazon S3 Standard. Analytics show that videos older than 30 days are accessed infrequently (fewer than 2 times per month), and videos older than 1 year are rarely accessed but must remain available within 12 hours when requested. Which S3 storage strategy minimizes costs while meeting access requirements?',
      type: 'mcq',
      options: [
        'Create an S3 Lifecycle policy to transition objects to S3 Standard-IA after 30 days, and to S3 Glacier Flexible Retrieval after 365 days',
        'Create an S3 Lifecycle policy to transition objects to S3 One Zone-IA after 30 days, and to S3 Glacier Deep Archive after 365 days',
        'Enable S3 Intelligent-Tiering on the bucket and let AWS automatically move objects between access tiers',
        'Create an S3 Lifecycle policy to transition objects to S3 Standard-IA after 30 days, and to S3 Glacier Deep Archive after 365 days',
      ],
      correctAnswer: '{0}',
      explanation: 'S3 Standard-IA is appropriate for infrequently accessed data that still needs millisecond access. After 30 days with reduced access patterns, this saves ~45% compared to S3 Standard. S3 Glacier Flexible Retrieval for data older than 1 year provides retrieval within 3-5 hours (well within the 12-hour requirement) at ~90% savings compared to Standard. One Zone-IA (Option B) risks data loss if an AZ fails — inappropriate for 50 TB of video assets. S3 Glacier Deep Archive (Options B, D) has a minimum retrieval time of 12 hours with standard retrieval, which does not reliably meet the 12-hour availability requirement (it could exceed it). Intelligent-Tiering (Option C) works but incurs per-object monitoring fees on 50 TB of data, which adds significant cost compared to a well-defined lifecycle policy.',
      difficulty: 'intermediate',
      tags: ['s3', 'storage-classes', 'lifecycle-policy', 'cost-optimization', 'glacier'],
      source: 'custom',
      certTier: 'associate',
      genMode: 'simulation',
    },
    {
      id: 'ref-saa-c03-d3-002',
      certificationCode: 'SAA-C03',
      domainId: 'domain-3',
      text: 'A company is migrating an on-premises Oracle database to AWS. The database is 20 TB, has complex stored procedures, and the application team needs to minimize code changes. The application requires low-latency reads with up to 5 read replicas. Which migration approach should the solutions architect recommend?',
      type: 'mcq',
      options: [
        'Use AWS Database Migration Service (DMS) to migrate to Amazon DynamoDB and refactor the application to use the DynamoDB API',
        'Use AWS DMS with the Schema Conversion Tool (SCT) to migrate to Amazon Aurora PostgreSQL, then create Aurora read replicas',
        'Use AWS DMS to migrate to Amazon RDS for Oracle with Multi-AZ deployment and create up to 5 RDS read replicas',
        'Use a native Oracle Data Pump export and import into Amazon RDS for Oracle, then create Oracle read replicas',
      ],
      correctAnswer: '{2}',
      explanation: 'Since the requirement is to minimize code changes, migrating to RDS for Oracle maintains full compatibility with existing stored procedures and application code (homogeneous migration). DMS handles the data migration with minimal downtime. Multi-AZ provides high availability, and RDS Oracle supports up to 5 read replicas for read scaling. DynamoDB (Option A) requires a complete application rewrite — it is a NoSQL service incompatible with Oracle stored procedures. Aurora PostgreSQL (Option B) is a different database engine that would require schema conversion and stored procedure rewriting, contradicting the minimize-code-changes requirement. Oracle Data Pump (Option D) works but requires extended downtime for a 20 TB database compared to DMS\'s continuous replication approach.',
      difficulty: 'intermediate',
      tags: ['database-migration', 'dms', 'rds-oracle', 'read-replicas', 'homogeneous-migration'],
      source: 'custom',
      certTier: 'associate',
      genMode: 'simulation',
    },
    {
      id: 'ref-saa-c03-d3-003',
      certificationCode: 'SAA-C03',
      domainId: 'domain-3',
      text: 'A global SaaS company needs to deliver its web application to users in North America, Europe, and Asia with the lowest possible latency. The application has both static assets (JavaScript, CSS, images) and dynamic API responses that are user-specific. Which combination of services should the solutions architect use? (Select TWO.)',
      type: 'multiple',
      options: [
        'Use Amazon CloudFront to cache static assets at edge locations with a long TTL, and configure dynamic content with a short TTL using cache policies based on headers and cookies',
        'Deploy identical application stacks in us-east-1, eu-west-1, and ap-southeast-1, and use Amazon Route 53 latency-based routing to direct users to the nearest Region',
        'Use AWS Global Accelerator to route all traffic to the nearest AWS edge location and proxy it to the origin servers',
        'Deploy the application in a single Region and use Amazon ElastiCache Global Datastore to replicate session data across Regions',
      ],
      correctAnswer: [0, 1],
      explanation: 'CloudFront (Option A) handles static content caching at 400+ edge locations globally, providing sub-10ms latency for cached assets. For dynamic/user-specific content, CloudFront can forward relevant headers and cookies to the origin while still providing TCP optimization and TLS termination at the edge. Multi-Region deployment with Route 53 latency-based routing (Option B) ensures dynamic API requests are served from the closest Region, minimizing round-trip time for user-specific data. Together, these provide the optimal static + dynamic content delivery strategy. Global Accelerator (Option C) improves routing to origins but does not cache content — it is better suited for non-HTTP workloads or TCP/UDP traffic. ElastiCache Global Datastore (Option D) replicates session data but does not address the core latency issue of serving the application from a distant Region.',
      difficulty: 'advanced',
      tags: ['cloudfront', 'route53', 'multi-region', 'latency', 'global-delivery', 'cdn'],
      source: 'custom',
      certTier: 'associate',
      genMode: 'simulation',
    },

    // ─── Domain 4: Design Cost-Optimized Architectures ───
    {
      id: 'ref-saa-c03-d4-001',
      certificationCode: 'SAA-C03',
      domainId: 'domain-4',
      text: 'A startup runs a batch data processing pipeline that executes every night between 2:00 AM and 6:00 AM UTC. The pipeline uses 20 Amazon EC2 instances (c5.2xlarge) that are only needed during this window. The workload is fault-tolerant and can restart failed tasks. The company wants to minimize compute costs. Which purchasing option should the solutions architect recommend?',
      type: 'mcq',
      options: [
        'Purchase 20 EC2 Reserved Instances (1-year, All Upfront) for c5.2xlarge to get the maximum discount',
        'Use EC2 Spot Instances with a Spot Fleet configured across multiple instance types and Availability Zones',
        'Use EC2 On-Demand Instances and create a scheduled scaling policy in Auto Scaling to launch instances at 2:00 AM and terminate at 6:00 AM',
        'Purchase 20 EC2 Savings Plans (1-year) for the c5 instance family to cover the nightly compute usage',
      ],
      correctAnswer: '{1}',
      explanation: 'Spot Instances provide up to 90% discount compared to On-Demand pricing. Since the workload is fault-tolerant and can restart failed tasks, Spot Instances are ideal. Using a Spot Fleet across multiple instance types and AZs maximizes availability and minimizes interruptions. Reserved Instances (Option A) require paying for 24/7 capacity when the workload only runs 4 hours — this wastes 83% of the reservation. On-Demand with scheduled scaling (Option C) works but is the most expensive compute option. Savings Plans (Option D) commit to a consistent hourly spend — since the workload only runs 4 hours per day, you would either over-commit (paying for unused hours) or under-commit (not getting full savings on the 4 active hours).',
      difficulty: 'intermediate',
      tags: ['spot-instances', 'cost-optimization', 'batch-processing', 'ec2-pricing'],
      source: 'custom',
      certTier: 'associate',
      genMode: 'simulation',
    },
    {
      id: 'ref-saa-c03-d4-002',
      certificationCode: 'SAA-C03',
      domainId: 'domain-4',
      text: 'A company has a three-tier web application with a fleet of EC2 instances in private subnets that need to download software updates from the internet. The current architecture uses a NAT gateway, which is generating significant data processing charges (over 500 GB per month). Which approach reduces NAT gateway costs while maintaining the security posture?',
      type: 'mcq',
      options: [
        'Replace the NAT gateway with a NAT instance on a t3.micro EC2 instance in the public subnet',
        'Create a VPC endpoint for Amazon S3 and configure the instances to download updates from an S3 bucket instead of internet repositories',
        'Move the EC2 instances to public subnets with an internet gateway and use security groups to restrict inbound traffic',
        'Set up AWS PrivateLink connections to each software vendor\'s update servers',
      ],
      correctAnswer: '{1}',
      explanation: 'Gateway VPC endpoints for S3 are free — there are no data processing or hourly charges. By hosting software updates in an S3 bucket (or using AWS-native package repositories that route through S3), you eliminate NAT gateway data processing charges entirely for this traffic. Many Linux distributions and AWS tools already have S3-based repositories accessible via VPC endpoints. A NAT instance (Option A) reduces hourly costs but still incurs data transfer charges and introduces a single point of failure with operational overhead. Moving to public subnets (Option C) weakens the security architecture by exposing instances to the internet. PrivateLink (Option D) requires the software vendors to offer AWS PrivateLink endpoints, which most do not.',
      difficulty: 'intermediate',
      tags: ['nat-gateway', 'vpc-endpoints', 's3', 'cost-optimization', 'networking'],
      source: 'custom',
      certTier: 'associate',
      genMode: 'simulation',
    },
    {
      id: 'ref-saa-c03-d4-003',
      certificationCode: 'SAA-C03',
      domainId: 'domain-4',
      text: 'A company runs an internal reporting application that generates weekly PDF reports. The application currently uses a dedicated m5.xlarge EC2 instance running 24/7, but the report generation only takes approximately 45 minutes each week. Which approach provides the MOST cost-effective solution?',
      type: 'mcq',
      options: [
        'Migrate the report generation logic to an AWS Lambda function triggered by an Amazon EventBridge scheduled rule',
        'Use an EC2 Spot Instance instead of On-Demand, triggered weekly by an EventBridge rule and terminated after completion',
        'Downsize to a t3.small Reserved Instance with a 1-year commitment',
        'Containerize the application and run it on AWS Fargate with an EventBridge scheduled task',
      ],
      correctAnswer: '{0}',
      explanation: 'Lambda is the most cost-effective option for a 45-minute weekly workload. You pay only for the compute time used (45 minutes per week vs. 168 hours per week with an EC2 instance). Lambda supports up to 15-minute execution timeouts per invocation, but the report generation can be broken into smaller chunks or the process can be orchestrated with Step Functions if needed. However, if the 45 minutes is a single continuous process, Fargate (Option D) would be the next best choice. A Spot Instance (Option B) is cheaper than On-Demand but still requires managing instance lifecycle. A Reserved Instance (Option C) still pays for 24/7 capacity for a 45-minute workload.',
      difficulty: 'basic',
      tags: ['lambda', 'serverless', 'cost-optimization', 'eventbridge', 'scheduling'],
      source: 'custom',
      certTier: 'associate',
      genMode: 'drill',
    },
  ],
};
